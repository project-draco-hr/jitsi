{
  byte[] contactImage=metaContact.getAvatar();
  if (contactImage != null && contactImage.length > 0) {
    return ImageUtils.getScaledRoundedIcon(contactImage,AVATAR_ICON_WIDTH,AVATAR_ICON_HEIGHT);
  }
 else   return null;
}
