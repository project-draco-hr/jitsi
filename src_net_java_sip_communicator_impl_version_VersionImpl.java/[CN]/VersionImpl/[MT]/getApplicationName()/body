{
  if (applicationName == null) {
    try {
      if (resourcesService == null) {
        ServiceReference serviceReference=VersionActivator.bundleContext.getServiceReference(ResourceManagementService.class.getName());
        if (serviceReference == null)         return null;
        resourcesService=(ResourceManagementService)VersionActivator.bundleContext.getService(serviceReference);
      }
      applicationName=resourcesService.getSettingsString("service.gui.APPLICATION_NAME");
    }
 catch (    Exception e) {
      applicationName=DEFAULT_APPLICATION_NAME;
    }
  }
  return applicationName;
}
