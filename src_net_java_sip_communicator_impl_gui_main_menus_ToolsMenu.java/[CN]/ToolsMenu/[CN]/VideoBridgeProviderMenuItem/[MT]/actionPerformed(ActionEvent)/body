{
  ConferenceInviteDialog inviteDialog;
  if (preselectedProvider != null)   inviteDialog=new ConferenceInviteDialog(preselectedProvider,true);
 else   inviteDialog=new ConferenceInviteDialog(videoBridgeProviders,true);
  inviteDialog.setVisible(true);
}
