{
  this.initProgressBar();
  if ((searchKeyword == null || searchKeyword == "") && dateHistoryTable.containsKey(startDate)) {
    HTMLDocument document=(HTMLDocument)dateHistoryTable.get(startDate);
    this.currentDocument=document;
    this.chatConvPanel.setContent(document);
  }
 else {
    this.chatConvPanel.clear();
    new MessagesLoader(startDate,endDate).start();
  }
}
