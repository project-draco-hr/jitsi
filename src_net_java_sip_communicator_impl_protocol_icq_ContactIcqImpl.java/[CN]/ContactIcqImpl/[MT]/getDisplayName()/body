{
  if (nickName != null) {
    return nickName;
  }
 else {
    String alias=joustSimBuddy.getAlias();
    if (alias == null)     ssclCallback.addContactForUpdate(this);
    nickName=alias == null ? getUIN() : alias;
    return nickName;
  }
}
