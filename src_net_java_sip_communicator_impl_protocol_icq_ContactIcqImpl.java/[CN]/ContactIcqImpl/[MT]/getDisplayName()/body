{
  String alias=joustSimBuddy.getAlias();
  if (alias != null)   return nickName=alias;
 else   if (nickName != null)   return nickName;
 else {
    ssclCallback.addContactForUpdate(this);
    return getUIN();
  }
}
