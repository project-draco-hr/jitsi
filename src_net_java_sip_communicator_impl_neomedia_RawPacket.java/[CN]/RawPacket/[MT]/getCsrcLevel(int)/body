{
  if (!getExtensionBit() || getExtensionLength() == 0)   return 0;
  int extOffset=FIXED_HEADER_SIZE + getCsrcCount() * 4 + EXT_HEADER_SIZE;
  int extensionEnd=extOffset + getExtensionLength();
  while (extOffset < getLength()) {
  }
}
