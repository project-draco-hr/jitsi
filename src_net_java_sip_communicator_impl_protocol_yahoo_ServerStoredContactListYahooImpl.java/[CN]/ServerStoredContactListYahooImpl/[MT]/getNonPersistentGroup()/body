{
  for (int i=0; i < getRootGroup().countSubgroups(); i++) {
    ContactGroupYahooImpl gr=(ContactGroupYahooImpl)getRootGroup().getGroup(i);
    if (!gr.isPersistent())     return gr;
  }
  return null;
}
