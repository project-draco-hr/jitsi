{
  this.remove(timeLabel);
  this.remove(durationLabel);
  if (value instanceof GuiCallParticipantRecord) {
    GuiCallParticipantRecord participant=(GuiCallParticipantRecord)value;
    this.direction=participant.getDirection();
    this.nameLabel.setText(participant.getParticipantName());
    this.timeLabel.setText(Messages.getString("at") + " " + GuiUtils.formatTime(participant.getStartTime()));
    this.durationLabel.setText(Messages.getString("duration") + " " + GuiUtils.formatTime(GuiUtils.substractDates(participant.getEndTime(),participant.getStartTime())));
    this.add(timeLabel,BorderLayout.EAST);
    this.add(durationLabel,BorderLayout.SOUTH);
    this.setBorder(BorderFactory.createEmptyBorder(1,1,1,1));
    this.setBounds(0,0,list.getWidth() - 2,25);
    this.isLeaf=true;
  }
 else   if (value instanceof String) {
    String dateString=(String)value;
    this.nameLabel.setText(dateString);
    this.setBorder(BorderFactory.createEmptyBorder(2,2,2,2));
    this.setBounds(0,0,list.getWidth() - 2,20);
    this.isLeaf=false;
  }
  this.isSelected=isSelected;
  return this;
}
