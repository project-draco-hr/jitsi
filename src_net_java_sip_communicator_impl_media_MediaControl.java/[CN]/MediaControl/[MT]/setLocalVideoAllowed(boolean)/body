{
  if (localVideoAllowed != allowed) {
    localVideoAllowed=allowed;
    if (sourceProcessor != null)     sourceProcessor.stop();
    initCaptureDevices();
    if (sourceProcessor.getState() != Processor.Started)     sourceProcessor.start();
  }
}
