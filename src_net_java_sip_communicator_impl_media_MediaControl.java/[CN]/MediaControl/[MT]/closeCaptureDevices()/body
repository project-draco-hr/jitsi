{
  try {
    if (avDataSource != null)     avDataSource.stop();
  }
 catch (  IOException exc) {
    logger.error("Failed to close a capture data source.",exc);
    throw new MediaException("Failed to close a capture data source.",MediaException.INTERNAL_ERROR,exc);
  }
}
