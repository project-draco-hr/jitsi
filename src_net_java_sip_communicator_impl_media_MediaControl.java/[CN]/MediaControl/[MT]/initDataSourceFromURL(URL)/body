{
  if (logger.isDebugEnabled())   logger.debug("Using a data source from url: " + dataSourceURL);
  avDataSource=createDataSource(new MediaLocator(dataSourceURL));
  if (avDataSource != null)   initProcessor(avDataSource);
}
