{
  if (sourceProcessor == null)   return;
  if (sourceProcessor.getState() == Processor.Started)   sourceProcessor.stop();
  if (processorReaders.contains(reader))   processorReaders.remove(reader);
}
