{
  JButton button=new JButton(new ImageIcon(ImageLoader.getImage(ImageLoader.ENTER_FULL_SCREEN_BUTTON)));
  button.setToolTipText(GuiActivator.getResources().getI18NString("CallParticipantPanel_enterFullScreenButtonToolTipText"));
  button.addActionListener(new ActionListener(){
    public void actionPerformed(    ActionEvent event){
      enterFullScreen();
    }
  }
);
  return button;
}
