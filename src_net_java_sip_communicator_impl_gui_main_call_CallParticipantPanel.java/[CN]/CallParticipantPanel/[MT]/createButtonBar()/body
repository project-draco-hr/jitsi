{
  Component[] buttons=new Component[]{new HoldButton(callParticipant),new MuteButton(callParticipant),createTransferCallButton(),createSecureCallButton(),createEnterFullScreenButton()};
  Dimension preferredButtonSize=new Dimension(24,24);
  return createButtonBar(buttons,preferredButtonSize);
}
