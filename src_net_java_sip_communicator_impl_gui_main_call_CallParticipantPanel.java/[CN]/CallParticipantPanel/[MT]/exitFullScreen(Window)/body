{
  GraphicsConfiguration gConfig=getGraphicsConfiguration();
  if (gConfig != null)   gConfig.getDevice().setFullScreenWindow(null);
  if (fullScreenWindow != null) {
    if (fullScreenWindow.isVisible())     fullScreenWindow.setVisible(false);
    fullScreenWindow.dispose();
  }
}
