{
  Component[] buttons=new Component[]{new HoldButton(callParticipant.getCall(),true),new MuteButton(callParticipant.getCall()),createExitFullScreenButton()};
  return createButtonBar(true,buttons);
}
