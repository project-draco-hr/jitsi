{
  Component[] buttons=new Component[]{new HoldButton(callParticipant),new MuteButton(callParticipant),createExitFullScreenButton()};
  Dimension preferredButtonSize=new Dimension(36,36);
  return createButtonBar(true,buttons,preferredButtonSize);
}
