{
  ExportedWindow loginWindow=SystrayActivator.getUIService().getAuthenticationWindow(handlerProvider,realm,userCredentials,isUserNameEditable);
  loginWindow.setVisible(true);
  return userCredentials;
}
