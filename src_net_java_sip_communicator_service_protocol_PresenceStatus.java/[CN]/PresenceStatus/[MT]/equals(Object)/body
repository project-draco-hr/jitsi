{
  if (obj == null || !(obj instanceof PresenceStatus))   return false;
  PresenceStatus status=(PresenceStatus)obj;
  if (status.getStatus() != getStatus() || !status.getStatusName().equals(statusName))   return false;
  return true;
}
