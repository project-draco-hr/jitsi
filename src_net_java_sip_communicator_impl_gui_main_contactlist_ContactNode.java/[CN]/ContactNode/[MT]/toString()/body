{
  String result="";
  if (userObject == null) {
    return null;
  }
 else {
    if (userObject instanceof ContactItem)     result=((ContactItem)userObject).getNickName();
 else     if (userObject instanceof GroupItem)     result=((GroupItem)userObject).getGroupName();
  }
  return result;
}
