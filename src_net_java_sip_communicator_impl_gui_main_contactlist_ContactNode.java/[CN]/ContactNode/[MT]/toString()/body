{
  String result="";
  if (userObject == null) {
    return null;
  }
 else {
    if (userObject instanceof MetaContact)     result=((MetaContact)userObject).getDisplayName();
 else     if (userObject instanceof MetaContactGroup)     result=((MetaContactGroup)userObject).getGroupName();
  }
  return result;
}
