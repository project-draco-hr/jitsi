{
  if (!super.matches(format))   return false;
  if (!(format instanceof AVFrameFormat))   return true;
  AVFrameFormat avFrameFormat=(AVFrameFormat)format;
  return (pixFmt == NOT_SPECIFIED || (avFrameFormat.pixFmt == NOT_SPECIFIED) || (pixFmt == avFrameFormat.pixFmt));
}
