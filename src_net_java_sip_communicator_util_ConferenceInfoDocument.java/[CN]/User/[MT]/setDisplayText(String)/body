{
  Element displayText=XMLUtils.findChild(userElement,"display-text");
  if (displayText == null) {
    displayText=document.createElement("display-text");
    userElement.appendChild(displayText);
  }
  displayText.setTextContent(text);
}
