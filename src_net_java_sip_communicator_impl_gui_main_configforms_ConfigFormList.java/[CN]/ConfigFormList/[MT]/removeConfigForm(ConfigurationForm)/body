{
  for (int i=0; i < listModel.getSize(); i++) {
    ConfigFormDescriptor descriptor=(ConfigFormDescriptor)listModel.get(i);
    if (descriptor.getConfigForm().equals(configForm))     listModel.removeElement(descriptor);
  }
}
