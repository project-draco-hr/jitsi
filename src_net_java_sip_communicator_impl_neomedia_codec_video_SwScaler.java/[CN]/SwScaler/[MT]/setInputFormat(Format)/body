{
  Format inputFormat=(format instanceof VideoFormat) ? super.setInputFormat(format) : null;
  if (inputFormat != null) {
    if (logger.isDebugEnabled())     logger.debug("SwScaler set to input in " + inputFormat);
  }
  return inputFormat;
}
