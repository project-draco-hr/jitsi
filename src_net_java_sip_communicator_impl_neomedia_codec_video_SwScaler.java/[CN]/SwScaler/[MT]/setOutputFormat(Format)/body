{
  Format outputFormat=super.setOutputFormat(format);
  if (logger.isDebugEnabled() && (outputFormat != null))   logger.debug("SwScaler set to output with size " + ((VideoFormat)outputFormat).getSize());
  return outputFormat;
}
