{
  String callPeerID=callPeer.getPeerID();
synchronized (listeners) {
    for (int i=0; i < listeners.size(); ++i) {
      if (listeners.get(i).get().getCallPeer().getPeerID().equals(callPeerID)) {
        return listeners.get(i).get();
      }
    }
  }
  return null;
}
