{
  List<RemoteControlListener> listeners=getListeners();
  if (!listeners.isEmpty()) {
    RemoteControlGrantedEvent event=new RemoteControlGrantedEvent(this);
    for (    RemoteControlListener l : listeners)     l.remoteControlGranted(event);
  }
}
