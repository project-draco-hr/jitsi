{
  if (contact == null)   return;
  if (!isContactVerified(contact))   return;
  this.configurator.removeProperty(getSessionID(contact) + "publicKey.verified");
  for (  ScOtrEngineListener l : listeners)   l.contactVerificationStatusChanged(contact);
}
