{
  String id=getSessionNS(getSessionID(contact),"policy");
  if (id == null || id.length() < 1)   return getGlobalPolicy();
  int policy=OtrActivator.configService.getInt(id,-1);
  if (policy < 0)   return getGlobalPolicy();
 else   return new OtrPolicyImpl(policy);
}
