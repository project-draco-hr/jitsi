{
  if (StringUtils.isNullOrEmpty(videobridge)) {
    throw new NullPointerException("videobridge");
  }
  request.setTo(videobridge);
  if (requestType == RequestType.EXPIRE_CHANNELS && !hasAnyChannelsToExpire) {
    return null;
  }
  return request;
}
