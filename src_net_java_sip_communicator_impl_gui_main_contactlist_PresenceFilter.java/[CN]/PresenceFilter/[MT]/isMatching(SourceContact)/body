{
  return isShowOffline || contact.getPresenceStatus().isOnline() || GuiActivator.getMUCService().isMUCSourceContact(contact)|| contact.getContactSource().getType() == ContactSourceService.RECENT_MESSAGES_TYPE;
}
