{
  super.paintComponent(g);
  Graphics2D g2=(Graphics2D)g;
  GradientPaint p=new GradientPaint(this.getWidth() / 2,0,gradientStartColor,this.getWidth() / 2,getHeight(),gradientEndColor);
  g2.setPaint(p);
  g2.fillRoundRect(0,0,this.getWidth(),getHeight(),10,10);
}
