{
  if (candidateList.size() > 0)   return candidateList;
 else   if (remoteCandidate != null) {
    List<RemoteCandidatePacketExtension> list=new ArrayList<RemoteCandidatePacketExtension>();
    list.add(remoteCandidate);
    return list;
  }
  return null;
}
