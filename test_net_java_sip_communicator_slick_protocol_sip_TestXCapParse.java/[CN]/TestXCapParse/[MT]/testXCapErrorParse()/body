{
  XCapErrorType cannotDelete=XCapErrorParser.fromXml(XCAP_ERROR_CANNOT_DELETE_XML);
  validateXCapErrorConnotDelete(cannotDelete);
  XCapErrorType uniquenessFailure=XCapErrorParser.fromXml(XCAP_ERROR_UNIQUENESS_FAILURE_XML);
  validateXCapErrorUniquenessFailure(uniquenessFailure);
}
