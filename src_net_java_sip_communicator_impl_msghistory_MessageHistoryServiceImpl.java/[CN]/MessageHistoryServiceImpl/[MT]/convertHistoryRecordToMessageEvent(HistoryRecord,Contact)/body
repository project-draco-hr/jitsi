{
  MessageImpl msg=new MessageImpl(hr);
  if (msg.isOutgoing) {
    return new MessageDeliveredEvent(new MessageImpl(hr),contact,hr.getTimestamp());
  }
 else   return new MessageReceivedEvent(new MessageImpl(hr),contact,hr.getTimestamp());
}
