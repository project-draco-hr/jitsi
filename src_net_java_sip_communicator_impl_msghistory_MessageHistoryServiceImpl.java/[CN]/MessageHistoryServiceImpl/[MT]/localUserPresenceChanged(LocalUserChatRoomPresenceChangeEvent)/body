{
  if (evt.getEventType() == LocalUserChatRoomPresenceChangeEvent.CHAT_ROOM_JOINED) {
    evt.getChatRoom().addMessageListener(this);
  }
 else {
    evt.getChatRoom().removeMessageListener(this);
  }
}
