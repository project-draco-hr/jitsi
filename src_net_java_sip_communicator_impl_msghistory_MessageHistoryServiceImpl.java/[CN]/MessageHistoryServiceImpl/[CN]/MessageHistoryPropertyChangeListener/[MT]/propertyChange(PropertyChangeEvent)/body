{
  String newPropertyValue=(String)evt.getNewValue();
  boolean isMessageHistoryEnabled=new Boolean(newPropertyValue).booleanValue();
  if (isMessageHistoryEnabled)   loadMessageHistoryService();
 else   stop(bundleContext);
}
