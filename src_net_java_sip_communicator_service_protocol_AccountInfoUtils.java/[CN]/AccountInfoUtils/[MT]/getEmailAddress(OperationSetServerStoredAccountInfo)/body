{
  EmailAddressDetail address=null;
  Iterator<EmailAddressDetail> addressDetails=accountInfoOpSet.getDetails(ServerStoredDetails.EmailAddressDetail.class);
  if (addressDetails.hasNext()) {
    address=addressDetails.next();
  }
  return address.getEMailAddress();
}
