{
  BirthDateDetail date=null;
  Iterator<BirthDateDetail> dateDetails=accountInfoOpSet.getDetails(ServerStoredDetails.BirthDateDetail.class);
  if (dateDetails.hasNext()) {
    date=dateDetails.next();
  }
  return date.getCalendar();
}
