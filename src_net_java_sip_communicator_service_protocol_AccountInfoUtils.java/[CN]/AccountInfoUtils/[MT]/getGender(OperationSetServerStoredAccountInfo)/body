{
  GenderDetail gender=null;
  Iterator<GenderDetail> genderDetails=accountInfoOpSet.getDetails(ServerStoredDetails.GenderDetail.class);
  if (genderDetails.hasNext()) {
    gender=genderDetails.next();
  }
  return gender.getGender();
}
