{
  Vector result=new Vector();
  Iterator iter=rs.iterator();
  while (iter.hasNext()) {
    Object item=iter.next();
    if (item instanceof CallRecord)     result.add(item);
  }
  return result;
}
