{
  this.moveDialog.dispose();
  if (toMetaContact.equals(contactItem)) {
    JOptionPane.showMessageDialog(this.mainFrame,Messages.getI18NString("moveSubcontactInSameContact").getText(),Messages.getI18NString("moveSubcontact").getText(),JOptionPane.WARNING_MESSAGE);
  }
 else {
    guiContactList.removeExcContactListListener(this);
    guiContactList.setCursor(Cursor.getPredefinedCursor(Cursor.HAND_CURSOR));
    if (moveAllContacts) {
      new MoveAllSubcontactsThread(toMetaContact).start();
    }
 else     if (contactToMove != null) {
      new MoveSubcontactThread(toMetaContact).start();
    }
  }
}
