{
  MediaLocator locator=this.locator;
  if (locator == null) {
    CaptureDeviceInfo playbackDevice=pulseAudioSystem.getPlaybackDevice();
    if (playbackDevice != null)     locator=playbackDevice.getLocator();
  }
  return locator;
}
