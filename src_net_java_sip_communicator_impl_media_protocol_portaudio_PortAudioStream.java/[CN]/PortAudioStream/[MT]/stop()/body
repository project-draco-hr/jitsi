{
synchronized (this) {
    this.started=false;
    try {
      PortAudio.Pa_CloseStream(getStream());
      stream=0;
    }
 catch (    PortAudioException paex) {
      paex.printStackTrace();
    }
  }
}
