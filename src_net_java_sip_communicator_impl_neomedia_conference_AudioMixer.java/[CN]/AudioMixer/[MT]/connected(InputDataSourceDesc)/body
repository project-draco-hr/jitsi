{
synchronized (inputDataSources) {
    if (inputDataSources.contains(inputDataSource) && (connected > 0)) {
      if (started > 0)       inputDataSource.start();
      if (outputStream != null)       getOutputStream();
    }
  }
}
