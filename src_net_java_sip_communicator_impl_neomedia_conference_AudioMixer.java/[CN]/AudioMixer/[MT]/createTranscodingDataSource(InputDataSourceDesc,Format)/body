{
  if (inputDataSourceDesc.createTranscodingDataSource(outputFormat)) {
    if (connected > 0)     inputDataSourceDesc.connect(this);
    if (started > 0)     inputDataSourceDesc.start();
    return true;
  }
 else   return false;
}
