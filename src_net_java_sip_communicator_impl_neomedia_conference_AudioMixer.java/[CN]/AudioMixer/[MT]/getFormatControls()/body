{
  FormatControl[] formatControls=captureDevice.getFormatControls();
  if (formatControls != null)   for (int i=0; i < formatControls.length; i++)   formatControls[i]=new ReadOnlyFormatControlDelegate(formatControls[i]);
  return formatControls;
}
