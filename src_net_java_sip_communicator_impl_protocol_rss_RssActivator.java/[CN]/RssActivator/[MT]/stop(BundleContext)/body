{
  RssActivator.rssProviderFactory.stop();
  rssPpFactoryServReg.unregister();
  context.removeServiceListener(uriHandler);
  if (logger.isInfoEnabled())   logger.info("RSS protocol implementation [STOPPED].");
}
