{
  if (uiService == null) {
    ServiceReference serviceReference=bundleContext.getServiceReference(UIService.class.getName());
    if (serviceReference != null)     uiService=(UIService)bundleContext.getService(serviceReference);
  }
  return uiService;
}
