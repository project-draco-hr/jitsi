{
  int idx=fullJID.indexOf('/');
  if (idx != -1) {
    String res=fullJID.substring(idx + 1);
    if (res.startsWith("android") || res.startsWith("Vtok")) {
      return true;
    }
  }
  if (fullJID.contains("@voice.google.com"))   return true;
  return false;
}
