{
  super(userID,accountProperties,"Zeroconf","zeroconf.org");
  first=(String)accountProperties.get("first");
  last=(String)accountProperties.get("last");
  mail=(String)accountProperties.get("mail");
  rememberContacts=new Boolean((String)accountProperties.get("rememberContacts")).booleanValue();
}
