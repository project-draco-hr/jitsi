{
  super(userID,accountProperties,ProtocolNames.ZEROCONF,"zeroconf.org");
  first=accountProperties.get("first");
  last=accountProperties.get("last");
  mail=accountProperties.get("mail");
  rememberContacts=new Boolean(accountProperties.get("rememberContacts")).booleanValue();
}
