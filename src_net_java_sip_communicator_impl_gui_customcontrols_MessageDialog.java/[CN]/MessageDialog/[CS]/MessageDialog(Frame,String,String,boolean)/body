{
  this(owner,title,message);
  if (!isCancelButtonEnabled) {
    doNotAskAgain.setText(GuiActivator.getResources().getI18NString("service.gui.DO_NOT_SHOW_AGAIN"));
    buttonsPanel.remove(cancelButton);
  }
}
