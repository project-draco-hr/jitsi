{
  if (!isMute)   soundButton.setBackgroundImage(ImageLoader.getImage(ImageLoader.QUICK_MENU_SOUND_ON_ICON));
 else   soundButton.setBackgroundImage(ImageLoader.getImage(ImageLoader.QUICK_MENU_SOUND_OFF_ICON));
  soundButton.repaint();
  this.getParent().repaint();
}
