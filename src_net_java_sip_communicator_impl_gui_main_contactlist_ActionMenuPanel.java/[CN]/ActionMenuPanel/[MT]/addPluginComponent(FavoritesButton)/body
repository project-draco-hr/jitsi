{
  SIPCommButton button=addMoreActionsButton(ImageLoader.getBytesInImage(c.getImage()),c.getText(),null);
  button.addActionListener(new ActionListener(){
    public void actionPerformed(    ActionEvent e){
      c.actionPerformed();
    }
  }
);
}
