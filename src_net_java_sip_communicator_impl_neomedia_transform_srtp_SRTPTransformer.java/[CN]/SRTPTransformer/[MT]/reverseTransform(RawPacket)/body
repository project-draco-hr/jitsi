{
  if ((pkt.readByte(0) & 0xC0) != 0x80)   return null;
  SRTPCryptoContext context=getContext(pkt.getSSRC(),reverseEngine,pkt.getSequenceNumber());
  return ((context != null) && context.reverseTransformPacket(pkt)) ? pkt : null;
}
