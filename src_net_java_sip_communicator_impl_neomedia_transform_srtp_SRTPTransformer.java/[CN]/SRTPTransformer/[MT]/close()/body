{
  forwardEngine.close();
  if (forwardEngine != reverseEngine)   reverseEngine.close();
  for (  Long ssrc : contexts.keySet()) {
    SRTPCryptoContext context=contexts.get(ssrc);
    if (context != null) {
      context.close();
      contexts.remove(ssrc);
    }
  }
}
