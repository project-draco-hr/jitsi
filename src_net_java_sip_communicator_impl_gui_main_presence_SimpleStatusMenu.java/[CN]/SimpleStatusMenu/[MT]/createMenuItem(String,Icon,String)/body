{
  JMenuItem menuItem=new JMenuItem(GuiActivator.getResources().getI18NString(textKey),icon);
  menuItem.setName(Constants.ONLINE_STATUS);
  menuItem.addActionListener(this);
  this.add(menuItem);
  return menuItem;
}
