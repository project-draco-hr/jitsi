{
  if (osName.startsWith("Mac")) {
    for (int i=0; i < legacyConfigurationFileNames.length; i++) {
      if (new File(new File(parent,name),legacyConfigurationFileNames[i]).exists())       return true;
    }
    return false;
  }
  return new File(parent,name).isDirectory();
}
