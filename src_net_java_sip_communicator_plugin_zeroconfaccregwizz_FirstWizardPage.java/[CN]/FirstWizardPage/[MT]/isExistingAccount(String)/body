{
  ProtocolProviderFactory factory=ZeroconfAccRegWizzActivator.getZeroconfProtocolProviderFactory();
  for (  AccountID accountID : factory.getRegisteredAccounts()) {
    if (userID.equalsIgnoreCase(accountID.getUserID()))     return true;
  }
  return false;
}
