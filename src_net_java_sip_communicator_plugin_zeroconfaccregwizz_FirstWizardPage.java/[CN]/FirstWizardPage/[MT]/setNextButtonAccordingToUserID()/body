{
  if (userIDField.getText() == null || userIDField.getText().equals("") || firstField.getText() == null || firstField.getText().equals("")) {
    wizardContainer.setNextFinishButtonEnabled(false);
  }
 else {
    wizardContainer.setNextFinishButtonEnabled(true);
  }
}
