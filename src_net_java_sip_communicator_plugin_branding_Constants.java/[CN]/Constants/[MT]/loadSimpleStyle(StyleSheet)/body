{
  InputStream is=Constants.class.getClassLoader().getResourceAsStream(BrandingResources.getString("textStyle"));
  Reader r=new BufferedReader(new InputStreamReader(is));
  try {
    style.loadRules(r,null);
    r.close();
  }
 catch (  IOException e) {
  }
}
