{
  isTypedPlus=true;
  plusZeroTimer.stop();
  if (dtmfOpSet != null) {
    sendDtmfTone(DTMFTone.DTMF_0);
    sendDtmfTone(DTMFTone.DTMF_0);
  }
 else {
    ComboBoxEditor phoneNumberEditor=phoneNumberCombo.getEditor();
    phoneNumberEditor.setItem(phoneNumberEditor.getItem() + "+");
  }
}
