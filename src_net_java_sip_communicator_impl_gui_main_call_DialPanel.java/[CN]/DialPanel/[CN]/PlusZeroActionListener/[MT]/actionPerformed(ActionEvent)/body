{
  isTypedPlus=true;
  plusZeroTimer.stop();
  if (parentCallPanel == null) {
    sendDtmfTone(DTMFTone.DTMF_0);
    sendDtmfTone(DTMFTone.DTMF_0);
  }
 else {
    parentCallPanel.setPhoneNumberComboText(parentCallPanel.getPhoneNumberComboText() + "+");
  }
}
