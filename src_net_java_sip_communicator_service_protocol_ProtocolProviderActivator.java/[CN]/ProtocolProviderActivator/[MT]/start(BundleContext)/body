{
  ProtocolProviderActivator.bundleContext=bundleContext;
  accountManagerServiceRegistration=bundleContext.registerService(AccountManager.class.getName(),new AccountManager(bundleContext),null);
  singleCallInProgressPolicy=new SingleCallInProgressPolicy(bundleContext);
}
