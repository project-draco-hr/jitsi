{
  try {
    Request register=clientTransaction.getRequest();
    ClientTransaction retryTran=sipProvider.getSipSecurityManager().handleChallenge(response,clientTransaction,getRegistrarJainSipProvider());
    retryTran.sendRequest();
    return;
  }
 catch (  Exception exc) {
  }
}
