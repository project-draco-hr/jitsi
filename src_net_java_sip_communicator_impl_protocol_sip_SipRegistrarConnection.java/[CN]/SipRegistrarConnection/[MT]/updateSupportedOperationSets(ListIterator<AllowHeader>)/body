{
  HashSet<String> set=new HashSet<String>();
  while (headerIter.hasNext()) {
    set.add(headerIter.next().getMethod());
  }
  if (!set.contains(Request.MESSAGE))   sipProvider.removeSupportedOperationSet(OperationSetBasicInstantMessaging.class);
}
