{
  logger.trace("DTMF send on RTP, code : " + code + " "+ "dur = "+ duration+ " "+ "ts = "+ timestamp+ " "+ (marker ? "Marker" : "")+ (end ? "End" : ""));
  setMarker(marker);
  setTimestamp(timestamp);
  setData(code,end,duration);
}
