{
  SecurityAuthorityImpl secAuth=new SecurityAuthorityImpl(protocolProvider);
  mainFrame.getAccountStatusPanel().startConnecting(protocolProvider);
  new RegisterProvider(protocolProvider,secAuth).start();
}
