{
  if (call.isConferenceFocus()) {
synchronized (objSync) {
      Iterator<? extends CallPeer> callPeerIter=call.getCallPeers();
      while (callPeerIter.hasNext())       notify(callPeerIter.next());
      version++;
    }
  }
}
