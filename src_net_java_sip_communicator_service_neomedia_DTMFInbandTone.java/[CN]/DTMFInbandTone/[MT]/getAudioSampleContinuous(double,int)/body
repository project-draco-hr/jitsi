{
  double u1=2.0 * Math.PI * this.frequency1 / samplingFrequency;
  double u2=2.0 * Math.PI * this.frequency2 / samplingFrequency;
  double audioSample;
  audioSample=Math.sin(u1 * sampleNumber) * 0.5 + Math.sin(u2 * sampleNumber) * 0.5;
  return audioSample;
}
