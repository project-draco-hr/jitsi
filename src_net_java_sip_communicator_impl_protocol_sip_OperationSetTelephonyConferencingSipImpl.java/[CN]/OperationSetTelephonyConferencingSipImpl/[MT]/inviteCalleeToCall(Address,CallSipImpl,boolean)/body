{
  if (!wasConferenceFocus && call.isConferenceFocus()) {
    Iterator<CallPeerSipImpl> callPeerIter=call.getCallPeers();
    while (callPeerIter.hasNext())     callPeerIter.next().sendReInvite();
  }
  return call.invite(calleeAddress,null);
}
