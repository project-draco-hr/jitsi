{
  if (logger.isTraceEnabled())   logger.trace("Created a chat with " + chat.getParticipant() + " local="+ isLocal);
  if (smackChatStateListener == null)   smackChatStateListener=new SmackChatStateListener();
  chat.addMessageListener(smackChatStateListener);
}
