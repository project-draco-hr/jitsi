{
  int cIndex=form.getIndex();
  String formTitle=form.getTitle();
  Component formComponent=(Component)form.getForm();
  if (cIndex >= getTabCount())   addTab(formTitle,formComponent);
 else   insertTab(formTitle,null,formComponent,formTitle,cIndex);
}
