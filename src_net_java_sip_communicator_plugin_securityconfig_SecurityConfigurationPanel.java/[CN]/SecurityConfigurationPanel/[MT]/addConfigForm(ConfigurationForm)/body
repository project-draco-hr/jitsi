{
  int index=form.getIndex();
  String title=form.getTitle();
  Component component=(Component)form.getForm();
  if (index >= getTabCount())   addTab(title,component);
 else   insertTab(title,null,component,title,index);
}
