{
  CaptureDeviceInfo captureDeviceInfo;
switch (mediaType) {
case AUDIO:
    captureDeviceInfo=mediaServiceImpl.getDeviceConfiguration().getAudioCaptureDevice();
  break;
case VIDEO:
captureDeviceInfo=mediaServiceImpl.getDeviceConfiguration().getVideoCaptureDevice();
break;
default :
captureDeviceInfo=null;
break;
}
return (captureDeviceInfo == null) ? null : new CaptureMediaDevice(captureDeviceInfo,mediaType);
}
