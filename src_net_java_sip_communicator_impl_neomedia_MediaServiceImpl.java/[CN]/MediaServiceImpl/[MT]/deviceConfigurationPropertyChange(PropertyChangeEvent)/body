{
  String propertyName=event.getPropertyName();
  if (DeviceConfiguration.AUDIO_CAPTURE_DEVICE.equals(propertyName) || DeviceConfiguration.VIDEO_CAPTURE_DEVICE.equals(propertyName)) {
    firePropertyChange(DEFAULT_DEVICE,null,null);
  }
}
