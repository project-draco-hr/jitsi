{
  AccountID accountID=protocolProvider.getAccountID();
  String password=(String)accountID.getAccountProperties().get(ProtocolProviderFactory.PASSWORD);
  String server=(String)accountID.getAccountProperties().get(ProtocolProviderFactory.SERVER_ADDRESS);
  String port=(String)accountID.getAccountProperties().get(ProtocolProviderFactory.SERVER_PORT);
  String autoNickChange=(String)accountID.getAccountProperties().get(ProtocolProviderFactory.AUTO_CHANGE_USER_NAME);
  String passwordRequired=(String)accountID.getAccountProperties().get(ProtocolProviderFactory.PASSWORD_REQUIRED);
  this.nickField.setEnabled(false);
  this.nickField.setText(accountID.getUserID());
  this.serverField.setText(server);
  if (password != null) {
    this.passField.setText(password);
    this.rememberPassBox.setSelected(true);
  }
  if (port != null) {
    this.portField.setText(port);
    this.portField.setEnabled(true);
    this.defaultPort.setSelected(false);
  }
  if (autoNickChange != null) {
    this.autoNickChange.setSelected(new Boolean(autoNickChange).booleanValue());
  }
  if (passwordRequired != null) {
    boolean isPassRequired=new Boolean(passwordRequired).booleanValue();
    this.passwordNotRequired.setSelected(!isPassRequired);
    passField.setEnabled(isPassRequired);
  }
}
