{
  this.protocolProvider=protocolProvider;
  this.systemRoomWrapper=new ChatRoomWrapper(protocolProvider,protocolProvider.getAccountID().getService(),protocolProvider.getAccountID().getService());
}
