{
  if (resourcesService == null) {
    ServiceReference serviceReference=StatusUpdateActivator.bundleContext.getServiceReference(ResourceManagementService.class.getName());
    if (serviceReference == null)     return null;
    resourcesService=(ResourceManagementService)StatusUpdateActivator.bundleContext.getService(serviceReference);
  }
  return resourcesService;
}
