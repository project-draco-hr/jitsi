{
  this.setOpaque(true);
  try {
    bgImage=ImageIO.read(BrandingActivator.getResources().getImageURL("splashScreenBg"));
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
  this.setPreferredSize(new Dimension(bgImage.getWidth(this),bgImage.getHeight(this)));
}
