{
  if (i >= formatList.length)   expandTable(i);
  Format format1;
  if ((format1=formatList[i]) != null)   return;
  formatList[i]=format;
  if (cache != null && (format instanceof VideoFormat))   cache.clockrate[i]=0x15f90;
  if (cache != null && (format instanceof AudioFormat))   if (mpegAudio.matches(format))   cache.clockrate[i]=0x15f90;
 else   cache.clockrate[i]=(int)((AudioFormat)format).getSampleRate();
}
