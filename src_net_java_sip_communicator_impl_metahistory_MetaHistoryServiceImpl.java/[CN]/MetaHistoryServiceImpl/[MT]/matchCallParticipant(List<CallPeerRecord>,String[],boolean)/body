{
  Iterator<CallPeerRecord> iter=cps.iterator();
  while (iter.hasNext()) {
    boolean match=false;
    CallPeerRecord callParticipant=iter.next();
    for (int i=0; i < keywords.length; i++) {
      String k=keywords[i];
      if (caseSensitive) {
        if (callParticipant.getPeerAddress().contains(k)) {
          match=true;
        }
 else {
          match=false;
          break;
        }
        continue;
      }
 else       if (callParticipant.getPeerAddress().toLowerCase().contains(k.toLowerCase())) {
        match=true;
      }
 else {
        match=false;
        break;
      }
    }
    if (match)     return true;
  }
  return false;
}
