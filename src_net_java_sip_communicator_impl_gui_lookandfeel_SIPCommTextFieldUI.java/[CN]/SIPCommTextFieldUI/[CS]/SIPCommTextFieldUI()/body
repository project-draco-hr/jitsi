{
  deleteButtonImg=ImageLoader.getImage(ImageLoader.DELETE_TEXT_ICON);
  deleteButtonRolloverImg=ImageLoader.getImage(ImageLoader.DELETE_TEXT_ROLLOVER_ICON);
  deleteButton=new SIPCommButton(deleteButtonImg,deleteButtonRolloverImg);
  deleteButton.setSize(deleteButtonImg.getWidth(null),deleteButtonImg.getHeight(null));
}
