{
  PluginComponent component=event.getPluginComponent();
  if (!component.getContainer().equals(Container.CONTAINER_MAIN_TOOL_BAR))   return;
  boolean added=addPluginComponent(component,GuiActivator.getContactList().getSelectionPath().getLastPathComponent());
  if (added) {
    this.revalidate();
    this.repaint();
  }
}
