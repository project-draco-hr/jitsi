{
  String oldAddress=getAddress();
  if (participantAddress.equals(address))   return;
  this.participantAddress=address;
  fireCallPeerChangeEvent(CallPeerChangeEvent.CALL_PEER_ADDRESS_CHANGE,oldAddress,address.toString());
}
