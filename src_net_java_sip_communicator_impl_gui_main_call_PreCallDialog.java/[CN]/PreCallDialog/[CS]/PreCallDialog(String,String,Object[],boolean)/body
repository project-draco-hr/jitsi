{
  preCallWindow=createPreCallWindow(title,text,accounts);
  this.video=video;
  this.video=video && GuiActivator.getMediaService().getDefaultDevice(MediaType.VIDEO,MediaUseCase.CALL).getDirection().allowsSending();
  this.initComponents();
}
