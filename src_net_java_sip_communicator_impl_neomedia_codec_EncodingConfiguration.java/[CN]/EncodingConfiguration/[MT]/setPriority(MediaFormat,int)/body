{
  encodingPreferences.put(encoding,priority);
  NeomediaActivator.getConfigurationService().setProperty(PROP_SDP_PREFERENCE + "." + encoding.getEncoding()+ "/"+ encoding.getClockRateString(),priority);
  updateSupportedEncodings();
}
