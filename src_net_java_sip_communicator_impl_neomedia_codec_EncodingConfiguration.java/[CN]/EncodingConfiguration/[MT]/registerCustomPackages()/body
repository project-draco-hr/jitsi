{
  @SuppressWarnings("unchecked") Vector<String> packages=PackageManager.getProtocolPrefixList();
  boolean loggerIsDebugEnabled=logger.isDebugEnabled();
  for (  String customPackage : CUSTOM_PACKAGES) {
    if (!packages.contains(customPackage)) {
      packages.add(customPackage);
      if (loggerIsDebugEnabled)       if (logger.isDebugEnabled())       logger.debug("Adding package  : " + customPackage);
    }
  }
  PackageManager.setProtocolPrefixList(packages);
  PackageManager.commitProtocolPrefixList();
  if (loggerIsDebugEnabled) {
    if (logger.isDebugEnabled())     logger.debug("Registering new protocol prefix list: " + packages);
  }
}
