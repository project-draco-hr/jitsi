{
  this.rssURL=contactRssURL;
  if (persistentData != null) {
    this.lastItemKey=RssItemKey.deserialize(persistentData);
    if (this.lastItemKey == null) {
      logger.error("Failed to deserialize RSS settings: " + persistentData);
    }
  }
  this.retrieveFlow();
}
