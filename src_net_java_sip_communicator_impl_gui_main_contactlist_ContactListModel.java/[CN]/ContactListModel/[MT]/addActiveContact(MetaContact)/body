{
synchronized (activeContacts) {
    if (activeContacts.size() == 0) {
      GuiActivator.getSystrayService().setSystrayIcon(ImageLoader.getImageInBytes(ImageLoader.SYSTRAY_ENVELOPE_ICON));
    }
    this.activeContacts.add(metaContact);
  }
}
