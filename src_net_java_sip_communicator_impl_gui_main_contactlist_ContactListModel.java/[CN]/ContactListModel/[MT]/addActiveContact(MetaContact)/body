{
synchronized (activeContacts) {
    if (activeContacts.size() == 0)     GuiActivator.getSystrayService().setSystrayIcon(SystrayService.ENVELOPE_IMG_TYPE);
    if (!activeContacts.contains(metaContact))     this.activeContacts.add(metaContact);
  }
}
