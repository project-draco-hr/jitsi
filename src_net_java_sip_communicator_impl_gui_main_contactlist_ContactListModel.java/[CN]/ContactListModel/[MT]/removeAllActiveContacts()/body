{
synchronized (activeContacts) {
    if (activeContacts.size() > 0) {
      this.activeContacts.removeAllElements();
      GuiActivator.getSystrayService().setSystrayIcon(ImageLoader.getImageInBytes(ImageLoader.SYSTRAY_ICON));
    }
  }
}
