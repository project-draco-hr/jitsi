{
synchronized (activeContacts) {
    this.activeContacts.remove(metaContact);
    if (activeContacts.size() == 0)     GuiActivator.getSystrayService().setSystrayIcon(ImageLoader.getImageInBytes(ImageLoader.SYSTRAY_ICON));
  }
}
