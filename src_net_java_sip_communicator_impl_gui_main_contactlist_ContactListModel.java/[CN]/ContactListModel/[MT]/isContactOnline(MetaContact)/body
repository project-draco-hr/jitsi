{
  if (contact.getDefaultContact() == null)   return false;
  if (contact.getDefaultContact().getPresenceStatus().getStatus() >= PresenceStatus.ONLINE_THRESHOLD) {
    return true;
  }
 else {
    return false;
  }
}
