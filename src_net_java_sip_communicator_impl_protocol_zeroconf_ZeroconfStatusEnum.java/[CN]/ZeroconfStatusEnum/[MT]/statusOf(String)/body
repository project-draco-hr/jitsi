{
  Iterator<PresenceStatus> statusIter=supportedStatusSet();
  while (statusIter.hasNext()) {
    ZeroconfStatusEnum state=(ZeroconfStatusEnum)statusIter.next();
    if (state.statusName.equalsIgnoreCase(status))     return state;
  }
  return null;
}
