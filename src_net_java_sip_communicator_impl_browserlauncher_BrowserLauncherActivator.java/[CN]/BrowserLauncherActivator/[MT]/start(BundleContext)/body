{
  bundleContext=bc;
  this.browserLauncher=new BrowserLauncherImpl();
  logger.info("UI Service...[  STARTED ]");
  bundleContext.registerService(BrowserLauncherService.class.getName(),this.browserLauncher,null);
  logger.info("UI Service ...[REGISTERED]");
}
