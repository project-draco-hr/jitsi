{
  try {
    provider.getYahooSession().extendConference(yahooConference,userAddress,reason);
  }
 catch (  IOException ioe) {
    if (logger.isDebugEnabled())     logger.debug("Failed to invite the user: " + userAddress + " Error: "+ ioe);
  }
}
