{
  Iterator<Contact> participantsIter=this.participants.values().iterator();
  while (participantsIter.hasNext()) {
    Contact contact=participantsIter.next();
    if (contact.getAddress().equals(address)) {
      return contact;
    }
  }
  return null;
}
