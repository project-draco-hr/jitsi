{
  if (enabled) {
    getConfigurationService().setProperty(PACKET_LOGGING_ICE4J_ENABLED_PROPERTY_NAME,Boolean.TRUE);
  }
 else {
    getConfigurationService().removeProperty(PACKET_LOGGING_ICE4J_ENABLED_PROPERTY_NAME);
  }
  ice4jLoggingEnabled=true;
}
