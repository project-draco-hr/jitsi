{
  if (enabled) {
    getConfigurationService().setProperty(PACKET_LOGGING_RTP_ENABLED_PROPERTY_NAME,Boolean.TRUE);
  }
 else {
    getConfigurationService().removeProperty(PACKET_LOGGING_RTP_ENABLED_PROPERTY_NAME);
  }
  rtpLoggingEnabled=true;
}
