{
  if (line.length() <= END_OF_MODE_COMMAND_PREFIX) {
    return;
  }
  final String rawModeString=line.substring(END_OF_MODE_COMMAND_PREFIX);
  this.connection.getClient().rawMessage("MODE " + source + " "+ rawModeString);
}
