{
  if (FileUtils.isImage(file.getName())) {
    try {
      ImageIcon image=new ImageIcon(file.toURI().toURL());
      this.setToolTipImage(image);
      image=ImageUtils.getScaledRoundedIcon(image.getImage(),64,64);
      this.setIcon(image);
    }
 catch (    MalformedURLException e) {
      logger.debug("Could not locate image.",e);
      this.setIcon(new ImageIcon(ImageLoader.getImage(ImageLoader.DEFAULT_FILE_ICON)));
    }
  }
 else {
    Icon icon=FileUtils.getIcon(file);
    if (icon == null)     icon=new ImageIcon(ImageLoader.getImage(ImageLoader.DEFAULT_FILE_ICON));
    this.setIcon(icon);
  }
}
