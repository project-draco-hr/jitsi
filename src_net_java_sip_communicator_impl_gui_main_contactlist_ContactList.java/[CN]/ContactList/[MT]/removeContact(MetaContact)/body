{
synchronized (contentToRefresh) {
    if (contact != null && (contentToRefresh.get(contact) == null || !contentToRefresh.get(contact).equals(REMOVE_OPERATION))) {
      contentToRefresh.put(contact,REMOVE_OPERATION);
      contentToRefresh.notifyAll();
    }
  }
}
