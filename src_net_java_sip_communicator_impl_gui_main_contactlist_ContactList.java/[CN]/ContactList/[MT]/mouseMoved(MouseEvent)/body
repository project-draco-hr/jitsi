{
  int selectedIndex=this.locationToIndex(e.getPoint());
  Object cell=listModel.getElementAt(selectedIndex);
  if (cell instanceof MetaContactGroup)   this.setCursor(Cursor.getPredefinedCursor(Cursor.HAND_CURSOR));
 else   this.setCursor(Cursor.getPredefinedCursor(Cursor.DEFAULT_CURSOR));
}
