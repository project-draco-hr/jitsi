{
  int selectedIndex=this.locationToIndex(e.getPoint());
  Object cell=listModel.getElementAt(selectedIndex);
  setCursor(Cursor.getPredefinedCursor((cell instanceof MetaContactGroup) ? Cursor.HAND_CURSOR : Cursor.DEFAULT_CURSOR));
}
