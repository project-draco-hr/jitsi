{
  for (int i=0; i < c.getComponentCount(); i++) {
    Component component=c.getComponent(i);
    int componentX=component.getX();
    if ((x > componentX) && (x < componentX + component.getWidth()))     return component;
  }
  return null;
}
