{
synchronized (activeContacts) {
    if (activeContacts.contains(metaContact))     this.activeContacts.remove(metaContact);
    if (activeContacts.size() == 0)     GuiActivator.getSystrayService().setSystrayIcon(SystrayService.SC_IMG_TYPE);
  }
  this.refreshContact(metaContact);
}
