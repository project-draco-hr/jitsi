{
  Response response=this.wrappedFactory.createResponse(statusCode,request);
  SipApplicationData.setApplicationData(response,SipApplicationData.KEY_SERVICE,this.protocolProvider);
  return response;
}
