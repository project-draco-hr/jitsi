{
  ConfigurationService configService=GeneralConfigPluginActivator.getConfigurationService();
  configService.setProperty(Preferences.ENABLE,Boolean.toString(enabled));
  configService.setProperty(Preferences.TIMER,timer);
}
