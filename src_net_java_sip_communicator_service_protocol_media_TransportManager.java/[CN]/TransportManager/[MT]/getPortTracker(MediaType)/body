{
  if (MediaType.AUDIO == mediaType && audioPortTracker != null)   return audioPortTracker;
  if (MediaType.VIDEO == mediaType && videoPortTracker != null)   return videoPortTracker;
  return defaultPortTracker;
}
