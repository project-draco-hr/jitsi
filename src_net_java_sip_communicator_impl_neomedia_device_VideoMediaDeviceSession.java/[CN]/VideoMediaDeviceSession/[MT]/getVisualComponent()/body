{
  Component visualComponent=null;
  if (getStartedDirection().allowsReceiving()) {
    Player player=getPlayer();
    if (player != null)     visualComponent=getVisualComponent(player);
  }
  return visualComponent;
}
