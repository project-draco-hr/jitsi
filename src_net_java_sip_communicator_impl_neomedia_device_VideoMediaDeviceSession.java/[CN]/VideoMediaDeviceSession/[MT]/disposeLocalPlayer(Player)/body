{
  Component visualComponent=getVisualComponent(player);
  if (localPlayer == player) {
    localPlayer=null;
  }
  player.stop();
  player.deallocate();
  player.close();
  if (visualComponent != null)   fireVideoEvent(VideoEvent.VIDEO_REMOVED,visualComponent,VideoEvent.LOCAL);
}
