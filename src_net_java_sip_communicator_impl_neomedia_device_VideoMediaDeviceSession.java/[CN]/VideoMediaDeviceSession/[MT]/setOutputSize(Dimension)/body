{
  if ((size != null && outputSize == null) || (size == null && outputSize != null) || (size != null && outputSize != null && !outputSize.equals(size)))   outputsizeChanged=true;
  outputSize=size;
}
