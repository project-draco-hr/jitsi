{
  if ((component != null) && DESKTOP_STREAMING_ICON.equals(component.getName())) {
    fireVideoEvent(VideoEvent.VIDEO_REMOVED,component,VideoEvent.LOCAL,false);
    return;
  }
  Player localPlayer=this.localPlayer;
  if (localPlayer != null)   disposeLocalPlayer(localPlayer);
}
