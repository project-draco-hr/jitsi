{
  this.callSession=callSession;
  this.participantSecurityCallback=callSession.getSessionCreatorCallback();
  Iterator<CallPeer> callParticipants=callSession.getCall().getCallParticipants();
  while (callParticipants.hasNext()) {
    this.callParticipant=callParticipants.next();
  }
}
