{
  this.callSession=callSession;
  this.peerSecurityCallback=callSession.getSessionCreatorCallback();
  Iterator<? extends CallPeer> callPeers=callSession.getCall().getCallPeers();
  while (callPeers.hasNext()) {
    this.callPeer=callPeers.next();
  }
}
