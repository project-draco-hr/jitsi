{
  if (logger.isInfoEnabled())   logger.info(sessionTypeToString(sessionType) + ": Security off");
  if (callParticipant.getCall() != null && !callParticipant.getCall().getCallState().equals(CallState.CALL_ENDED)) {
    ((AbstractCallParticipant)callParticipant).setSecurityOff(sessionType);
  }
}
