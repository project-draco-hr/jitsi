{
  if (logger.isInfoEnabled())   logger.info(sessionTypeToString(sessionType) + ": Security off");
  if (callPeer.getCall() != null && !callPeer.getCall().getCallState().equals(CallState.CALL_ENDED)) {
    peerSecurityCallback.securityOff(sessionType);
  }
}
