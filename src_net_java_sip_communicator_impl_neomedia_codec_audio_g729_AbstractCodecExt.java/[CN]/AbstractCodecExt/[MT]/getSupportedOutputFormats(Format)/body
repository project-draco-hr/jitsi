{
  if (inputFormat == null)   return supportedOutputFormats;
  if (!formatClass.isInstance(inputFormat) || (null == matches(inputFormat,inputFormats)))   return new Format[0];
  return getMatchingOutputFormats(inputFormat);
}
