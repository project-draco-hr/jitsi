{
  int width=0;
  if (imageLabel.getIcon() != null)   width+=imageLabel.getIcon().getIconWidth();
  width+=textWidth;
  int imageHeight=0;
  if (imageLabel.getIcon() != null)   imageHeight=imageLabel.getIcon().getIconHeight();
  int height=imageHeight > textHeight ? imageHeight : textHeight;
  return new Dimension(width + 15,height);
}
