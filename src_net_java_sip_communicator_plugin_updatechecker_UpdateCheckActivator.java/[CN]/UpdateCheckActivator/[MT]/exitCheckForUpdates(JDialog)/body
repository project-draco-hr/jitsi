{
  if (inCheckForUpdates == 0)   throw new IllegalStateException("inCheckForUpdates");
 else {
    inCheckForUpdates--;
    if ((checkForUpdatesDialog != null) && (this.checkForUpdatesDialog == checkForUpdatesDialog))     this.checkForUpdatesDialog=null;
  }
}
