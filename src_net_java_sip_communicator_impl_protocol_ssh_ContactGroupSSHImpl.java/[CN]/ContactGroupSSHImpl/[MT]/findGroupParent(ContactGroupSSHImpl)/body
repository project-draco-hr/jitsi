{
  if (subGroups.contains(sshGroup))   return this;
  Iterator<ContactGroup> subGroupsIter=subgroups();
  while (subGroupsIter.hasNext()) {
    ContactGroupSSHImpl subgroup=(ContactGroupSSHImpl)subGroupsIter.next();
    ContactGroupSSHImpl parent=subgroup.findGroupParent(sshGroup);
    if (parent != null)     return parent;
  }
  return null;
}
