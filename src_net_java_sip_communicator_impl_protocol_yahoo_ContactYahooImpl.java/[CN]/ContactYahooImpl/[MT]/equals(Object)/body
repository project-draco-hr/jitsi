{
  if (obj == null || !(obj instanceof ContactYahooImpl) || !(((ContactYahooImpl)obj).getAddress().equals(getAddress()) && ((ContactYahooImpl)obj).getProtocolProvider() == getProtocolProvider()))   return false;
 else   return true;
}
