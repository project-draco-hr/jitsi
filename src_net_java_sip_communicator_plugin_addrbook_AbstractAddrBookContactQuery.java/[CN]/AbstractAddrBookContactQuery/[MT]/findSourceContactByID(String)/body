{
synchronized (sourceContacts) {
    for (    SourceContact sc : sourceContacts) {
      Object scID=sc.getData(SOURCE_CONTACT_ID_DATA_KEY);
      if (id.equals(scID))       return sc;
    }
  }
  return null;
}
