{
  ServiceRegistration registration;
synchronized (registeredAccounts) {
    registration=(ServiceRegistration)registeredAccounts.get(accountID);
  }
  return (registration == null) ? null : registration.getReference();
}
