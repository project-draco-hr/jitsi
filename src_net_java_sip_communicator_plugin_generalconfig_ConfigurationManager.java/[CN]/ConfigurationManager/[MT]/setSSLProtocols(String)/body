{
  if (StringUtils.isNullOrEmpty(enabledProtocols,true))   configService.removeProperty("gov.nist.javax.sip.TLS_CLIENT_PROTOCOLS");
 else   configService.setProperty("gov.nist.javax.sip.TLS_CLIENT_PROTOCOLS",enabledProtocols);
}
