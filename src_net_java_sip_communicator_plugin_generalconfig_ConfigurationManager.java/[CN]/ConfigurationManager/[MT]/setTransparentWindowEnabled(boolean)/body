{
  ConfigurationManager.isTransparentWindowEnabled=isTransparentWindowEnabled;
  configService.setProperty("net.java.sip.communicator.impl.gui.isTransparentWindowEnabled",new Boolean(isTransparentWindowEnabled).toString());
}
