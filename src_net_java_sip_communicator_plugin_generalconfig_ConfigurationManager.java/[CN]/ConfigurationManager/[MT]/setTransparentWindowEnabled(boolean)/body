{
  ConfigurationManager.isTransparentWindowEnabled=isTransparentWindowEnabled;
  configService.setProperty("impl.gui.IS_TRANSPARENT_WINDOW_ENABLED",new Boolean(isTransparentWindowEnabled).toString());
}
