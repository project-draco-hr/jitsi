{
  for (  Entry<MediaFormat,Byte> entry : ProtocolMediaActivator.getMediaService().getDynamicPayloadTypePreferences().entrySet()) {
    if (entry.getValue() == payloadTypePreference)     return entry.getKey();
  }
  return null;
}
