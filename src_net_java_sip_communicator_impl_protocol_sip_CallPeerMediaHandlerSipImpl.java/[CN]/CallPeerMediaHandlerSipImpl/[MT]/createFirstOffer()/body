{
  Vector<MediaDescription> mediaDescs=createMediaDescriptions();
  String userName=peer.getProtocolProvider().getAccountID().getUserID();
  SessionDescription sDes=SdpUtils.createSessionDescription(getLastUsedLocalHost(),userName,mediaDescs);
  this.localSess=sDes;
  return localSess;
}
