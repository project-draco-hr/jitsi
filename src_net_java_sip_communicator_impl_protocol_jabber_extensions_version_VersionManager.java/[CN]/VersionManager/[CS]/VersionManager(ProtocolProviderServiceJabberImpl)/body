{
  this.parentProvider=parentProvider;
  this.parentProvider.addRegistrationStateChangeListener(this);
  ProviderManager.getInstance().addIQProvider("query","jabber:iq:version",new VersionIQProvider());
}
