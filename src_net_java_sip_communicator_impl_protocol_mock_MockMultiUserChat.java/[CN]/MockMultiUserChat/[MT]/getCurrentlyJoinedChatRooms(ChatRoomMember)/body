{
  List result=new Vector();
  Iterator iter=joinedChatRooms.iterator();
  while (iter.hasNext()) {
    ChatRoom elem=(ChatRoom)iter.next();
    if (elem.getMembers().contains(chatRoomMember))     result.add(elem);
  }
  return result;
}
