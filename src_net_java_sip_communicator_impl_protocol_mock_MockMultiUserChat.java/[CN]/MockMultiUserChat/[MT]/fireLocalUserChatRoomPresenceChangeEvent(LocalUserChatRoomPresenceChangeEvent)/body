{
  Iterator iter=localUserChatRoomPresenceListeners.iterator();
  while (iter.hasNext()) {
    LocalUserChatRoomPresenceListener elem=(LocalUserChatRoomPresenceListener)iter.next();
    elem.localUserPresenceChanged(evt);
  }
}
