{
  if (idleListener == null)   return;
  getSystemActivityNotificationsService().removeIdleSystemChangeListener(idleListener);
  getSystemActivityNotificationsService().removeSystemActivityChangeListener(idleListener);
  idleListener=null;
}
