{
  try {
synchronized (this) {
      if (streams != null) {
        for (        Object stream : streams) {
          try {
            ((PortAudioStream)stream).setDeviceIndex(PortAudio.paNoDevice);
          }
 catch (          IOException ioex) {
          }
        }
      }
    }
  }
  finally {
    super.doDisconnect();
  }
}
