{
  super.doConnect();
  int deviceIndex=getDeviceIndex();
synchronized (getStreamSyncRoot()) {
    for (    Object stream : getStreams())     ((PortAudioStream)stream).setDeviceIndex(deviceIndex);
  }
}
