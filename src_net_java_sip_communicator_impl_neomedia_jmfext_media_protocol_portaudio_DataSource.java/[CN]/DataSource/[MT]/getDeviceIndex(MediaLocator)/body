{
  if (PortAudioSystem.LOCATOR_PROTOCOL.equalsIgnoreCase(locator.getProtocol())) {
    return Integer.parseInt(locator.getRemainder().replace("#",""));
  }
 else {
    throw new IllegalArgumentException("locator.protocol");
  }
}
