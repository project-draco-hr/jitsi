{
  if (!connected)   return;
  try {
    stop();
  }
 catch (  IOException ioex) {
    logger.warn("Failed to stop DataSource with locator " + getLocator(),ioex);
  }
  connected=false;
  if (logger.isTraceEnabled())   logger.trace("Disconnected " + MediaStreamImpl.toString(this));
}
