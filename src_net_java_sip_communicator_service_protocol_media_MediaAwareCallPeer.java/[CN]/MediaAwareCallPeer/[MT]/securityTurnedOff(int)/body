{
  if ((call != null) && !call.getCallState().equals(CallState.CALL_ENDED)) {
    CallPeerSecurityOffEvent event=new CallPeerSecurityOffEvent(this,sessionType);
    fireCallPeerSecurityOffEvent(event);
  }
}
