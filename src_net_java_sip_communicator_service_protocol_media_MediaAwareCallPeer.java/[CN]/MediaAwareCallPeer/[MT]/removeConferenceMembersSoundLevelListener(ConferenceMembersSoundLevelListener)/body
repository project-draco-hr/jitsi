{
synchronized (conferenceMemberAudioLevelListeners) {
    if (conferenceMemberAudioLevelListeners.remove(listener) && (conferenceMemberAudioLevelListeners.size() == 0)) {
      getMediaHandler().setCsrcAudioLevelListener(null);
    }
  }
}
