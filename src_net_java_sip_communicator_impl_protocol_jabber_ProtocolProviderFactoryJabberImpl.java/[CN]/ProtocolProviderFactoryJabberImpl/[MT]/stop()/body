{
  Enumeration registrations=this.registeredAccounts.elements();
  while (registrations.hasMoreElements()) {
    ServiceRegistration reg=((ServiceRegistration)registrations.nextElement());
    ProtocolProviderServiceJabberImpl provider=(ProtocolProviderServiceJabberImpl)JabberActivator.getBundleContext().getService(reg.getReference());
    provider.shutdown();
    reg.unregister();
  }
  registeredAccounts.clear();
}
