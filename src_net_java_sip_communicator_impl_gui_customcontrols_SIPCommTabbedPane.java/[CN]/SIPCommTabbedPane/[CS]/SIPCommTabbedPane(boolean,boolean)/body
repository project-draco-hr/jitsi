{
  super.setTabLayoutPolicy(JTabbedPane.SCROLL_TAB_LAYOUT);
  this.setUI(new SIPCommTabbedPaneEnhancedUI());
  if (!closingTabs)   this.setCloseIcon(false);
  if (!maximizingTabs)   this.setMaxIcon(false);
}
