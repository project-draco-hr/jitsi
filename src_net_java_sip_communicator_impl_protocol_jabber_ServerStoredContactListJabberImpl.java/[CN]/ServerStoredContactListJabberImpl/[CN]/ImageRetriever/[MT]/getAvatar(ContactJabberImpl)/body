{
  try {
    XMPPConnection connection=jabberProvider.getConnection();
    if (connection == null || !connection.isAuthenticated())     return null;
    VCard card=new VCard();
    card.load(connection,contact.getAddress());
    return card.getAvatar();
  }
 catch (  Exception exc) {
    if (logger.isDebugEnabled())     logger.debug("Cannot load image for contact " + this + " : "+ exc.getMessage(),exc);
    return new byte[0];
  }
}
