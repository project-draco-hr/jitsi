{
  Component c=(Component)event.getSource();
  if (event.getContainerID().equals(UIService.CONTAINER_GROUP_RIGHT_BUTTON_MENU)) {
    this.add(c);
    if (c instanceof ContactAwareComponent) {
      ((ContactAwareComponent)c).setCurrentContactGroup(group);
    }
    this.repaint();
  }
}
