{
  if (!connected) {
    try {
      doConnect();
    }
 catch (    IOException ioex) {
      logger.error("Failed to connect " + getClass().getSimpleName() + " "+ getLocator(),ioex);
      throw ioex;
    }
    connected=true;
  }
}
