{
  try {
    stop();
  }
 catch (  IOException ioex) {
    logger.error("Failed to stop " + getClass().getSimpleName() + " "+ getLocator(),ioex);
  }
  if (connected) {
    doDisconnect();
    connected=false;
  }
}
