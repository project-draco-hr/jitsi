{
  try {
    multiUserChat.changeNickname(nickname);
  }
 catch (  XMPPException e) {
    throw new OperationFailedException("The " + nickname + "already exists in this chat room.",OperationFailedException.NICKNAME_ALREADY_EXISTS);
  }
}
