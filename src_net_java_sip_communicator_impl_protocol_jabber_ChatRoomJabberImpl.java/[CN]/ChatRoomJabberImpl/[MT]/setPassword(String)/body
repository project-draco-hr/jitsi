{
  multiUserChatConfigForm.setAnswer("password",password);
  try {
    multiUserChat.sendConfigurationForm(multiUserChatConfigForm);
  }
 catch (  XMPPException e) {
    logger.error("Failed to send configuration form for this chat room.",e);
  }
}
