{
  try {
    multiUserChat.changeNickname(nickname);
  }
 catch (  XMPPException ex) {
    logger.error("Failed to changed subject for chat room" + getName(),ex);
    throw new OperationFailedException("Failed to changed nickname in chat room" + getName(),OperationFailedException.FORBIDDEN,ex);
  }
}
