{
  PacketExtension pe;
  while (null != (pe=packet.getExtension(ConferenceDescriptionPacketExtension.NAMESPACE))) {
    packet.removeExtension(pe);
  }
  if (ext != null)   packet.addExtension(ext);
}
