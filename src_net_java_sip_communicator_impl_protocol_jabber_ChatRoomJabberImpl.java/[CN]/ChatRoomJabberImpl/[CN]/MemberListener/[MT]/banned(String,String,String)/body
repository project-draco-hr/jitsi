{
  logger.info(participant + " has been banned from " + getName()+ " chat room.");
  ChatRoomMember member=smackParticipantToScMember(participant);
  if (member == null)   return;
  banList.put(participant,member);
  fireMemberRoleEvent(member,member.getRole(),ChatRoomMemberRole.OUTCAST);
}
