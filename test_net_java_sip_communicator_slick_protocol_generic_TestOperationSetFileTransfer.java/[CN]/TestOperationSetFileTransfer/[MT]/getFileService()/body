{
  if (fileAccessService == null) {
    ServiceReference ref=getContext().getServiceReference(FileAccessService.class.getName());
    fileAccessService=(FileAccessService)getContext().getService(ref);
  }
  return fileAccessService;
}
