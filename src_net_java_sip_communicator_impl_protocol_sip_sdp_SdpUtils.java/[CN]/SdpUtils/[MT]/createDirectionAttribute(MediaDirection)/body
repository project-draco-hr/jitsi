{
  String dirStr;
switch (direction) {
case RECVONLY:
    dirStr="recvonly";
  break;
case SENDONLY:
dirStr="sendonly";
break;
case SENDRECV:
dirStr="sendrecv";
break;
default :
dirStr="inactive";
break;
}
return sdpFactory.createAttribute(dirStr,null);
}
