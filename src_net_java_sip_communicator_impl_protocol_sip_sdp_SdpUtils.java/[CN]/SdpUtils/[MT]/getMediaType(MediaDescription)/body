{
  try {
    return MediaType.parseString(description.getMedia().getMediaType());
  }
 catch (  SdpException exc) {
    if (logger.isDebugEnabled())     logger.debug("Invalid media type in m= line: " + description,exc);
    throw new IllegalArgumentException("Invalid media type in m= line: " + description,exc);
  }
}
