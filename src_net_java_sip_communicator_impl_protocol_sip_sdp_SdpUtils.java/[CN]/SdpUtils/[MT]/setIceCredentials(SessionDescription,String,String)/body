{
  try {
    sDes.setAttribute(ICE_UFRAG,uFrag);
    sDes.setAttribute(ICE_PWD,pwd);
  }
 catch (  Exception cause) {
    ProtocolProviderServiceSipImpl.throwOperationFailedException("Failed to set ICE credentials",OperationFailedException.INTERNAL_ERROR,cause,logger);
  }
}
