{
  if (this.parentProvider.getIrcStack().isConnected()) {
    return this.parentProvider.getIrcStack().isAway() ? IrcStatusEnum.AWAY : IrcStatusEnum.ONLINE;
  }
 else {
    return IrcStatusEnum.OFFLINE;
  }
}
