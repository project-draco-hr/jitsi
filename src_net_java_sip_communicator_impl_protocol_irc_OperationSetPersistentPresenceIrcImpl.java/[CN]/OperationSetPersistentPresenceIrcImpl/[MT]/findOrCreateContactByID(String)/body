{
  Contact contact=findContactByID(name);
  if (contact == null) {
    contact=createVolatileContact(name);
    LOGGER.debug("No existing contact found. Created volatile contact" + " for nick name '" + name + "'.");
  }
  return contact;
}
