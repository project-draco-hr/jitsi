{
synchronized (childContacts) {
    this.childContacts.add(metaContact);
    childContactsOrderedCopy=new LinkedList<MetaContactImpl>(childContacts);
    return childContactsOrderedCopy.indexOf(metaContact);
  }
}
