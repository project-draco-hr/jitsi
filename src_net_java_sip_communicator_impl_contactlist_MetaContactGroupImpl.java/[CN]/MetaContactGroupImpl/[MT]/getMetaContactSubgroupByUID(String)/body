{
  Iterator<MetaContactGroupImpl> groupsIter=getSubgroups();
  while (groupsIter.hasNext()) {
    MetaContactGroupImpl mcGroup=groupsIter.next();
    if (mcGroup.getMetaUID().equals(groupUID))     return mcGroup;
  }
  return null;
}
