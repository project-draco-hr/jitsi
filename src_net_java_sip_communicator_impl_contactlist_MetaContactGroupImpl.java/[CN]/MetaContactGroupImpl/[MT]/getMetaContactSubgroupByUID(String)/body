{
  Iterator groupsIter=getSubgroups();
  while (groupsIter.hasNext()) {
    MetaContactGroupImpl mcGroup=(MetaContactGroupImpl)groupsIter.next();
    if (mcGroup.getGroupName().equals(groupUID))     return mcGroup;
  }
  return null;
}
