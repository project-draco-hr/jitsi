{
  for (  Object value : values) {
    if (value instanceof String) {
      if (((String)value).toLowerCase().contains(query))       return true;
    }
 else     if (value instanceof Object[]) {
      for (      Object subValue : (Object[])value) {
        if ((subValue instanceof String) && ((String)subValue).toLowerCase().contains(query))         return true;
      }
    }
  }
  return false;
}
