{
  if (obj == this)   return true;
  if (!(obj instanceof ChatRoomMemberRole))   return false;
  ChatRoomMemberRole role=(ChatRoomMemberRole)obj;
  return role.getRoleName().equals(getRoleName()) && (role.getRoleIndex() == getRoleIndex());
}
