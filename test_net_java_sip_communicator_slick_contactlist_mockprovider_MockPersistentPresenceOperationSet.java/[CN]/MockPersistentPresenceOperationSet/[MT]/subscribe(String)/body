{
  MockContact contact=new MockContact(contactIdentifier,parentProvider,contactListRoot);
  contactListRoot.addContact(contact);
  fireSubscriptionEvent(contact,contactListRoot,SubscriptionEvent.SUBSCRIPTION_CREATED);
}
