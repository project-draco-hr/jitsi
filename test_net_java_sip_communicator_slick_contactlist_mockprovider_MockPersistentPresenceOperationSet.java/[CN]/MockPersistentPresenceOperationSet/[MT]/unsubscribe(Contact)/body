{
  ((MockContact)contact).getParentGroup().removeContact((MockContact)contact);
  fireSubscriptionEvent((MockContact)contact,((MockContact)contact).getParentGroup(),SubscriptionEvent.SUBSCRIPTION_REMOVED);
}
