{
  if (captureDevice == null) {
    try {
      setCaptureDevice((CaptureDevice)Manager.createDataSource(captureDeviceInfo.getLocator()));
    }
 catch (    IOException ioe) {
    }
catch (    NoDataSourceException ndse) {
    }
  }
  return captureDevice;
}
