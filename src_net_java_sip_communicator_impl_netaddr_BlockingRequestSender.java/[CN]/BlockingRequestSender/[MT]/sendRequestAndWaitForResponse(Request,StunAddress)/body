{
  stunProvider.sendRequest(request,serverAddress,apDescriptor,BlockingRequestSender.this);
  try {
    wait();
  }
 catch (  InterruptedException ex) {
    ex.printStackTrace();
  }
  StunMessageEvent res=responseEvent;
  responseEvent=null;
  return res;
}
