{
  DiscoverInfo discoverInfo=((AbstractCallPeerJabberGTalkImpl)callPeer).getDiscoverInfo();
  return (this.parentProvider.getDiscoveryManager().includesFeature(InputEvtIQ.NAMESPACE_SERVER) && discoverInfo != null && discoverInfo.containsFeature(InputEvtIQ.NAMESPACE_CLIENT));
}
