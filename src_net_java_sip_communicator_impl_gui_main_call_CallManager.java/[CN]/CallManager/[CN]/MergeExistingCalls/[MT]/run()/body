{
  putOffHold(first);
  if (!calls.isEmpty()) {
    CallConference conference=first.getConference();
    for (    Call call : calls) {
      if (call == first)       continue;
      putOffHold(call);
      closeCallContainerIfNotNecessary(call,false);
      call.setConference(conference);
    }
  }
}
