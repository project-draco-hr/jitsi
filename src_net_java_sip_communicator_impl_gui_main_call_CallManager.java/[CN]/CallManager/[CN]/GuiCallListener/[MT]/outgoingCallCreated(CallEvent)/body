{
  Call sourceCall=event.getSourceCall();
  CallManager.openCallContainer(sourceCall);
  List<CallPanel> calls=new ArrayList<CallPanel>(activeCalls.values());
  for (  CallPanel cp : calls) {
    if (cp != null)     cp.incomingCallReceived(event);
  }
}
