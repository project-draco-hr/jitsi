{
  Collection<Call> calls=getActiveCalls();
  List<Call> inProgressCalls=new ArrayList<Call>(calls.size());
  for (  Call call : calls) {
    if (call.getCallState() == CallState.CALL_IN_PROGRESS)     inProgressCalls.add(call);
  }
  return inProgressCalls;
}
