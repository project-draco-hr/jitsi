{
  OperationSetBasicTelephony<?> telephony=protocolProvider.getOperationSet(OperationSetBasicTelephony.class);
  OperationSetResourceAwareTelephony resourceTelephony=protocolProvider.getOperationSet(OperationSetResourceAwareTelephony.class);
  if (resourceTelephony != null && contactResource != null) {
    if (contact != null)     resourceTelephony.createCall(contact,contactResource);
 else     if (!StringUtils.isNullOrEmpty(stringContact))     resourceTelephony.createCall(stringContact,contactResource.getResourceName());
  }
 else   if (telephony != null) {
    if (contact != null) {
      telephony.createCall(contact);
    }
 else     if (!StringUtils.isNullOrEmpty(stringContact))     telephony.createCall(stringContact);
  }
}
