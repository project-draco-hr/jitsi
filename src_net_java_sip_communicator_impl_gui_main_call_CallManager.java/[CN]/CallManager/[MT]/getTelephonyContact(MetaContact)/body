{
  Iterator i=metaContact.getContacts();
  while (i.hasNext()) {
    Contact contact=(Contact)i.next();
    OperationSetBasicTelephony telephony=mainFrame.getTelephonyOpSet(contact.getProtocolProvider());
    if (telephony != null)     return contact;
  }
  return null;
}
