{
  setDevice(device);
  NeomediaActivator.getMediaServiceImpl().getDeviceConfiguration().addPropertyChangeListener(this);
  this.srtpControl=(srtpControl == null) ? new ZrtpControlImpl() : srtpControl;
  if (connector != null)   setConnector(connector);
}
