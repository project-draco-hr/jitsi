{
  if (!zrtpControl.getSecureCommunicationStatus())   return;
  zrtpControl.cleanup();
  zrtpControl.getZrtpEngine().setStartMuted(true);
  this.zrtpControl.setConnector(rtpConnector);
  rtpConnector.setEngine(createTransformEngineChain());
  zrtpRestarted=true;
}
