{
  if (!zrtpControl.getSecureCommunicationStatus())   return;
  zrtpControl.cleanup();
  AbstractRTPConnector rtpConnector=getRTPConnector();
  zrtpControl.setConnector(rtpConnector);
  if (rtpConnector instanceof RTPTransformUDPConnector)   ((RTPTransformUDPConnector)rtpConnector).setEngine(createTransformEngineChain());
 else   if (rtpConnector instanceof RTPTransformUDPConnector)   ((RTPTransformUDPConnector)rtpConnector).setEngine(createTransformEngineChain());
  zrtpRestarted=true;
}
