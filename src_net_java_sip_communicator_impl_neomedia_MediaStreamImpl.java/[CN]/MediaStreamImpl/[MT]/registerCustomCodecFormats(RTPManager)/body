{
synchronized (dynamicRTPPayloadTypes) {
    for (    Map.Entry<Integer,MediaFormat> dynamicRTPPayloadType : dynamicRTPPayloadTypes.entrySet()) {
      MediaFormatImpl<? extends Format> mediaFormatImpl=(MediaFormatImpl<? extends Format>)dynamicRTPPayloadType.getValue();
      rtpManager.addFormat(mediaFormatImpl.getFormat(),dynamicRTPPayloadType.getKey());
    }
  }
}
