{
synchronized (activeRTPExtensions) {
    Set<Map.Entry<Byte,RTPExtension>> extSet=this.activeRTPExtensions.entrySet();
    for (    Map.Entry<Byte,RTPExtension> entry : extSet) {
      if (entry.getValue().equals(urn))       return entry.getKey();
    }
  }
  return -1;
}
