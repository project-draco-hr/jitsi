{
  for (int i=0; i < getComponentCount(); i++) {
    Component c=getComponent(i);
    if (c instanceof JLabel) {
      if (i <= (soundLevel))       ((JLabel)c).setIcon(soundLevelActiveImage);
 else       ((JLabel)c).setIcon(soundLevelInactiveImage);
    }
  }
  this.repaint();
}
