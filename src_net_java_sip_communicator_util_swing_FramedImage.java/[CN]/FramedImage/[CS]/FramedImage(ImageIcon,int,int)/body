{
  this.width=width;
  this.height=height;
  this.setPreferredSize(new Dimension(width,height));
  loadSkin();
  if (imageIcon != null)   this.icon=ImageUtils.scaleIconWithinBounds(imageIcon.getImage(),width,height);
}
