{
  for (int i=0, n=flavor.length; i < n; i++) {
    if (flavor[i].equals(uiContactDataFlavor) || flavor[i].equals(metaContactDataFlavor)) {
      if (comp instanceof DefaultContactList) {
        return true;
      }
      return false;
    }
  }
  return false;
}
