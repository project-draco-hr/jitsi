{
  if (component instanceof DefaultContactList) {
    MetaContact c=(MetaContact)((DefaultContactList)component).getSelectedValue();
    return new MetaContactTransferable(c);
  }
  return super.createTransferable(component);
}
