{
  Enumeration participants=wbParticipants.elements();
  while (participants.hasMoreElements()) {
    WhiteboardParticipant participant=(WhiteboardParticipant)participants.nextElement();
    if (participant.getContactAddress().equals(contactAddress))     return participant;
  }
  return null;
}
