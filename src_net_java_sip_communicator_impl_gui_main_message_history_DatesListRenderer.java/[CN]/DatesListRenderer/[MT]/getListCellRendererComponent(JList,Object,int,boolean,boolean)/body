{
  Date dateValue=(Date)value;
  calendar.setTime(dateValue);
  String text=this.processTime(calendar.get(Calendar.DAY_OF_MONTH)) + "/" + this.processTime(calendar.get(Calendar.MONTH) + 1)+ "/"+ this.processTime(calendar.get(Calendar.YEAR));
  this.label.setText(text);
  this.isSelected=isSelected;
  return this;
}
