{
  this.configContainer.add(configForm);
  ConfigMenuItemPanel configItem=new ConfigMenuItemPanel(configForm.getTitle(),configForm.getIcon());
  configItem.addMouseListener(this);
  this.configList.addCell(configItem);
}
