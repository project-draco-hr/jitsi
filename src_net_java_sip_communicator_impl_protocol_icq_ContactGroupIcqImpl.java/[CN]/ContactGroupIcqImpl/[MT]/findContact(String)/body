{
  Iterator contacts=contacts();
  while (contacts.hasNext()) {
    ContactIcqImpl item=(ContactIcqImpl)contacts.next();
    if (item.getJoustSimBuddy().getScreenname().getFormatted().equalsIgnoreCase(screenName))     return item;
  }
  return null;
}
