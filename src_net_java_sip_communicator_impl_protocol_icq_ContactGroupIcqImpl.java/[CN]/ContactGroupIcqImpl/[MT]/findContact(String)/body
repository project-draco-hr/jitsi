{
  Iterator contacts=contacts();
  while (contacts.hasNext()) {
    ContactIcqImpl item=(ContactIcqImpl)contacts.next();
    if (item.getJoustSimBuddy().getScreenname().getFormatted().equals(screenName))     return item;
  }
  return null;
}
