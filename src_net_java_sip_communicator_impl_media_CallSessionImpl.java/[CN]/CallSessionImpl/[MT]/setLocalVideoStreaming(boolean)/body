{
  if (localVideoStreaming != streaming) {
    boolean oldValue=localVideoStreaming;
    localVideoStreaming=streaming;
    firePropertyChange(LOCAL_VIDEO_STREAMING,oldValue,localVideoStreaming);
  }
}
