{
  player.stop();
  Component visualComponent=getVisualComponent(player);
  player.deallocate();
  player.close();
  players.remove(player);
  if (visualComponent != null)   fireVideoEvent(VideoEvent.VIDEO_REMOVED,visualComponent,VideoEvent.REMOTE);
}
