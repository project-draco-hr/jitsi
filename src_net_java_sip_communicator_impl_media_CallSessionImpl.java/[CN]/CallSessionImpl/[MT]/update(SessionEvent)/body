{
  if (logger.isDebugEnabled()) {
    if (event instanceof NewParticipantEvent) {
      Participant participant=((NewParticipantEvent)event).getParticipant();
      logger.debug("A new participant had just joined: " + participant.getCNAME());
    }
 else {
      logger.debug("Received the following JMF Session event - " + event.getClass().getName() + "="+ event);
    }
  }
}
