{
  if (activator) {
    this.secureStatusChanged(new SecureEvent(this,SecureEvent.SECURE_COMMUNICATION,source));
  }
 else {
    this.secureStatusChanged(new SecureEvent(this,SecureEvent.UNSECURE_COMMUNICATION,source));
  }
}
