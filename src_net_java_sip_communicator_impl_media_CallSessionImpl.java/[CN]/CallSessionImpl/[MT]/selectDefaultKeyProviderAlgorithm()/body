{
  KeyProviderAlgorithm defaultProvider=(KeyProviderAlgorithm)keySharingAlgorithms.get(0);
  if (defaultProvider == null) {
    return new DummyKeyProvider(0);
  }
 else {
    return defaultProvider;
  }
}
