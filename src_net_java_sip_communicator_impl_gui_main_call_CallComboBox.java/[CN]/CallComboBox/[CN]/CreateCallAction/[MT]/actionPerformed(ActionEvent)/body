{
  String item=getEditor().getItem().toString();
  if (item.length() > 0)   CallManager.createCall(parentCallPanel.getCallProvider(),item);
 else {
    if (!isPopupVisible())     setPopupVisible(true);
  }
}
