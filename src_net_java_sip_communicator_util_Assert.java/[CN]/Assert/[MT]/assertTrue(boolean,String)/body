{
  if (EnableAssertions && !condition) {
    StackTraceElement caller=new Throwable().getStackTrace()[1];
    String fullText="Assertion failed at " + caller.getMethodName() + ", line "+ caller.getLineNumber()+ "\n\tMessage: "+ message;
    log.error(fullText);
    throw new AssertionError(fullText);
  }
}
