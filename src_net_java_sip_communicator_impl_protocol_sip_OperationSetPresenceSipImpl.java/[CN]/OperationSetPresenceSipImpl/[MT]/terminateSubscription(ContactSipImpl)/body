{
  if (contact == null) {
    logger.debug("null contact provided, can't terminate" + " subscription");
    return;
  }
  contact.setClientDialog(null);
  changePresenceStatusForContact(contact,SipStatusEnum.UNKNOWN);
  contact.setResolved(false);
}
