{
  try {
    return parentProvider.parseAddressString(contact.getAddress());
  }
 catch (  ParseException ex) {
    String message="An unexpected error occurred while constructing the address";
    logger.error(message,ex);
    throw new OperationFailedException(message,OperationFailedException.INTERNAL_ERROR,ex);
  }
}
