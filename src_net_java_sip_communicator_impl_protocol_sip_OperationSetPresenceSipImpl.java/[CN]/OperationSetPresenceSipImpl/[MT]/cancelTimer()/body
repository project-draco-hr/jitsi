{
  if (republishTask != null)   republishTask=null;
  if (pollingTask != null)   pollingTask=null;
synchronized (timerLock) {
    if (timer != null) {
      timer.cancel();
      timer=null;
    }
  }
}
