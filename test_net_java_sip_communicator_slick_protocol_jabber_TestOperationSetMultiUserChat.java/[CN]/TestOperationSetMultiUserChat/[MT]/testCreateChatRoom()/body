{
  ChatRoom chatRoom1=opSetMultiChat1.createChatRoom("mychatroom@conference.voipgw.u-strasbg.fr",new Hashtable());
  chatRoom1.join();
  try {
    Thread.currentThread().wait(100000);
  }
 catch (  InterruptedException ex) {
  }
}
