{
  if (obj == null || !(obj instanceof ContactZeroconfImpl))   return false;
  ContactZeroconfImpl zeroconfContact=(ContactZeroconfImpl)obj;
  return this.getAddress().equals(zeroconfContact.getAddress());
}
