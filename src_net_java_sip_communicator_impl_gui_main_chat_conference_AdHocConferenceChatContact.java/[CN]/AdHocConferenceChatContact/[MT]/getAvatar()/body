{
  byte[] avatarBytes=participant.getImage();
  if (avatarBytes != null && avatarBytes.length > 0) {
    return ImageUtils.getScaledRoundedIcon(avatarBytes,AVATAR_ICON_WIDTH,AVATAR_ICON_HEIGHT);
  }
 else   return null;
}
