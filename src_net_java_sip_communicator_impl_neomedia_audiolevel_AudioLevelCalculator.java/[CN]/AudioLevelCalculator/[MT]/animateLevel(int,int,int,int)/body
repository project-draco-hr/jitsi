{
  int diff=lastLevel - level;
  if (diff >= 0) {
    int maxDiff=(int)(maxLevel * DEC_LEVEL);
    if (diff > maxDiff)     level=lastLevel - maxDiff;
  }
 else {
    int maxDiff=(int)(maxLevel * INC_LEVEL);
    if (diff > maxDiff)     level=lastLevel + maxDiff;
  }
  return level;
}
