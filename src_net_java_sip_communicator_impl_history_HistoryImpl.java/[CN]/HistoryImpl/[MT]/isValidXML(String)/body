{
  try {
    this.historyServiceImpl.getDocumentBuilder().parse(new ByteArrayInputStream(str.getBytes("UTF-8")));
  }
 catch (  Exception ex) {
    System.out.println("not valid xml " + ex.getMessage());
    return false;
  }
  return true;
}
