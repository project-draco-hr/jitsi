{
  try {
    bgImage=ImageIO.read(WindowBackground.class.getResource(BrandingResources.getString("aboutWindowBg")));
  }
 catch (  IOException e) {
    bgImage=null;
  }
  this.setPreferredSize(new Dimension(bgImage.getWidth(this),bgImage.getHeight(this)));
}
