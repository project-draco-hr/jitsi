{
  try {
    bgImage=ImageIO.read(WindowBackground.class.getResource("resources/aboutWindowBackground.png"));
  }
 catch (  IOException e) {
    bgImage=null;
  }
  this.setPreferredSize(new Dimension(bgImage.getWidth(this),bgImage.getHeight(this)));
}
