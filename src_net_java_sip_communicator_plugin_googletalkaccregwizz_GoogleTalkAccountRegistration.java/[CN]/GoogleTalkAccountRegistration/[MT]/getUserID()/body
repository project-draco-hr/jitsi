{
  String serverAddress=getServerAddress();
  return ((userID != null) && (userID.indexOf('@') < 0) && ((serverAddress == null) || serverAddress.equals(GOOGLE_CONNECT_SRV))) ? (userID + '@' + GOOGLE_USER_SUFFIX) : userID;
}
