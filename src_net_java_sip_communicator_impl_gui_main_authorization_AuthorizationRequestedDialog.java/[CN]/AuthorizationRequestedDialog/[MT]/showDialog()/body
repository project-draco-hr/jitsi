{
  this.setVisible(true);
synchronized (lock) {
    try {
      lock.wait();
    }
 catch (    InterruptedException e) {
      logger.error("Authorization request interrupted.",e);
    }
  }
  return result;
}
