{
  if (suggestedLatency != PortAudio.LATENCY_UNSPECIFIED)   return suggestedLatency;
  if (OSUtils.IS_MAC || OSUtils.IS_LINUX)   return PortAudio.LATENCY_HIGH;
 else   if (OSUtils.IS_WINDOWS)   return 0.1d;
 else   return PortAudio.LATENCY_UNSPECIFIED;
}
