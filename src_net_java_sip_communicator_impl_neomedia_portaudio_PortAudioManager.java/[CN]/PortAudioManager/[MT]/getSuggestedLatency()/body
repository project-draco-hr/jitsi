{
  if (suggestedLatency != PortAudio.LATENCY_UNSEPCIFIED)   return suggestedLatency;
  if (OSUtils.isMac())   return PortAudio.LATENCY_HIGH;
 else   if (OSUtils.isLinux())   return PortAudio.LATENCY_HIGH;
 else   if (OSUtils.isWindows())   return 0.1d;
  return PortAudio.LATENCY_UNSEPCIFIED;
}
