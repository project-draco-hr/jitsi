{
  if (resourcesService == null) {
    ServiceReference serviceReference=RssActivator.bundleContext.getServiceReference(ResourceManagementService.class.getName());
    if (serviceReference == null)     return null;
    resourcesService=(ResourceManagementService)RssActivator.bundleContext.getService(serviceReference);
  }
  return resourcesService;
}
