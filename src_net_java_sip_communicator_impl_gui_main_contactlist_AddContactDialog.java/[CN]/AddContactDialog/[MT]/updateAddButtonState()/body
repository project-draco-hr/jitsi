{
  String contactAddress=contactAddressField.getText();
  if (accountCombo.getSelectedItem() instanceof ProtocolProviderService && contactAddress != null && contactAddress.length() > 0)   addButton.setEnabled(true);
 else   addButton.setEnabled(false);
}
