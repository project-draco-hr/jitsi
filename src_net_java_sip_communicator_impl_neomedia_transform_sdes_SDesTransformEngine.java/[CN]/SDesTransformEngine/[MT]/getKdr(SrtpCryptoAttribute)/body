{
  if (attribute.getSessionParams() != null) {
    for (    SrtpSessionParam param : attribute.getSessionParams()) {
      if (param instanceof KdrSessionParam)       return ((KdrSessionParam)param).getKeyDerivationRateExpanded();
    }
  }
  return 0;
}
