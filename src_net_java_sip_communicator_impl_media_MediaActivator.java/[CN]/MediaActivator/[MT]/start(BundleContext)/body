{
  logger.debug("Started.");
  MediaActivator.bundleContext=context;
  mediaServiceImpl=new MediaServiceImpl();
  mediaServiceImpl.start();
  mediaServiceRegistration=context.registerService(MediaService.class.getName(),mediaServiceImpl,null);
  logger.debug("Media Service ... [REGISTERED]");
  context.registerService(ConfigurationForm.class.getName(),new LazyConfigurationForm("net.java.sip.communicator.impl.media.MediaConfigurationPanel",getClass().getClassLoader(),"plugin.mediaconfig.PLUGIN_ICON","impl.media.configform.TITLE",40),null);
  System.setProperty("gov.nist.core.STRIP_ADDR_SCOPES","true");
}
