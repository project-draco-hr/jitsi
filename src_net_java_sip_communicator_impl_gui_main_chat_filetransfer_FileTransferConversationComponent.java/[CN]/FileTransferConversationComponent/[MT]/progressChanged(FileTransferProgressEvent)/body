{
  progressBar.setValue((int)event.getProgress());
  ByteFormat format=new ByteFormat();
  String bytesSent=format.format(event.getFileTransfer().getTransferedBytes());
  progressBar.setString(bytesSent + " " + resources.getI18NString("service.gui.SENT"));
}
