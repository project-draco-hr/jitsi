{
  progressBar.setValue((int)event.getProgress());
  ByteFormat format=new ByteFormat();
  String bytesString=format.format(event.getFileTransfer().getTransferedBytes());
  progressBar.setString(getProgressLabel(bytesString));
}
