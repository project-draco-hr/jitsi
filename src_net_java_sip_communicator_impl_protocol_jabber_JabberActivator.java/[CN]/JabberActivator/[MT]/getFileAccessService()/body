{
  if (fileService == null) {
    ServiceReference fileReference=bundleContext.getServiceReference(FileAccessService.class.getName());
    fileService=(FileAccessService)bundleContext.getService(fileReference);
  }
  return fileService;
}
