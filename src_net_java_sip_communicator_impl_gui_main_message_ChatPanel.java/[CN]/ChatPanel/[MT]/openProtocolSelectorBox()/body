{
  SIPCommSelectorBox contactSelector=this.sendPanel.getContactSelectorBox();
  JPopupMenu popup=contactSelector.getPopup();
  if (!popup.isVisible()) {
    popup.setLocation(contactSelector.calculatePopupLocation());
    popup.setVisible(true);
  }
}
