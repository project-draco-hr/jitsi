{
  int i, j;
  float temp;
  float[] ptr1;
  int ptr1_offset;
  for (i=0; i < M; i++) {
    ptr1=L;
    ptr1_offset=i * M;
    temp=0;
    for (j=0; j < i; j++) {
      temp+=ptr1[ptr1_offset + j] * x[j];
    }
    temp=b[i] - temp;
    x[i]=temp;
  }
}
