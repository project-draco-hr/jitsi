{
  int i, j;
  float temp;
  float[] ptr1;
  int ptr1_offset;
  for (i=M - 1; i >= 0; i--) {
    ptr1=L;
    ptr1_offset=i;
    temp=0;
    for (j=M - 1; j > i; j--) {
      temp+=ptr1[ptr1_offset + j * M] * x[x_offset + j];
    }
    temp=b[i] - temp;
    x[x_offset + i]=temp;
  }
}
