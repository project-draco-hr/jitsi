{
  if (evt.getSource() instanceof CallPeer && ((CallPeer)evt.getSource()).isConferenceFocus())   return;
  if (evt.getLevels().size() > 0)   updateSoundBar(evt.getLevels().values().iterator().next());
}
