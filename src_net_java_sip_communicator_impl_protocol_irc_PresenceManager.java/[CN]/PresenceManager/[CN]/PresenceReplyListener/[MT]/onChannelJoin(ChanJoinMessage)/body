{
  final String user=msg.getSource().getNick();
  if (user == null || !PresenceManager.this.nickWatchList.contains(user)) {
    return;
  }
  PresenceManager.this.operationSet.updateNickContactPresence(user,IrcStatusEnum.ONLINE);
}
