{
  Window window=getWindow();
  if (window instanceof Frame) {
    Frame frame=(Frame)window;
    if ((frame.getExtendedState() & Frame.ICONIFIED) == Frame.ICONIFIED)     frame.setExtendedState(Frame.NORMAL);
  }
  window.toFront();
}
