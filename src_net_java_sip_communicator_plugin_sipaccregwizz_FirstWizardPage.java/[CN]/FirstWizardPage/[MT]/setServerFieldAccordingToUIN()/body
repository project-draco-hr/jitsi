{
  if (!enableAdvOpButton.isSelected()) {
    String serverAddress=wizard.getServerFromUserName(uinField.getText());
    serverField.setText(serverAddress);
    proxyField.setText(serverAddress);
  }
}
