{
  try {
    logger.logEntry();
    Text text=getTextNode(parentNode);
    if (text == null) {
      return null;
    }
 else {
      return text.getData();
    }
  }
  finally {
    logger.logExit();
  }
}
