{
  if (CallManager.isLocalUser(conferenceMember))   return;
  if (CallManager.addressesAreEqual(conferenceMember.getAddress(),callPeer.getAddress())) {
    return;
  }
  getCallContainer().enableConferenceInterface(CallManager.isVideoStreaming(call));
}
