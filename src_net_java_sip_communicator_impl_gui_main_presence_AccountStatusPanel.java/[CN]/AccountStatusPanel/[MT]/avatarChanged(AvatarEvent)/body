{
  currentImage=event.getNewAvatar();
  if (currentImage == null) {
    currentImage=ImageUtils.toByteArray(ImageLoader.getImage(ImageLoader.DEFAULT_USER_PHOTO));
  }
  AvatarCacheUtils.cacheAvatar(event.getSourceProvider(),currentImage);
  accountImageLabel.setImageIcon(currentImage);
}
