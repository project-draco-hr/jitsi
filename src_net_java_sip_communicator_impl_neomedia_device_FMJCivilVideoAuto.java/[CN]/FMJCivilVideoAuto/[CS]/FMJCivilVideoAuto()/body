{
  if (logger.isInfoEnabled())   logger.info("Start detecting Civil Capture Devices");
  final Class<?> clazz=Class.forName("net.sf.fmj.media.cdp.civil.CaptureDevicePlugger");
  final Method addCaptureDevices=clazz.getMethod("addCaptureDevices");
  final Object captureDevicePlugger=clazz.newInstance();
  addCaptureDevices.invoke(captureDevicePlugger);
  if (logger.isInfoEnabled())   logger.info("Finish detecting Civil Capture Devices");
}
