{
synchronized (players) {
    for (    Player player : players) {
      Component visualComponent=getVisualComponent(player);
      if (visualComponent != null)       return visualComponent;
    }
  }
  return null;
}
