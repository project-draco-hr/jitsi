{
  if (certificate.getSelectedItem() != null && certificate.getSelectedItem() instanceof CertificateConfigEntry)   return ((CertificateConfigEntry)certificate.getSelectedItem()).getId();
  return null;
}
