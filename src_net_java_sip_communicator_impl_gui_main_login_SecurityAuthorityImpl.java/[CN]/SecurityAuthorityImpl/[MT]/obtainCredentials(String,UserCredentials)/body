{
  AuthenticationWindow loginWindow=new AuthenticationWindow(mainFrame,protocolProvider,realm,userCredentials);
  loginWindow.setVisible(true);
  loginWindow.getPasswdField().requestFocus();
  return loginWindow.getUserCredentials();
}
