{
  if (!answerUnconditional)   return false;
  @SuppressWarnings("unchecked") Iterator<? extends CallPeer> peers=call.getCallPeers();
  while (peers.hasNext()) {
    final CallPeer peer=peers.next();
    answerPeer(peer);
  }
  return true;
}
