{
  if (logger.isInfoEnabled())   logger.info("pausing transmission... ");
  for (  SendStream sendStream : sendStreams) {
    try {
      sendStream.stop();
    }
 catch (    IOException ex) {
      logger.warn("Exception when pausing transmission ",ex);
    }
  }
}
