{
  if (evt instanceof NewParticipantEvent) {
    Participant p=((NewParticipantEvent)evt).getParticipant();
    if (logger.isInfoEnabled())     logger.info("A new participant had just joined: " + p.getCNAME());
  }
}
