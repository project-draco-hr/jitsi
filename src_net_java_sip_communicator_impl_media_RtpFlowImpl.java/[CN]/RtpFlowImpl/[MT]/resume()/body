{
  if (logger.isInfoEnabled())   logger.info("resuming transmission... ");
  for (  SendStream sendStream : sendStreams) {
    try {
      sendStream.start();
    }
 catch (    IOException ex) {
      logger.warn("Exception when resuming transmission ",ex);
    }
  }
}
