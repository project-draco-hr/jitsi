{
  if (chatTransport.allowsInstantMessage()) {
    Image img=createTransportStatusImage(chatTransport);
    JMenuItem menuItem=new JMenuItem("From: " + chatTransport.getProtocolProvider().getAccountID().getAccountAddress() + " To: "+ chatTransport.getName(),new ImageIcon(img));
    menuItem.addActionListener(this);
    this.transportMenuItems.put(chatTransport,menuItem);
    this.menu.add(menuItem);
    updateEnableStatus();
  }
}
