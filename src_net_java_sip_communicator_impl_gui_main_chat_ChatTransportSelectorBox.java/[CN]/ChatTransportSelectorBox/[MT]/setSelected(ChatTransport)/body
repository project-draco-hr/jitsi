{
  JCheckBoxMenuItem menuItem=transportMenuItems.get(chatTransport);
  if (menuItem == null)   return;
  this.setSelected(menuItem,chatTransport,new ImageIcon(createTransportStatusImage(chatTransport)));
}
