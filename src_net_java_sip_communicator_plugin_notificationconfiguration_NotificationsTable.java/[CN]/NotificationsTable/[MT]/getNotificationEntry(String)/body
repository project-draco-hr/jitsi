{
  for (int row=0; row < notifTable.getRowCount(); row++) {
    NotificationEntry entry=(NotificationEntry)notifTable.getValueAt(row,notifTable.getColumnCount() - 1);
    if (entry.getEvent() == eventName)     return entry;
  }
  return null;
}
