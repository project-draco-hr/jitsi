{
  for (int row=0; row < notifTable.getRowCount(); row++) {
    NotificationEntry e=(NotificationEntry)notifTable.getValueAt(row,notifTable.getColumnCount() - 1);
    if (e.equals(entry))     return row;
  }
  return -1;
}
