{
  this.popup=p;
  ExportedWindow win=SystrayActivator.getUIService().getExportedWindow(ExportedWindow.MAIN_WINDOW);
  if (win != null) {
    Object source=win.getSource();
    if (source instanceof JFrame)     this.mframe=(Component)source;
  }
}
