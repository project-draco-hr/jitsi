{
  String actName=((JButton)e.getSource()).getName();
  if (actName.equals("grab")) {
    if (this.videoComponent == null)     return;
    this.grabSnapshot.setEnabled(false);
    new SnapshotTimer().start();
  }
 else {
    close(false);
    this.setVisible(false);
  }
}
