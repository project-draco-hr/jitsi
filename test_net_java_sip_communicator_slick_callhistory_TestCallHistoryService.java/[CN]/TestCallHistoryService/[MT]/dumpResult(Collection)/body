{
  Iterator rs=c.iterator();
  while (rs.hasNext()) {
    CallRecord hr=(CallRecord)rs.next();
    logger.info("----------------------");
    logger.info(hr.getPeerRecords());
    logger.info("----------------------");
  }
}
