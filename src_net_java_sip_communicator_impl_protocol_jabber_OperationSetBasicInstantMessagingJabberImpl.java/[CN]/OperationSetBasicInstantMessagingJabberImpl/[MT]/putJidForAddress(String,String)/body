{
synchronized (jids) {
    purgeOldJids();
    TargetAddress ta=jids.get(address);
    if (ta == null)     ta=new TargetAddress();
    ta.jid=jid;
    ta.lastUpdatedTime=System.currentTimeMillis();
  }
}
