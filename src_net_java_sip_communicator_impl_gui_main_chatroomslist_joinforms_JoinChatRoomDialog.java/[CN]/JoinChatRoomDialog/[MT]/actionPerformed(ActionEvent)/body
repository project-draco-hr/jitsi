{
  JButton button=(JButton)e.getSource();
  String name=button.getName();
  if (name.equals("service.gui.JOIN")) {
    GuiActivator.getMUCService().joinChatRoom(searchPanel.getChatRoomName(),chatRoomProvider);
  }
  this.dispose();
}
