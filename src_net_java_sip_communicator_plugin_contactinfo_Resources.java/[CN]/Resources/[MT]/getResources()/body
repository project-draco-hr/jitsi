{
  if (resourcesService == null) {
    ServiceReference serviceReference=ContactInfoActivator.bundleContext.getServiceReference(ResourceManagementService.class.getName());
    if (serviceReference == null)     return null;
    resourcesService=(ResourceManagementService)ContactInfoActivator.bundleContext.getService(serviceReference);
  }
  return resourcesService;
}
