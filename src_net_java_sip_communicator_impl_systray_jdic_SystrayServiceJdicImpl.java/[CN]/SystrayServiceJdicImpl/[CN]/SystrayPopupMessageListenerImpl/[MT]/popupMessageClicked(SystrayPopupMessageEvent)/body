{
  UIService uiService=SystrayActivator.getUIService();
  ExportedWindow chatWindow=uiService.getExportedWindow(ExportedWindow.CHAT_WINDOW);
  if (chatWindow != null)   chatWindow.bringToFront();
  Object o=evt.getTag();
  if (o instanceof Contact) {
  }
}
