{
  String formattedMessage=message;
  if (keyword != null) {
    formattedMessage=processKeyWords(message,keyword);
  }
  return this.processMessage(contactName,date,messageType,formattedMessage);
}
