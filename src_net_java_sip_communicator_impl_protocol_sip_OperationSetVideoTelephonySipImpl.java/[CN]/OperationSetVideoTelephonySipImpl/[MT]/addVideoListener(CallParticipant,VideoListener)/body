{
  if (listener == null)   throw new NullPointerException("listener");
  ((CallSipImpl)participant.getCall()).getMediaCallSession().addVideoListener(new InternalVideoListener(this,participant,listener));
}
