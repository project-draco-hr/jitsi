{
synchronized (this) {
    if (evt != null)     return;
    try {
      this.wait(millis);
    }
 catch (    InterruptedException ex) {
      logger.error("Interrupted while waiting for contact creation",ex);
    }
  }
}
