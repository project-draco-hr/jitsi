{
synchronized (this) {
    Contact contact=evt.getSourceGroup().getContact(subscriptionAddress);
    if (contact != null) {
      this.evt=evt;
      this.sourceContact=contact;
      this.notifyAll();
    }
  }
}
