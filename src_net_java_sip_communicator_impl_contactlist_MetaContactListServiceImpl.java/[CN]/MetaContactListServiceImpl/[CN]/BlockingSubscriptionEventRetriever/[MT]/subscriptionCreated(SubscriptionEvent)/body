{
synchronized (this) {
    if (evt.getSourceContact().getAddress().equals(subscriptionAddress) || evt.getSourceContact().equals(subscriptionAddress)) {
      this.evt=evt;
      this.sourceContact=evt.getSourceContact();
      this.notifyAll();
    }
  }
}
