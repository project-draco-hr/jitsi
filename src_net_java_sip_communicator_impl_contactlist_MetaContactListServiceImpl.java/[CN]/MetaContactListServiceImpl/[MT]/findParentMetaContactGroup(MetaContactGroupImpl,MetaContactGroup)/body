{
  if (root.contains(child))   return root;
  Iterator subgroups=root.getSubgroups();
  while (subgroups.hasNext()) {
    MetaContactGroup contactGroup=findParentMetaContactGroup((MetaContactGroupImpl)subgroups.next(),child);
    if (contactGroup != null)     return contactGroup;
  }
  return null;
}
