{
  metaContainer.removeProtoGroup(groupToRemove);
  removeAllContactsForProvider(metaContainer,sourceProvider);
  fireMetaContactGroupEvent(metaContainer,sourceProvider,MetaContactGroupEvent.CONTACT_GROUP_REMOVED_FROM_META_GROUP);
}
