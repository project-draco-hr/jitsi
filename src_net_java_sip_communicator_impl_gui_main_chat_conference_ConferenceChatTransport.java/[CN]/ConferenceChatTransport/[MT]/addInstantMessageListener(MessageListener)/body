{
  if (!allowsInstantMessage())   return;
  OperationSetBasicInstantMessaging imOpSet=(OperationSetBasicInstantMessaging)chatRoom.getParentProvider().getOperationSet(OperationSetBasicInstantMessaging.class);
  imOpSet.addMessageListener(l);
}
