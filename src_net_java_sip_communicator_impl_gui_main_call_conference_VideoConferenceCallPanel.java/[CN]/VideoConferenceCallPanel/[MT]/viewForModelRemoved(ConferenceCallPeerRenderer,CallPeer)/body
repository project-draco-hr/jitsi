{
  videoContainer.remove(callPeerPanel.getComponent());
  if ((callPeer != null) && (callPeerPanel instanceof ConferenceParticipantContainer)) {
    removeConferenceMemberContainers((ConferenceParticipantContainer)callPeerPanel,true);
  }
}
