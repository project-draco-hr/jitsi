{
  boolean isAudioSecured=(audioStream == null) || audioStream.getSrtpControl().getSecureCommunicationStatus();
  if (!isAudioSecured)   return false;
  boolean isVideoSecured=(videoStream == null) || videoStream.getSrtpControl().getSecureCommunicationStatus();
  if (!isVideoSecured)   return false;
  return true;
}
