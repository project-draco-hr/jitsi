{
synchronized (streamAudioLevelListenerLock) {
    this.streamAudioLevelListener=listener;
    MediaStream audioStream=getStream(MediaType.AUDIO);
    if (audioStream != null) {
      ((AudioMediaStream)audioStream).setStreamAudioLevelListener(listener);
    }
  }
}
