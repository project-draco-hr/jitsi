{
  VisualComponentResolveListener[] listeners;
synchronized (visualComponentResolveListeners) {
    listeners=this.visualComponentResolveListeners.toArray(new VisualComponentResolveListener[this.visualComponentResolveListeners.size()]);
  }
  for (  VisualComponentResolveListener l : listeners) {
    l.visualComponentResolved(new VisualComponentResolveEvent(this,visualComponent,conferenceMember));
  }
}
