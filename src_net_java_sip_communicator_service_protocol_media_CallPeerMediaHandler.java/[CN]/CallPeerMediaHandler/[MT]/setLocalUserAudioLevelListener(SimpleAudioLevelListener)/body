{
synchronized (localAudioLevelListenerLock) {
    this.localAudioLevelListener=listener;
    MediaStream audioStream=getStream(MediaType.AUDIO);
    if (audioStream != null) {
      ((AudioMediaStream)audioStream).setLocalUserAudioLevelListener(listener);
    }
  }
}
