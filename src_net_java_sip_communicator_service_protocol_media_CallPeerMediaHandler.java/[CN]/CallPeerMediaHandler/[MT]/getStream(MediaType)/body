{
  MediaHandler mediaHandler=getMediaHandler();
  if (mediaHandler != null)   return mediaHandler.getStream(this,mediaType);
switch (mediaType) {
case AUDIO:
    return audioStream;
case VIDEO:
  return videoStream;
default :
throw new IllegalArgumentException("mediaType");
}
}
