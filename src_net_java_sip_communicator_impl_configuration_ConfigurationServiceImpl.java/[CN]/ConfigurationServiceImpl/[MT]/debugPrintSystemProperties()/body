{
  if (logger.isDebugEnabled()) {
    for (    Map.Entry<Object,Object> entry : System.getProperties().entrySet())     if (logger.isDebugEnabled())     logger.debug(entry.getKey() + "=" + entry.getValue());
  }
}
