{
  defaultImage=ImageLoader.getImage(ImageLoader.CALL_HISTORY_BUTTON);
  if (!isToggleDisabled())   pressedImage=ImageLoader.getImage(ImageLoader.CALL_HISTORY_BUTTON_PRESSED);
  setHistoryView();
  super.loadSkin();
}
