{
  Enumeration statusCombos=protocolStatusCombos.elements();
  while (statusCombos.hasMoreElements()) {
    StatusSelectorBox statusSelectorBox=(StatusSelectorBox)statusCombos.nextElement();
    if (statusSelectorBox.isSelected()) {
      return true;
    }
  }
  return false;
}
