{
  StatusSelectorBox protocolStatusCombo=(StatusSelectorBox)this.protocolStatusCombos.get(protocolProvider);
  if (protocolStatusCombo == null)   return;
  protocolStatusCombo.setAccountIndex(mainFrame.getProviderIndex(protocolProvider));
  this.revalidate();
  this.repaint();
}
