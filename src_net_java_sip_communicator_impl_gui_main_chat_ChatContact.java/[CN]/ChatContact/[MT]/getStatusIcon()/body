{
  if (!isMultiChatContact) {
    return new ImageIcon(Constants.getStatusIcon(((Contact)sourceContact).getPresenceStatus()));
  }
 else {
    return new ImageIcon(Constants.getStatusIcon(Constants.ONLINE_STATUS));
  }
}
