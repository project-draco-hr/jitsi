{
  int pos=0;
  try {
    while (pos < len) {
      int c=read();
      if (c == -1) {
        if (pos == 0)         pos=-1;
        break;
      }
      bytes[off + pos]=(byte)c;
      pos++;
    }
  }
 catch (  IOException e) {
    pos=-1;
  }
  return pos;
}
