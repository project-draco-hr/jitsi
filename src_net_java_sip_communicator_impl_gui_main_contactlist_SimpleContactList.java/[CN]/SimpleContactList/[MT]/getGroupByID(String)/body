{
  Iterator<MetaContactGroup> i=contactListService.getRoot().getSubgroups();
  while (i.hasNext()) {
    MetaContactGroup group=i.next();
    if (group.getMetaUID().equals(metaUID))     return group;
  }
  return null;
}
