{
  Object sService=SipActivator.getBundleContext().getService(event.getServiceReference());
  if (!(sService instanceof CertificateVerificationService)) {
    return;
  }
  if (event.getType() == ServiceEvent.REGISTERED)   guiVerification=(CertificateVerificationService)sService;
 else   if (event.getType() == ServiceEvent.UNREGISTERING)   guiVerification=null;
}
