{
  ConfigurationService configService=SipActivator.getConfigurationService();
  String trafficClass=(String)configService.getProperty(SIP_DSCP_PROPERTY);
  if (trafficClass != null) {
    return Integer.parseInt(trafficClass);
  }
  return 0;
}
