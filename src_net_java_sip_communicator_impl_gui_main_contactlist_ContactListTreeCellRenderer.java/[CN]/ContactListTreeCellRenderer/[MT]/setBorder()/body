{
  if (treeNode instanceof ContactNode && !(((ContactNode)treeNode).getContactDescriptor() instanceof ShowMoreContact)) {
    this.setBorder(BorderFactory.createEmptyBorder(TOP_BORDER,LEFT_BORDER,BOTTOM_BORDER,RIGHT_BORDER));
  }
 else {
    this.setBorder(BorderFactory.createEmptyBorder(4,LEFT_BORDER,0,RIGHT_BORDER));
  }
}
