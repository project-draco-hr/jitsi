{
  Contact actualReturn=metaContact.getContact(mockContact.getAddress(),MclSlickFixture.mockProvider);
  assertNotNull("getContact() return null.",actualReturn);
  assertSame("getContact() did not return the right proto group.",mockContact,actualReturn);
}
