{
  try {
    if (!peer.getState().equals(CallPeerState.INCOMING_CALL)) {
      timer.cancel();
    }
 else {
      serverTran.sendResponse(response);
    }
  }
 catch (  Exception ex) {
    timer.cancel();
  }
}
