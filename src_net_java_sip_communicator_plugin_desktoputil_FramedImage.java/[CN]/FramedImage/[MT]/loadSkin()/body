{
  ImageIcon frameIcon=DesktopUtilActivator.getResources().getImage("service.gui.USER_PHOTO_FRAME");
  if (frameIcon.getIconWidth() >= width && frameIcon.getIconHeight() >= frameIcon.getIconHeight()) {
    this.frameImage=ImageUtils.scaleImageWithinBounds(frameIcon.getImage(),width,height);
  }
}
