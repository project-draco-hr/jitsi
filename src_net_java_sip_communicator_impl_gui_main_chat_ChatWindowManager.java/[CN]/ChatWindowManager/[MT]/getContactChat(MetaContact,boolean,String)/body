{
  if (!create)   return getContactChat(metaContact,null,create,escapedMessageID);
 else {
    MetaContactChatCreateRunnable runnable=new MetaContactChatCreateRunnable(metaContact,null,escapedMessageID);
    return runnable.getChatPanel();
  }
}
