{
  Iterator<ChatPanel> chatPanelsIter=chatPanels.iterator();
  while (chatPanelsIter.hasNext()) {
    ChatPanel chatPanel=chatPanelsIter.next();
    if (chatSession.equals(chatPanel.getChatSession()))     return chatPanel;
  }
  return null;
}
