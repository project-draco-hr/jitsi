{
synchronized (syncChat) {
    ChatSession chatSession=findChatSessionForDescriptor(chatRoomWrapper);
    if (chatSession != null)     return getChat(chatSession);
 else     return createChat(chatRoomWrapper);
  }
}
