{
  if (!create)   return getContactChat(metaContact,null,null,create,null);
 else {
    MetaContactChatCreateRunnable runnable=new MetaContactChatCreateRunnable(metaContact,null,null,null);
    return runnable.getChatPanel();
  }
}
