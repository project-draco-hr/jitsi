{
  if (!create)   return getContactChat(metaContact,null,create,null);
 else {
    MetaContactChatCreateRunnable runnable=new MetaContactChatCreateRunnable(metaContact,null,null);
    return runnable.getChatPanel();
  }
}
