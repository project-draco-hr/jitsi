{
synchronized (syncChat) {
    ChatSession chatSession=findChatSessionForDescriptor(metaContact);
    if (chatSession != null)     return getChat(chatSession);
 else     return createChat(metaContact,protocolContact,escapedMessageID);
  }
}
