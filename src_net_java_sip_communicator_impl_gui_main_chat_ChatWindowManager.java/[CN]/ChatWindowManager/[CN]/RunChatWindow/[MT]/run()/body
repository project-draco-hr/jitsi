{
  ChatPanel chatPanel=getContactChat(metaContact,protocolContact);
  if (chatPanel == null)   return;
  if (isSmsSelected != null)   chatPanel.setSmsSelected(isSmsSelected);
  openChat(chatPanel,true);
}
