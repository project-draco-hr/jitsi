{
  List details=infoRetreiver.getContactDetails(uin);
  Iterator iter=details.iterator();
  while (iter.hasNext()) {
    Object obj=iter.next();
    if (detailClass.isAssignableFrom(obj.getClass()))     return true;
  }
  return false;
}
