{
synchronized (listeners) {
    listeners.add(listener);
  }
  NetaddrActivator.getBundleContext().addServiceListener(this);
  this.systemActivityNotificationsService=ServiceUtils.getService(NetaddrActivator.getBundleContext(),SystemActivityNotificationsService.class);
  this.systemActivityNotificationsService.addSystemActivityChangeListener(this);
}
