{
  if (logger.isTraceEnabled())   logger.trace("Query: " + query + " setStatus "+ status);
  super.setStatus(status);
  if (status == QUERY_CANCELED) {
    getContactSource().clearRemainingNotifications();
  }
}
