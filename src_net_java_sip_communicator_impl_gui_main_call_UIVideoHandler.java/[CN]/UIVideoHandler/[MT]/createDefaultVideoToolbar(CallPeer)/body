{
  ConferencePeerPanel peerPanel=new ConferencePeerPanel((ConferenceCallPanel)callRenderer,callRenderer.getCallContainer(),callPeer,true);
  peerPanel.setPeerName(peerPanel.getParticipantName() + " unresolved");
  return peerPanel;
}
