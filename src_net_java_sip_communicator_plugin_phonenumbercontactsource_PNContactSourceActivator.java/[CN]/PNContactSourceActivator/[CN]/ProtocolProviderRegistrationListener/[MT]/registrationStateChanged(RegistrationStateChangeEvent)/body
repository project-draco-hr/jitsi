{
  ProtocolProviderService protocolProvider=evt.getProvider();
  if (evt.getNewState() == RegistrationState.REGISTERED) {
    if (!phoneProviders.contains(protocolProvider))     phoneProviders.add(protocolProvider);
  }
}
