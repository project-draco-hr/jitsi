{
  String securityString=event.getSecurityString();
  if (securityString != null) {
    securityStringLabel.setText(securityString);
  }
  sasVerificationButton.setImage(event.isSecurityVerified() ? iconEncrVerified : iconEncr);
  revalidate();
  repaint();
}
