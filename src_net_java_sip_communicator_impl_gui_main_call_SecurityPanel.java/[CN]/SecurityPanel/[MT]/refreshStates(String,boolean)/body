{
  if (securityString != null) {
    securityStringLabel.setText(securityString);
  }
  sasVerificationButton.setImage(isSecurityVerified ? iconEncrVerified : iconEncr);
  revalidate();
  repaint();
}
