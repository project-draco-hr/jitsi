{
  ContactIcqImpl contact=ssContactList.findContactByScreenName(screenname.getFormatted());
  if (contact != null) {
    byte[] oldImage=contact.getImage();
    contact.setImage(icon);
    fireContactPropertyChangeEvent(ContactPropertyChangeEvent.PROPERTY_IMAGE,contact,null,icon);
  }
}
