{
  InputStream is=ProtocolIconDictImpl.getResources().getImageInputStreamForPath(imagePath);
  byte[] icon=null;
  try {
    icon=new byte[is.available()];
    is.read(icon);
  }
 catch (  IOException exc) {
    logger.error("Failed to load icon: " + imagePath,exc);
  }
  return icon;
}
