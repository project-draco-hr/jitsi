{
  RawPacket pkt=super.createRawPacket(buffer,offset,length);
  PacketTransformer transformer=getTransformer();
  if (transformer != null) {
    pkt=transformer.transform(pkt);
    if ((pkt == null) && (targets.size() > 0))     throw new NullPointerException("pkt");
  }
  return pkt;
}
