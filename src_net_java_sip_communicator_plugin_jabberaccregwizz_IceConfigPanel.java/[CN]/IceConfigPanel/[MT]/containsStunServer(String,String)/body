{
  for (int i=0; i < tableModel.getRowCount(); i++) {
    StunServer stunServer=(StunServer)tableModel.getValueAt(i,0);
    if (stunServer.getIpAddress().equalsIgnoreCase(address) && stunServer.getPort().equalsIgnoreCase(port))     return true;
  }
  return false;
}
