{
  LinkedList<StunServerDescriptor> serversList=new LinkedList<StunServerDescriptor>();
  Iterator<Vector<StunServerDescriptor>> i=tableModel.getDataVector().iterator();
  while (i.hasNext()) {
    Vector<StunServerDescriptor> row=i.next();
    serversList.add(row.elementAt(0));
  }
  return serversList;
}
