{
  AudioLevelEffect levelEffect=new AudioLevelEffect(new SimpleAudioLevelListener(){
    public void audioLevelChanged(    int level){
      fireLocalUserAudioLevelEvent(level);
    }
  }
);
  tc.setCodecChain(new Codec[]{levelEffect});
}
