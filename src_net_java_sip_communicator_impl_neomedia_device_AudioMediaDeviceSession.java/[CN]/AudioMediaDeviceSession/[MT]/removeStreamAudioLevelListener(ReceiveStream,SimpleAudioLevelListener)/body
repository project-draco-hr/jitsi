{
synchronized (streamAudioLevelListeners) {
    AudioLevelEventDispatcher dispatcher=streamAudioLevelListeners.get(receiveStream);
    if (dispatcher != null) {
      dispatcher.removeAudioLevelListener(listener);
    }
  }
}
