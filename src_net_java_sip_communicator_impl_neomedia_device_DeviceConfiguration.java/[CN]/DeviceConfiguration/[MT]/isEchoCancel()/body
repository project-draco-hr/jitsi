{
  ConfigurationService cfg=NeomediaActivator.getConfigurationService();
  boolean value=DEFAULT_AUDIO_ECHOCANCEL;
  if (cfg != null)   value=cfg.getBoolean(PROP_AUDIO_ECHOCANCEL,value);
  return value;
}
