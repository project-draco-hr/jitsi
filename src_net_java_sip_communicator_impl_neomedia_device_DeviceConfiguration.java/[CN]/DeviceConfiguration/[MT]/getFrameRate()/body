{
  if (frameRate == -1) {
    ConfigurationService cfg=NeomediaActivator.getConfigurationService();
    int value=DEFAULT_VIDEO_FRAMERATE;
    if (cfg != null)     value=cfg.getInt(PROP_VIDEO_FRAMERATE,value);
    frameRate=value;
  }
  return frameRate;
}
