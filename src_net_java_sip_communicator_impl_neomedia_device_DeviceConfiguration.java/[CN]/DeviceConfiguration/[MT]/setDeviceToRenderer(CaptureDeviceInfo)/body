{
  if (devInfo == null)   return;
  try {
    net.java.sip.communicator.impl.neomedia.renderer.audio.PortAudioRenderer.setDevice(devInfo.getLocator());
  }
 catch (  Exception e) {
    logger.error("error setting device to renderer",e);
  }
}
