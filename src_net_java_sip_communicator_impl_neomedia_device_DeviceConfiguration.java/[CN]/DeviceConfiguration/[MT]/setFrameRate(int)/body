{
  this.frameRate=frameRate;
  ConfigurationService cfg=NeomediaActivator.getConfigurationService();
  if (frameRate != DEFAULT_VIDEO_FRAMERATE)   cfg.setProperty(PROP_VIDEO_FRAMERATE,frameRate);
 else   cfg.removeProperty(PROP_VIDEO_FRAMERATE);
}
