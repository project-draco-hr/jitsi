{
  this.frameRate=frameRate;
  if (frameRate != DEFAULT_FRAME_RATE) {
    NeomediaActivator.getConfigurationService().setProperty(PROP_VIDEO_FRAMERATE,frameRate);
  }
 else {
    NeomediaActivator.getConfigurationService().removeProperty(PROP_VIDEO_FRAMERATE);
  }
}
