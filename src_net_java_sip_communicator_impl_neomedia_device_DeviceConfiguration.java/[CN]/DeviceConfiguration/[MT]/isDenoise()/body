{
  ConfigurationService cfg=NeomediaActivator.getConfigurationService();
  boolean value=DEFAULT_AUDIO_DENOISE;
  if (cfg != null)   value=cfg.getBoolean(PROP_AUDIO_DENOISE,value);
  return value;
}
