{
  CaptureDeviceInfo dev=null;
switch (useCase) {
case ANY:
case CALL:
    dev=videoCaptureDevice;
  break;
case DESKTOP:
CaptureDeviceInfo devs[]=getAvailableVideoCaptureDevices(MediaUseCase.DESKTOP);
if (devs.length > 0) dev=devs[0];
break;
default :
break;
}
return dev;
}
