{
  historyMenu=new HistoryMenu(this);
  this.setSize(Constants.HISTORY_WINDOW_WIDTH,Constants.HISTORY_WINDOW_HEIGHT);
  this.setIconImage(ImageLoader.getImage(ImageLoader.SIP_LOGO));
  this.init();
}
