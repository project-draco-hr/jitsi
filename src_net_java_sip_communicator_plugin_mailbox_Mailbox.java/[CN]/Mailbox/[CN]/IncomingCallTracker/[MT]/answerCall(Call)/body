{
  OperationSetBasicTelephony telephony=call.getProtocolProvider().getOperationSet(OperationSetBasicTelephony.class);
  Iterator peers=call.getCallPeers();
  while (peers.hasNext()) {
    CallPeer peer=(CallPeer)peers.next();
    try {
      telephony.answerCallPeer(peer);
    }
 catch (    OperationFailedException exc) {
      logger.error("Could not answer to : " + peer + " caused by the following exception: "+ exc.getMessage(),exc);
    }
  }
}
