{
  Call sourceCall=event.getSourceCall();
  if (incomingCalls.contains(sourceCall))   incomingCalls.remove(sourceCall);
 else   if (outgoingCalls.contains(sourceCall))   outgoingCalls.remove(sourceCall);
}
