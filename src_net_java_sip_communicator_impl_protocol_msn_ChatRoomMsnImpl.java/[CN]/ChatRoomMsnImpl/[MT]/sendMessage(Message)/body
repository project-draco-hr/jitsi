{
  if (!isJoined()) {
    throw new OperationFailedException("This chat room is not active.",OperationFailedException.CHAT_ROOM_NOT_JOINED);
  }
  switchboard.sendText(message.getContent());
  ChatRoomMessageDeliveredEvent msgDeliveredEvt=new ChatRoomMessageDeliveredEvent(this,new Date(),message,ChatRoomMessageDeliveredEvent.CONVERSATION_MESSAGE_DELIVERED);
  fireMessageEvent(msgDeliveredEvt);
}
