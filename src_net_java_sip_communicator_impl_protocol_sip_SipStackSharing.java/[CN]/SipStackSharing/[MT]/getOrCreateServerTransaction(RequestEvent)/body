{
  if (event.getServerTransaction() != null)   return event.getServerTransaction();
 else {
    SipProvider jainSipProvider=(SipProvider)event.getSource();
    return jainSipProvider.getNewServerTransaction(event.getRequest());
  }
}
