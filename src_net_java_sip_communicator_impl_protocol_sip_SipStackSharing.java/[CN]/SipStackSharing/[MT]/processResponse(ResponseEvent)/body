{
  logger.trace(responseEvent);
  ProtocolProviderServiceSipImpl recipient=findTargetFor(responseEvent.getClientTransaction());
  if (recipient != null)   recipient.processResponse(responseEvent);
 else   logger.error("couldn't find a ProtocolProviderServiceSipImpl " + "to dispatch to");
}
