{
  super(GuiActivator.getUIService().getMainFrame(),GuiActivator.getResources().getI18NString("service.gui.CERT_DIALOG_TITLE"),true);
  this.cert=certs[0];
  this.host=host;
  this.port=port;
  setMinimumSize(new Dimension(600,200));
  setDefaultCloseOperation(JDialog.DISPOSE_ON_CLOSE);
  init();
  setLocationRelativeTo(getParent());
}
