{
  this.invoker=invoker;
  this.init();
  for (  Object o : telephonyObjects) {
    if (o instanceof Contact)     this.addTelephonyContactItem((Contact)o);
 else     if (o instanceof ChatTransport)     this.addTelephonyChatTransportItem((ChatTransport)o);
  }
}
