{
  this.invoker=invoker;
  this.init(GuiActivator.getResources().getI18NString("service.gui.CHOOSE_CONTACT"));
  for (  Object o : telephonyObjects) {
    if (o instanceof UIContactDetail)     this.addTelephonyContactItem((UIContactDetail)o,opSetClass);
 else     if (o instanceof ChatTransport)     this.addTelephonyChatTransportItem((ChatTransport)o,opSetClass);
  }
}
