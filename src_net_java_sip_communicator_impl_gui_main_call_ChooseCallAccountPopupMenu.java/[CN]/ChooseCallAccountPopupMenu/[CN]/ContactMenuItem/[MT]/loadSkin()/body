{
  BufferedImage contactIcon=null;
  PresenceStatus status=contact.getPresenceStatus();
  if (status != null)   contactIcon=Constants.getStatusIcon(status);
 else   contactIcon=Constants.getStatusIcon(Constants.OFFLINE_STATUS);
  if (contactIcon != null)   this.setIcon(new ImageIcon(contactIcon));
}
