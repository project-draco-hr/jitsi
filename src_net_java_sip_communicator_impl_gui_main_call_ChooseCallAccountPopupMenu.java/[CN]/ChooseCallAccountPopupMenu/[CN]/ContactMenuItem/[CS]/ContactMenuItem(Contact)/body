{
  this.contact=contact;
  this.setText(contact.getDisplayName());
  BufferedImage contactIcon=Constants.getStatusIcon(contact.getPresenceStatus());
  if (contactIcon != null)   this.setIcon(new ImageIcon(contactIcon));
}
