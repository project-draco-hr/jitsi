{
  this.chatTransport=chatTransport;
  this.setText(chatTransport.getDisplayName());
  BufferedImage contactIcon=Constants.getStatusIcon(chatTransport.getStatus());
  if (contactIcon != null)   this.setIcon(new ImageIcon(contactIcon));
}
