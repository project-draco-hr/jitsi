{
  PresenceStatus status=chatTransport.getStatus();
  Image statusIcon=null;
  statusIcon=ImageLoader.getIndexedProtocolImage(ImageUtils.getBytesInImage(status.getStatusIcon()),chatTransport.getProtocolProvider());
  if (statusIcon != null)   this.setIcon(new ImageIcon(statusIcon));
}
