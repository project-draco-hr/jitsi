{
  PresenceStatus status=chatTransport.getStatus();
  Image statusIcon=null;
  statusIcon=ImageLoader.badgeImageWithProtocolIndex(ImageUtils.getBytesInImage(status.getStatusIcon()),chatTransport.getProtocolProvider());
  if (statusIcon != null)   this.setIcon(new ImageIcon(statusIcon));
}
