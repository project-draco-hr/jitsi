{
  QualityControl qControls=getMediaQualityControl();
  if (qControls != null) {
    qControls.setRemoteSendMaxPreset(preset);
    try {
      peer.sendModifyVideoResolutionContent();
    }
 catch (    Throwable ex) {
      ProtocolProviderServiceJabberImpl.throwOperationFailedException("Failed to re-invite for video quality change.",OperationFailedException.INTERNAL_ERROR,ex,logger);
    }
  }
}
