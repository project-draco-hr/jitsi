{
  for (int i=0; i < listModel.getSize(); i++) {
    Object o=listModel.getElementAt(i);
    if (o instanceof ChatRoom) {
      if ((((ChatRoom)o).getName()).equalsIgnoreCase(chatRoomName))       return ((ChatRoom)(o));
    }
  }
  return null;
}
