{
  try {
    Document doc=this.conversationPanel.getChatTextPane().getDocument();
    doc.remove(0,doc.getLength());
  }
 catch (  BadLocationException ex) {
    logger.error(ex);
  }
  loadHistory();
}
