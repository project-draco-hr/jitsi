{
  writeMessagePanel.setSelectedChatTransport(chatTransport,isMessageOrFileTransferReceived);
  if (chatSession != null && !chatTransport.equals(chatSession.getCurrentChatTransport()))   return;
  setChatIcon(new ImageIcon(Constants.getStatusIcon(chatTransport.getStatus())));
}
