{
  if (subjectPanel != null) {
    subjectPanel.setSubject(subject);
    this.processMessage(chatSession.getChatName(),new Date(System.currentTimeMillis()),Constants.STATUS_MESSAGE,GuiActivator.getResources().getI18NString("service.gui.CHAT_ROOM_SUBJECT_CHANGED",new String[]{chatSession.getChatName(),subject}),ChatConversationPanel.TEXT_CONTENT_TYPE);
  }
}
