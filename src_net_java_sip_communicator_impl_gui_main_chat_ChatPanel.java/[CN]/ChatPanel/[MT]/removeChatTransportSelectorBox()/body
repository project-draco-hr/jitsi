{
  if (transportSelectorBox == null)   return;
  JPanel sendPanel=getChatSendPanel().getSendPanel();
  sendPanel.remove(transportSelectorBox);
  sendPanel.remove(sendViaLabel);
  this.revalidate();
  this.repaint();
}
