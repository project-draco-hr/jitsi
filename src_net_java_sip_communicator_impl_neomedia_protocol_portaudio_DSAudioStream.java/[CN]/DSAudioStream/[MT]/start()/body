{
  if (stream == null) {
    AudioFormat audioFormat=(AudioFormat)DataSource.getCaptureFormat();
    stream=PortAudioManager.getInstance().getInputStream(deviceIndex,audioFormat.getSampleRate(),audioFormat.getChannels());
  }
  stream.start();
}
