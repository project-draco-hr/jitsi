{
  if ((fingerprint == null) || contact == null)   return;
  this.configurator.setProperty(contact.getAddress() + fingerprint + ".fingerprint.verified",false);
  for (  ScOtrKeyManagerListener l : getListeners())   l.contactVerificationStatusChanged(contact);
}
