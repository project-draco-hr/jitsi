{
  if ((contact == null) || !isVerified(contact))   return;
  this.configurator.removeProperty(contact.getAddress() + ".publicKey.verified");
  for (  ScOtrKeyManagerListener l : listeners)   l.contactVerificationStatusChanged(contact);
}
