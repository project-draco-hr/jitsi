{
  if (contact == null)   return;
  if (this.isVerified(contact))   return;
  this.configurator.setProperty(contact.getAddress() + ".publicKey.verified",true);
  for (  ScOtrKeyManagerListener l : listeners)   l.contactVerificationStatusChanged(contact);
}
