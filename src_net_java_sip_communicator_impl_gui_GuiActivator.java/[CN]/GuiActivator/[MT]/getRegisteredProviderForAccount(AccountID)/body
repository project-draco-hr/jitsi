{
  ProtocolProviderFactory providerFactory=getProtocolProviderFactory(accountID.getProtocolDisplayName());
  ServiceReference serRef=providerFactory.getProviderForAccount(accountID);
  return (ProtocolProviderService)GuiActivator.bundleContext.getService(serRef);
}
