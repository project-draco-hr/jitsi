{
  if (msgHistoryService == null) {
    ServiceReference serviceReference=bundleContext.getServiceReference(MessageHistoryService.class.getName());
    msgHistoryService=(MessageHistoryService)bundleContext.getService(serviceReference);
  }
  return msgHistoryService;
}
