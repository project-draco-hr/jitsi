{
  if (mucService == null) {
    ServiceReference mucServiceReference=bundleContext.getServiceReference(MUCService.class.getName());
    mucService=(MUCService)bundleContext.getService(mucServiceReference);
  }
  return mucService;
}
