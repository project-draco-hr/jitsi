{
  ChatRoomYahooImpl chatRoom=(ChatRoomYahooImpl)invitation.getTargetChatRoom();
  try {
    yahooProvider.getYahooSession().declineConferenceInvite(chatRoom.getYahooConference(),rejectReason);
  }
 catch (  IOException e) {
    logger.debug("Failed to reject Invitation: " + e);
  }
}
