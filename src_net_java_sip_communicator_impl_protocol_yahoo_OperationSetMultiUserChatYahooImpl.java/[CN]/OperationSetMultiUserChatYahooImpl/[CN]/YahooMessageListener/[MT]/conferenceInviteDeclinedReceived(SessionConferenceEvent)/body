{
  logger.debug("Group Chat invite declined received. " + ev.toString());
  try {
    ChatRoom chatRoom=findRoom(ev.getRoom());
    fireInvitationRejectedEvent(chatRoom,ev.getFrom(),ev.getMessage());
  }
 catch (  Exception e) {
    logger.debug("Error: " + e);
  }
}
