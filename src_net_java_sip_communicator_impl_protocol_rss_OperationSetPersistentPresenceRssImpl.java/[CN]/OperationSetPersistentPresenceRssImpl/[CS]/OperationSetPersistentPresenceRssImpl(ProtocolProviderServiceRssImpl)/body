{
  super(provider);
  contactListRoot=new ContactGroupRssImpl("RootGroup",provider);
  parentProvider.addRegistrationStateChangeListener(new UnregistrationListener());
  imageRetriever=new ImageRetriever(this);
  imageRetriever.start();
}
