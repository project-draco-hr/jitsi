{
  ContactGroupRssImpl newGroup=new ContactGroupRssImpl(groupName,parentProvider);
  ((ContactGroupRssImpl)parent).addSubgroup(newGroup);
  this.fireServerStoredGroupEvent(newGroup,ServerStoredGroupEvent.GROUP_CREATED_EVENT);
}
