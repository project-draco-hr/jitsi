{
  ContactGroupRssImpl parentGroup=(ContactGroupRssImpl)((ContactRssImpl)contact).getParentContactGroup();
  parentGroup.removeContact((ContactRssImpl)contact);
  fireSubscriptionEvent((ContactRssImpl)contact,((ContactRssImpl)contact).getParentContactGroup(),SubscriptionEvent.SUBSCRIPTION_REMOVED);
}
