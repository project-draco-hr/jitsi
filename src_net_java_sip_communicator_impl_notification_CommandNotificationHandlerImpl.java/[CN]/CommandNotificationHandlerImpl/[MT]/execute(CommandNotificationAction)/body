{
  if (StringUtils.isNullOrEmpty(action.getDescriptor(),true))   return;
  try {
    Runtime.getRuntime().exec(action.getDescriptor());
  }
 catch (  IOException e) {
    logger.error("Failed execute the following command: " + action.getDescriptor(),e);
  }
}
