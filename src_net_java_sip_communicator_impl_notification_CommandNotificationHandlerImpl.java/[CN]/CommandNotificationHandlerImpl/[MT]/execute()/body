{
  if (StringUtils.isNullOrEmpty(commandDescriptor,true))   return;
  try {
    Runtime.getRuntime().exec(commandDescriptor);
  }
 catch (  IOException e) {
    logger.error("Failed execute the following command: " + commandDescriptor,e);
  }
}
