{
  this.messenger=messenger;
  contactListModManager=new EventManager(msnProvider,(BasicMessenger)messenger);
  contactListModManager.addModificationListener(contactListModListenerImpl);
  messenger.addContactListListener(new ContactListListener());
}
