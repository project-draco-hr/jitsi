{
  for (int i=0; i < getRootGroup().countSubgroups(); i++) {
    ContactGroupMsnImpl gr=(ContactGroupMsnImpl)getRootGroup().getGroup(i);
    if (!gr.isPersistent())     return gr;
  }
  return null;
}
