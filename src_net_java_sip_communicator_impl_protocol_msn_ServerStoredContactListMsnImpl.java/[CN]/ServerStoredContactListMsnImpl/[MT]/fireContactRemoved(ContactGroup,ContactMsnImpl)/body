{
  if (parentOperationSet == null) {
    logger.debug("No presence op. set available. Bailing out.");
    return;
  }
  parentOperationSet.fireSubscriptionEvent(SubscriptionEvent.SUBSCRIPTION_REMOVED,contact,parentGroup);
}
