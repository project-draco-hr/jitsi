{
  Iterator contactGroups=rootGroup.subgroups();
  while (contactGroups.hasNext()) {
    ContactGroupMsnImpl contactGroup=(ContactGroupMsnImpl)contactGroups.next();
    if (contactGroup.getSourceGroup().getGroupId().equals(id))     return contactGroup;
  }
  return null;
}
