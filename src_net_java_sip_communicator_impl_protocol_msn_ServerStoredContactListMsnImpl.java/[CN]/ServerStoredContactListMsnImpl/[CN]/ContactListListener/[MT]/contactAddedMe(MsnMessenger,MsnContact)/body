{
  try {
    logger.trace("Contact add us " + contact);
    ContactMsnImpl contactImpl=findContactById(contact.getEmail().getEmailAddress());
    if (contactImpl == null)     addContact(contact.getEmail().getEmailAddress());
  }
 catch (  OperationFailedException ex) {
    logger.error("cannot add ",ex);
  }
}
