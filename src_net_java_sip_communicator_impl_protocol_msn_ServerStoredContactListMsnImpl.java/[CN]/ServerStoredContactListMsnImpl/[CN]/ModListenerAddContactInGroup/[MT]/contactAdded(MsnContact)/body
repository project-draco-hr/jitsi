{
synchronized (this) {
    if (contact.getEmail().getEmailAddress().equals(contactID)) {
      isAddRecieved=true;
      msnProvider.getMessenger().copyFriend(Email.parseStr(contactID),parent.getSourceGroup().getGroupId());
      notifyAll();
    }
  }
}
