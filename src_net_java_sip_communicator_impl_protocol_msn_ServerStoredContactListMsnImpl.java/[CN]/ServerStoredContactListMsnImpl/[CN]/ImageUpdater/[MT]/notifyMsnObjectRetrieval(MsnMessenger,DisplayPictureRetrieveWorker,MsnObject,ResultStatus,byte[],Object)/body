{
  if (result == ResultStatus.GOOD) {
    contact.setImage(resultBytes);
    parentOperationSet.fireContactPropertyChangeEvent(ContactPropertyChangeEvent.PROPERTY_IMAGE,contact,null,resultBytes);
  }
}
