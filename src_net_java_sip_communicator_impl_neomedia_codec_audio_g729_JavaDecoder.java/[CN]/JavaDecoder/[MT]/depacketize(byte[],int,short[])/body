{
  serial[0]=SYNC_WORD;
  serial[1]=SIZE_WORD;
  for (int s=0; s < L_FRAME; s++) {
    int input=inputFrame[inputFrameOffset + s / 8];
    input&=1 << (7 - (s % 8));
    serial[2 + s]=(0 != input) ? BIT_1 : BIT_0;
  }
}
