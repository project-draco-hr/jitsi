{
  try {
    logger.logEntry();
    bundleContext.registerService(ConfigurationService.class.getName(),impl,new java.util.Hashtable());
    logger.debug("Successfully registered " + getClass().getName());
  }
  finally {
    logger.logExit();
  }
}
