{
  if ((Math.abs(originX - mouseX) + Math.abs(originY - mouseY)) != 0) {
    doneDrawing=true;
    appendAndSend(new WhiteboardShapeLine(id(),spinModel.getNumber().intValue(),currentColor,new WhiteboardPoint(originX,originY),new WhiteboardPoint(mouseX,mouseY),s2w));
  }
}
