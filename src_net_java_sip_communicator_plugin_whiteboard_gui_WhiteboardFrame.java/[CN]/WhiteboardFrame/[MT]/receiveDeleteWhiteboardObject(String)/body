{
  if (logger.isDebugEnabled())   logger.debug("receiveDeleteWhiteboardObject");
  int i=0;
  while (i < displayList.size()) {
    WhiteboardShape wbs=displayList.get(i);
    if (id.equals(wbs.getID()))     displayList.remove(i);
 else     i++;
  }
  repaint();
  return;
}
