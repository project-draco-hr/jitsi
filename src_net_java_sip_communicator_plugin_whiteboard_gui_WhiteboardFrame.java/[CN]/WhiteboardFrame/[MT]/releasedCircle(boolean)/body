{
  doneDrawing=true;
  int r=originWidth / 2;
  int cx=drawX + r;
  int cy=drawY + r;
  appendAndSend(new WhiteboardShapeCircle(id(),spinModel.getNumber().intValue(),currentColor,new WhiteboardPoint(cx,cy),r,fill,s2w));
}
