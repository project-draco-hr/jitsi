{
  if (resourcesService == null) {
    ServiceReference serviceReference=ZeroconfActivator.bundleContext.getServiceReference(ResourceManagementService.class.getName());
    if (serviceReference == null)     return null;
    resourcesService=(ResourceManagementService)ZeroconfActivator.bundleContext.getService(serviceReference);
  }
  return resourcesService;
}
