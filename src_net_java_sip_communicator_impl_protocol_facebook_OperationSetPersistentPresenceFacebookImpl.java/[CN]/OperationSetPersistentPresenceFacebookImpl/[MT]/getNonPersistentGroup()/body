{
  for (int i=0; i < getServerStoredContactListRoot().countSubgroups(); i++) {
    ContactGroupFacebookImpl gr=(ContactGroupFacebookImpl)getServerStoredContactListRoot().getGroup(i);
    if (!gr.isPersistent())     return gr;
  }
  return null;
}
