{
  ContactGroupFacebookImpl newGroup=new ContactGroupFacebookImpl(groupName,parentProvider);
  ((ContactGroupFacebookImpl)parent).addSubgroup(newGroup);
  this.fireServerStoredGroupEvent(newGroup,ServerStoredGroupEvent.GROUP_CREATED_EVENT);
}
