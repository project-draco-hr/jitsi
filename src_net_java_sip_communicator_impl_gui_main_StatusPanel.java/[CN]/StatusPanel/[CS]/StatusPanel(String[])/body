{
  this.setLayout(new FlowLayout(FlowLayout.LEFT));
  this.setBorder(BorderFactory.createMatteBorder(1,0,0,0,LookAndFeelConstants.CONTACTPANEL_MOVER_START_COLOR));
  this.userProtocols=userProtocols;
  this.init();
}
