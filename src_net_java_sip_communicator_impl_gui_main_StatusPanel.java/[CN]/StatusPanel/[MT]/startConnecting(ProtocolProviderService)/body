{
  StatusSelectorBox selectorBox=(StatusSelectorBox)protocolStatusCombos.get(protocolProvider);
  selectorBox.startConnecting(Constants.getProtocolAnimatedIcon(protocolProvider.getProtocolName()));
  selectorBox.repaint();
}
