{
  this.presence=presence;
  Enumeration statusCombos=this.getProtocolStatusCombos().elements();
  while (statusCombos.hasMoreElements()) {
    ((StatusSelectorBox)statusCombos.nextElement()).setPresence(presence);
  }
}
