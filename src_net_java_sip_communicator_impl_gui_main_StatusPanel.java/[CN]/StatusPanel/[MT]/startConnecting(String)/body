{
  StatusSelectorBox selectorBox=(StatusSelectorBox)protocolStatusCombos.get(protocol);
  selectorBox.startConnecting(ImageLoader.getAnimatedImage(ImageLoader.ICQ_CONNECTING));
  selectorBox.repaint();
}
