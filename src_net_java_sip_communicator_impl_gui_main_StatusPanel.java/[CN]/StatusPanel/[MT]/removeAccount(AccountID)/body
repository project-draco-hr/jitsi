{
  StatusSelectorBox protocolStatusCombo=(StatusSelectorBox)this.protocolStatusCombos.get(accountID);
  this.protocolStatusCombos.remove(accountID);
  this.remove(protocolStatusCombo);
  this.revalidate();
  this.repaint();
}
