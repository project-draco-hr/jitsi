{
  StatusSelectorBox protocolStatusCombo=new StatusSelectorBox(this.mainFrame,protocolProvider);
  this.protocolStatusCombos.put(protocolProvider.getAccountID(),protocolStatusCombo);
  this.add(protocolStatusCombo);
  this.getParent().validate();
}
