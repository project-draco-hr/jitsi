{
  if (list == null || list.size() == 0)   return null;
  Iterator iter=list.iterator();
  while (iter.hasNext()) {
    StoredProtoContactDescriptor desc=(StoredProtoContactDescriptor)iter.next();
    if (desc.contactAddress.equals(contactAddress))     return desc;
  }
  return null;
}
