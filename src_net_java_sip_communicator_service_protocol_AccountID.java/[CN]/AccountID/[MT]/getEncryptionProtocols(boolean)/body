{
  String encryptionProtocols;
  if (enabled) {
    encryptionProtocols=getAccountPropertyString(ProtocolProviderFactory.ENABLED_ENCRYPTION_PROTOCOLS);
    if (encryptionProtocols == null) {
      ArrayList<String> result=new ArrayList<String>(1);
      result.add("ZRTP");
      return result;
    }
  }
 else {
    encryptionProtocols=getAccountPropertyString(ProtocolProviderFactory.DISABLED_ENCRYPTION_PROTOCOLS);
    if (encryptionProtocols == null) {
      return new ArrayList<String>(0);
    }
  }
  String[] tmp=encryptionProtocols.split(" ");
  ArrayList<String> encryptionProtocolList=new ArrayList<String>(tmp.length);
  for (int i=0; i < tmp.length; ++i) {
    encryptionProtocolList.add(tmp[i]);
  }
  return encryptionProtocolList;
}
