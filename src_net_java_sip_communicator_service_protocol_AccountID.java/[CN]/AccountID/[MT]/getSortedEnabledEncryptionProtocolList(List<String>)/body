{
  List<String> enabledEncryptionProtocolList=this.getEncryptionProtocols(true);
  ArrayList<String> result=new ArrayList<String>(enabledEncryptionProtocolList.size());
  for (int i=0; i < enabledEncryptionProtocolList.size(); ++i) {
    if (encryptionProtocolList.contains(enabledEncryptionProtocolList.get(i))) {
      result.add(enabledEncryptionProtocolList.get(i));
    }
  }
  return result;
}
