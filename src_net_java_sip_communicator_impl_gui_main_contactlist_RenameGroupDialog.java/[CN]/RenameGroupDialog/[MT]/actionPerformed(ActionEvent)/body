{
  JButton button=(JButton)e.getSource();
  String name=button.getName();
  if (name.equals("rename")) {
    if (metaGroup != null) {
      this.clist.renameMetaContactGroup(metaGroup,renameGroupPanel.getNewName());
    }
    this.dispose();
  }
 else {
    this.dispose();
  }
}
