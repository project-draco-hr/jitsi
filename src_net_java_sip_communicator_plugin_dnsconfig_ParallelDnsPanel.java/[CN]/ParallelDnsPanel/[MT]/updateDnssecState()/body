{
  boolean isDnssec=configService.getBoolean(DnsUtilActivator.PNAME_DNSSEC_RESOLVER_ENABLED,DnsUtilActivator.PDEFAULT_DNSSEC_RESOLVER_ENABLED);
  if (isDnssec)   chkBackupDnsEnabled.setSelected(false);
  chkBackupDnsEnabled.setEnabled(!isDnssec);
  updateButtonsState();
}
