{
  if (e.getSource() == enableDefaultEncryption || e.getSource() == enableSDesAttribute) {
    loadStates();
  }
  if (e.getSource() == cmdExpandAdvancedSettings) {
    pnlAdvancedSettings.setVisible(!pnlAdvancedSettings.isVisible());
  }
}
