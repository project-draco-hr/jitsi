{
  for (int i=0; i < getRootGroup().countSubgroups(); i++) {
    ContactGroupIcqImpl gr=(ContactGroupIcqImpl)getRootGroup().getGroup(i);
    if (!gr.isPersistent() && !gr.getGroupName().equals(awaitingAuthorizationGroupName))     return gr;
  }
  return null;
}
