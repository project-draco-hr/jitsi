{
  if (address != null && address.length() > 0) {
    int idx=address.indexOf(':',5);
    if (idx > -1)     address=address.substring(0,idx);
    idx=address.indexOf(';');
    if (idx > -1)     address=address.substring(0,idx);
  }
  return address;
}
