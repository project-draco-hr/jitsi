{
synchronized (sync) {
    if (instance == null) {
      long ptr=init();
      if (ptr != 0) {
        instance=new DSManager(ptr);
      }
    }
    if (instance != null) {
      ref++;
    }
  }
  return instance;
}
