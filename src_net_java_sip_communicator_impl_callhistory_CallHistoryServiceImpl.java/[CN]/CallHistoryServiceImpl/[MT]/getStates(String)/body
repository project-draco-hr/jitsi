{
  List<CallParticipantState> result=new LinkedList<CallParticipantState>();
  Collection<String> stateStrs=getCSVs(str);
  for (  String item : stateStrs) {
    result.add(convertStateStringToState(item));
  }
  return result;
}
