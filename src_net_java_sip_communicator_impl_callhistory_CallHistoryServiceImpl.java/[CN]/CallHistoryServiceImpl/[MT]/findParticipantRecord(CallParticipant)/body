{
  CallRecord record=findCallRecord(callParticipant.getCall());
  if (record == null)   return null;
  return (CallParticipantRecordImpl)record.findParticipantRecord(callParticipant.getAddress());
}
