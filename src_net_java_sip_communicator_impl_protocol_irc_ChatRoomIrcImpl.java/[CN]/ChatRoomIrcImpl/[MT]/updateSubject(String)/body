{
  if (!this.chatSubject.equals(subject)) {
    this.chatSubject=subject;
    ChatRoomPropertyChangeEvent topicChangeEvent=new ChatRoomPropertyChangeEvent(this,ChatRoomPropertyChangeEvent.CHAT_ROOM_SUBJECT,"",subject);
    firePropertyChangeEvent(topicChangeEvent);
  }
}
