{
  assertConnected();
  if (((MessageIrcImpl)message).isCommand()) {
    parentProvider.getIrcStack().sendCommand(this,message);
  }
 else {
    parentProvider.getIrcStack().sendMessage(chatRoomName,message.getContent());
  }
  this.fireMessageDeliveredEvent(message);
}
