{
  final int prime=31;
  int result=1;
  result=prime * result + ((chatRoomName == null) ? 0 : chatRoomName.hashCode());
  result=prime * result + ((parentProvider == null) ? 0 : parentProvider.hashCode());
  return result;
}
