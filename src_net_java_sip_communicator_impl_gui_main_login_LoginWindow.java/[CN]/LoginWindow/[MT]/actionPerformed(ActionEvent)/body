{
  JButton button=(JButton)e.getSource();
  String buttonName=button.getName();
  if (buttonName.equals("login")) {
    this.dispose();
    ProtocolProviderService pps=this.loginManager.installAccount(providerFactory,uinComboBox.getSelectedItem().toString(),new String(passwdField.getPassword()));
  }
 else {
    this.dispose();
  }
}
