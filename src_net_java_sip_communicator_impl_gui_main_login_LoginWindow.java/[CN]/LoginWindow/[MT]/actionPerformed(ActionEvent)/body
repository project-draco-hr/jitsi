{
  JButton button=(JButton)e.getSource();
  String buttonName=button.getName();
  if (buttonName.equals("login")) {
    this.dispose();
    this.loginManager.login(accountManager,uinComboBox.getSelectedItem().toString(),"");
  }
 else {
    this.dispose();
  }
}
