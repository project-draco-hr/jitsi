{
  JButton button=(JButton)e.getSource();
  String buttonName=button.getName();
  if (buttonName.equals("login")) {
    this.loginManager.login(uinTextField.getText(),new String(passwdField.getPassword()));
    this.dispose();
  }
 else {
    this.dispose();
  }
}
