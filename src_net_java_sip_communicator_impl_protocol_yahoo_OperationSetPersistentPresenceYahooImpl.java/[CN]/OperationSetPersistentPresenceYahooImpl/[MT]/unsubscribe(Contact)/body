{
  assertConnected();
  if (!(contact instanceof ContactYahooImpl))   throw new IllegalArgumentException("Argument is not an yahoo contact (contact=" + contact + ")");
  ssContactList.removeContact((ContactYahooImpl)contact);
}
