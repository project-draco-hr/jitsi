{
  assertConnected();
  if (!(group instanceof ContactGroupYahooImpl))   throw new IllegalArgumentException("The specified group is not an yahoo contact group: " + group);
  ssContactList.removeGroup(((ContactGroupYahooImpl)group));
}
