{
  String ext=getExtension(f);
  if (ext != null) {
    return ext.equals(wav) || ext.equals(mid) || ext.equals(mp2)|| ext.equals(mp3)|| ext.equals(mod)|| ext.equals(ogg)|| ext.equals(wma)|| ext.equals(gsm)|| ext.equals(au)|| ext.equals(ram);
  }
  return false;
}
