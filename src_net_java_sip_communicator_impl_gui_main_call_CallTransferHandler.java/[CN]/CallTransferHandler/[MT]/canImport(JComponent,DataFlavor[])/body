{
  for (int i=0, n=flavor.length; i < n; i++) {
    if (flavor[i].equals(DataFlavor.stringFlavor) || flavor[i].equals(uiContactDataFlavor)) {
      if (comp instanceof JPanel) {
        return true;
      }
      return false;
    }
  }
  return false;
}
