{
  if (groupCombo.getSelectedItem() != null || groupCombo.getSelectedItem() != "") {
    parentWizard.setNextFinishButtonEnabled(true);
  }
 else {
    parentWizard.setNextFinishButtonEnabled(false);
  }
}
