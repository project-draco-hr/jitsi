{
  char[] stringAsCharArray=s.toCharArray();
  boolean digitsOnly=true;
  for (  char character : stringAsCharArray) {
    if (character != ' ' && character != '(' && character != ')' && character != '-' && DIGIT_MAPPINGS.get(Character.toUpperCase(character)) == null)     digitsOnly=false;
  }
  return digitsOnly;
}
