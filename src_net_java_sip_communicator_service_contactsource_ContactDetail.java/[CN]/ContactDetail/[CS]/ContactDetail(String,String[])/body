{
  this.contactAddress=contactAddress;
  if (labels != null) {
    for (    String label : labels) {
      if ((label != null) && !this.labels.contains(label))       this.labels.add(label);
    }
  }
}
