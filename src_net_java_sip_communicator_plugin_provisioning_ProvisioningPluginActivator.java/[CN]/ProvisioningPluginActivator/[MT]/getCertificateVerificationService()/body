{
  if (certVerification == null) {
    ServiceReference certVerifyReference=bundleContext.getServiceReference(CertificateVerificationService.class.getName());
    if (certVerifyReference != null)     certVerification=(CertificateVerificationService)bundleContext.getService(certVerifyReference);
  }
  return certVerification;
}
