{
  super.paintComponent(g);
  if (bgImage != null) {
    g=g.create();
    try {
      AntialiasingManager.activateAntialiasing(g);
      g.drawImage(bgImage,30,30,null);
    }
  finally {
      g.dispose();
    }
  }
}
