{
  int M=Ld8k.M;
  int NC=Ld8k.NC;
  int j;
  float diff, tmp;
  for (j=NC; j < M; j++) {
    diff=buf[j - 1] - buf[j];
    tmp=(diff + gap) * 0.5f;
    if (tmp > 0) {
      buf[j - 1]-=tmp;
      buf[j]+=tmp;
    }
  }
}
