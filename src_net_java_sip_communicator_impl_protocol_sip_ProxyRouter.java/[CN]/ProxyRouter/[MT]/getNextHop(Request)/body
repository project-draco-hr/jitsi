{
  logger.debug("Trying to get a router for req: " + request);
  Router router=this.getRouterFor(request);
  if (router != null)   return router.getNextHop(request);
  logger.warn("Hmm we are returning a null router. This doesn't look good",new Exception());
  return null;
}
