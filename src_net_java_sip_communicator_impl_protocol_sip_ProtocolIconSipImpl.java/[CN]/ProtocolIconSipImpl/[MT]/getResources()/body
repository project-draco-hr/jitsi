{
  if (resourcesService == null) {
    ServiceReference serviceReference=SipActivator.bundleContext.getServiceReference(ResourceManagementService.class.getName());
    if (serviceReference == null)     return null;
    resourcesService=(ResourceManagementService)SipActivator.bundleContext.getService(serviceReference);
  }
  return resourcesService;
}
