{
  boolean conferenceFocus;
  if (calls.length < 1)   conferenceFocus=false;
 else   if (calls.length > 1)   conferenceFocus=true;
 else   conferenceFocus=(calls[0].getCallPeerCount() > 1);
  return conferenceFocus;
}
