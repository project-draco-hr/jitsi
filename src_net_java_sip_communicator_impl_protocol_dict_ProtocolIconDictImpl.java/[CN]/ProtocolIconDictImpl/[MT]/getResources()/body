{
  if (resourcesService == null) {
    ServiceReference serviceReference=DictActivator.bundleContext.getServiceReference(ResourceManagementService.class.getName());
    if (serviceReference == null)     return null;
    resourcesService=(ResourceManagementService)DictActivator.bundleContext.getService(serviceReference);
  }
  return resourcesService;
}
