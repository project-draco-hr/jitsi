{
  if (resourcesService == null) {
    ServiceReference serviceReference=AccountInfoActivator.bundleContext.getServiceReference(ResourceManagementService.class.getName());
    if (serviceReference == null)     return null;
    resourcesService=(ResourceManagementService)AccountInfoActivator.bundleContext.getService(serviceReference);
  }
  return resourcesService;
}
