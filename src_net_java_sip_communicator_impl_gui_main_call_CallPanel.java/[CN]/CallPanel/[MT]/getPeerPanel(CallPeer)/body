{
  for (  Map.Entry<CallPeer,CallPeerPanel> peerEntry : peersPanels.entrySet()) {
    CallPeer entryPeer=peerEntry.getKey();
    if ((entryPeer != null) && entryPeer.equals(peer)) {
      return peerEntry.getValue();
    }
  }
  return null;
}
