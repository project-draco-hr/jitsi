{
  Iterator participants=participantsPanels.entrySet().iterator();
  while (participants.hasNext()) {
    Map.Entry participantEntry=(Map.Entry)participants.next();
    CallParticipantWrapper participantWrapper=(CallParticipantWrapper)participantEntry.getKey();
    if (participantWrapper.getCallParticipant().equals(participant))     return participantWrapper;
  }
  return null;
}
