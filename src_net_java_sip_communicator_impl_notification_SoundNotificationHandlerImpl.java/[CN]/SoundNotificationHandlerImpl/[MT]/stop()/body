{
  AudioNotifierService audioNotifService=NotificationActivator.getAudioNotifier();
  if (audioNotifService == null || audio == null)   return;
  audio.stop();
  audioNotifService.destroyAudio(audio);
  audio=null;
}
