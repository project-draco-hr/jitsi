{
  setPreferredSize(new Dimension(44,38));
  if (isFullScreen)   setIconImage(ImageLoader.getImage(ImageLoader.EXIT_FULL_SCREEN_BUTTON));
 else   setIconImage(ImageLoader.getImage(ImageLoader.ENTER_FULL_SCREEN_BUTTON));
}
