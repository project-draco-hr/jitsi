{
  String encoding=getEncodings()[rowIndex];
switch (columnIndex) {
case 0:
    return Boolean.valueOf(encodingConfiguration.getPriority(encoding) > 0);
case 1:
  return MediaUtils.sdpToJmfEncoding(encoding);
default :
return null;
}
}
