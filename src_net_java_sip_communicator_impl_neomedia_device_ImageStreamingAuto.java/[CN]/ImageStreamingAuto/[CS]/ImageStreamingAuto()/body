{
  String name="DesktopStreaming";
  CaptureDeviceInfo devInfo=new CaptureDeviceInfo(name,new MediaLocator(ImageStreamingUtils.LOCATOR_PREFIX + name),DataSource.getFormats());
  CaptureDeviceManager.addDevice(devInfo);
}
