{
  if (task == this) {
    task=null;
  }
synchronized (JmDNS.this) {
    for (Iterator<ServiceInfo> i=services.values().iterator(); i.hasNext(); ) {
      ServiceInfo info=i.next();
      if (info.task == this) {
        info.task=null;
      }
    }
  }
  return super.cancel();
}
