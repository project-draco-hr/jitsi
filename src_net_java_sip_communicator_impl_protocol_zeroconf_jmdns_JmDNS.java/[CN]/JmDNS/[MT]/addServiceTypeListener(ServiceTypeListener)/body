{
synchronized (this) {
    typeListeners.remove(listener);
    typeListeners.add(listener);
  }
  for (  String serviceType : serviceTypes.values()) {
    listener.serviceTypeAdded(new ServiceEvent(this,serviceType,null,null));
  }
  new TypeResolver().start();
}
