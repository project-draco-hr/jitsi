{
  String path=findString(urlKey,imageResourceBundle);
  if (path == null || path.length() == 0) {
    logger.warn("Missing resource for key: " + urlKey + " / "+ imageResourceBundle);
    return null;
  }
  return getImageURLForPath(path);
}
