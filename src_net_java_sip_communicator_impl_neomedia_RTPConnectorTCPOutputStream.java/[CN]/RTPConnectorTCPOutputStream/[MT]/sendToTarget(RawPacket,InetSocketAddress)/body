{
  int len=packet.getLength();
  int off=packet.getOffset();
  data[0]=(byte)((len >> 8) & 0xff);
  data[1]=(byte)(len & 0xff);
  System.arraycopy(packet.getBuffer(),off,data,2,len);
  socket.getOutputStream().write(data,0,len + 2);
}
