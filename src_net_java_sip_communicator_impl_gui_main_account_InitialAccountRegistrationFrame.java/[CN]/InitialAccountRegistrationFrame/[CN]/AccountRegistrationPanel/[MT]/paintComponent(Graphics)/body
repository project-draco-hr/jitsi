{
  AntialiasingManager.activateAntialiasing(g);
  super.paintComponent(g);
  g.setColor(new Color(ColorProperties.getColor("desktopBackgroundColor")));
  g.fillRoundRect(0,0,getWidth(),getHeight(),15,15);
}
