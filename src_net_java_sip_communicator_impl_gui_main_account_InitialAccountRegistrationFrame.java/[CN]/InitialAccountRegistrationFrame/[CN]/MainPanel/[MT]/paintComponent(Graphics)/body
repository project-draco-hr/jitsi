{
  AntialiasingManager.activateAntialiasing(g);
  super.paintComponent(g);
  g.setColor(new Color(ColorProperties.getColor("accountRegistrationBackground")));
  g.fillRoundRect(10,10,getWidth() - 20,getHeight() - 20,15,15);
}
