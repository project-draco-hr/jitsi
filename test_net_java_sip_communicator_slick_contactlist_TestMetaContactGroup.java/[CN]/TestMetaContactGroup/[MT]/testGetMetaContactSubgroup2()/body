{
  MetaContactGroup actualGroup=metaGroup.getMetaContactSubgroup((metaGroup.getSubgroups().next()).getGroupName());
  assertNotNull("getMetaContact(String) returned null for group 0",actualGroup);
}
