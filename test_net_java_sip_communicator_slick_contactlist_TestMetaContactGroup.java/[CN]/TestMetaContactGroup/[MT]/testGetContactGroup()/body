{
  ContactGroup actualReturn=metaGroup.getContactGroup(mockGroup.getGroupName(),MclSlickFixture.mockProvider);
  assertNotNull("getContactGroup() return null.",actualReturn);
  assertSame("getContactGroup() did not return the right proto group.",actualReturn,mockGroup);
}
