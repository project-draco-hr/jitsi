{
  MetaContactGroup parentGroup=metaGroup.getParentMetaContactGroup();
  return GuiActivator.getContactListService().getSourceIndex() * MAX_GROUPS + ((parentGroup == null) ? 0 : (parentGroup.indexOf(metaGroup) + 1)) * MAX_CONTACTS;
}
