{
  account.setEnabled(isEnable);
  AccountID accountID=account.getAccountID();
  if (isEnable)   GuiActivator.getAccountManager().loadAccount(accountID);
 else   GuiActivator.getAccountManager().unloadAccount(accountID);
}
