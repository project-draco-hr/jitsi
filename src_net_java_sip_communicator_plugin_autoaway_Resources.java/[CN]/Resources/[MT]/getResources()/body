{
  if (resourcesService == null) {
    ServiceReference serviceReference=AutoAwayActivator.bundleContext.getServiceReference(ResourceManagementService.class.getName());
    if (serviceReference == null)     return null;
    resourcesService=(ResourceManagementService)AutoAwayActivator.bundleContext.getService(serviceReference);
  }
  return resourcesService;
}
