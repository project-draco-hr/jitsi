{
  super(parent,title,fileOperation);
  if (fileOperation != SipCommFileChooser.LOAD_FILE_OPERATION || fileOperation != SipCommFileChooser.SAVE_FILE_OPERATION) {
    try {
      throw new Exception("UnknownFileOperation");
    }
 catch (    Exception e) {
      e.printStackTrace();
    }
  }
}
