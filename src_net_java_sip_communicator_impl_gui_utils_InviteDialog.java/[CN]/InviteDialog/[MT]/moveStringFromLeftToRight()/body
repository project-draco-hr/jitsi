{
  String newContactText=newContactField.getText();
  if (newContactText != null && newContactText.length() > 0)   selectedContactListModel.addElement(newContactField.getText());
  newContactField.setText("");
}
