{
  if (resourcesService == null) {
    ServiceReference serviceReference=WhiteboardActivator.bundleContext.getServiceReference(ResourceManagementService.class.getName());
    if (serviceReference == null)     return null;
    resourcesService=(ResourceManagementService)WhiteboardActivator.bundleContext.getService(serviceReference);
  }
  return resourcesService;
}
