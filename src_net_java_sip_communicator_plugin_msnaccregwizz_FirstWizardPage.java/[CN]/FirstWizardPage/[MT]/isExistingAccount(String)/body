{
  ProtocolProviderFactory factory=MsnAccRegWizzActivator.getMsnProtocolProviderFactory();
  for (  AccountID accountID : factory.getRegisteredAccounts()) {
    if (accountName.equalsIgnoreCase(accountID.getUserID()))     return true;
  }
  return false;
}
