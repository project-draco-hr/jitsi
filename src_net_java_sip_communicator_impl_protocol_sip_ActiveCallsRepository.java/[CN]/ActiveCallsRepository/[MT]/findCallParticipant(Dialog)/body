{
  Iterator activeCalls=getActiveCalls();
  while (activeCalls.hasNext()) {
    CallSipImpl call=(CallSipImpl)activeCalls.next();
    CallParticipantSipImpl callParticipant=call.findCallParticipant(dialog);
    if (callParticipant != null)     return callParticipant;
  }
  return null;
}
