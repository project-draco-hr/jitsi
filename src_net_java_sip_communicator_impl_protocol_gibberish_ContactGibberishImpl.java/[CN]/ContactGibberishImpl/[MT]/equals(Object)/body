{
  if (obj == null || !(obj instanceof ContactGibberishImpl))   return false;
  ContactGibberishImpl gibberishContact=(ContactGibberishImpl)obj;
  return this.getAddress().equals(gibberishContact.getAddress());
}
