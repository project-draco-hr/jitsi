{
  super(Resources.getString("contactDetails"));
  this.metaContact=metaContact;
  this.setTitle(Resources.getString("contactDetails") + ": " + metaContact.getDisplayName());
  Iterator<Contact> subContacts=metaContact.getContacts();
  this.contactPanel=new ContactInfoContactPanel(subContacts,this);
  this.getContentPane().add(contactPanel,BorderLayout.WEST);
  this.getContentPane().add(detailsPanel,BorderLayout.CENTER);
  this.pack();
}
