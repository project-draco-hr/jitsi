{
  try {
    if (!freed) {
      long ptr=getPtr();
      long bufferPtr=FFmpeg.avpicture_get_data0(ptr);
      if (bufferPtr != 0)       freeData0(bufferPtr);
      FFmpeg.av_free(ptr);
      freed=true;
    }
  }
  finally {
    super.finalize();
  }
}
