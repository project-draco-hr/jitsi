{
  Object source=evt.getSource();
  String itemName;
  if (source instanceof Component)   itemName=((Component)source).getName();
 else   itemName=((MenuComponent)source).getName();
  if (itemName.equals("online")) {
    if (!this.provider.isRegistered()) {
      new ProviderRegistration(provider).start();
    }
  }
 else {
    RegistrationState registrationState=provider.getRegistrationState();
    if (!RegistrationState.UNREGISTERED.equals(registrationState) && !RegistrationState.UNREGISTERING.equals(registrationState)) {
      new ProviderUnRegistration(this.provider).start();
    }
  }
}
