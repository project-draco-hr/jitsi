{
  if (currentFilter == null || !currentFilter.equals(filter))   this.currentFilter=filter;
  treeModel.clear();
  addAllMatching();
  if (treeModel.getChildCount(treeModel.getRoot()) > 0)   return true;
  return false;
}
