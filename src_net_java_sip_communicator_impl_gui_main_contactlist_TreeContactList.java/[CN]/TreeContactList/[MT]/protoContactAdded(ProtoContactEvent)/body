{
  MetaContact parent=evt.getNewParent();
synchronized (tempTreeModel) {
    ContactNode contactNode=treeModel.findContactNodeByMetaContact(parent);
    if (contactNode == null && currentFilter.isMatching(parent)) {
      addContact(parent);
    }
  }
}
