{
  final MetaContactGroup metaGroup=evt.getSourceMetaContactGroup();
  UIGroup uiGroup=MetaContactListSource.createUIGroup(metaGroup);
  if (currentFilter.isMatching(uiGroup))   addGroup(uiGroup,true);
 else   MetaContactListSource.removeUIGroup(metaGroup);
}
