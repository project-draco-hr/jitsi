{
  final MetaContactGroup metaGroup=evt.getSourceMetaContactGroup();
  SwingUtilities.invokeLater(new Runnable(){
    public void run(){
synchronized (tempTreeModel) {
        if (currentFilter.isMatching(metaGroup))         addGroup(metaGroup);
      }
    }
  }
);
}
