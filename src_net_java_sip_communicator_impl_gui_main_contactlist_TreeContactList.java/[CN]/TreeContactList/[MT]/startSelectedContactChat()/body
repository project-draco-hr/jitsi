{
  TreePath selectionPath=getSelectionPath();
  if (selectionPath != null && selectionPath.getLastPathComponent() instanceof ContactNode) {
    ContactNode contactNode=(ContactNode)selectionPath.getLastPathComponent();
    GuiActivator.getUIService().getChatWindowManager().startChat(contactNode.getMetaContact());
  }
}
