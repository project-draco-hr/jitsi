{
  GroupNode parentNode=treeModel.getRoot();
  GroupNode groupNode=parentNode.sortedAddContactGroup(group,isRefreshView);
  expandPath(new TreePath(treeModel.getRoot().getPath()));
  return groupNode;
}
