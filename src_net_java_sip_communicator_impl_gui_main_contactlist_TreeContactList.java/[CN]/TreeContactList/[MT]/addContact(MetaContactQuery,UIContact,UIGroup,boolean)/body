{
  if (!SwingUtilities.isEventDispatchThread()) {
    LowPriorityEventQueue.invokeLater(new Runnable(){
      public void run(){
        if (query != null && !query.isCanceled())         addContact(contact,group,isSorted,true);
      }
    }
);
    return;
  }
}
