{
  ContactNode contactNode=treeModel.findContactNodeByMetaContact(metaContact);
  if (contactNode != null)   contactNode.setActive(isActive);
  if (isActive) {
    activeContacts.add(metaContact);
  }
 else   activeContacts.remove(metaContact);
}
