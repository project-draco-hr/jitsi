{
  SwingUtilities.invokeLater(new Runnable(){
    public void run(){
synchronized (filterLock) {
        UIGroup uiGroup=MetaContactListSource.getUIGroup(evt.getSourceMetaContactGroup());
        if (uiGroup != null)         removeGroup(uiGroup);
      }
    }
  }
);
}
