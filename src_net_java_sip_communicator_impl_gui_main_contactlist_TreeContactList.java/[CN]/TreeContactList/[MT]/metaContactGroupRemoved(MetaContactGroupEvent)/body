{
  final UIGroup uiGroup=MetaContactListSource.getUIGroup(evt.getSourceMetaContactGroup());
  SwingUtilities.invokeLater(new Runnable(){
    public void run(){
synchronized (tempTreeModel) {
        removeGroup(uiGroup);
      }
    }
  }
);
}
