{
  this.defaultFilter=filter;
  if (defaultFilter.equals(presenceFilter))   TreeContactList.searchFilter.setSearchSourceType(SearchFilter.DEFAULT_SOURCE);
 else   if (defaultFilter.equals(historyFilter))   TreeContactList.searchFilter.setSearchSourceType(SearchFilter.HISTORY_SOURCE);
}
