{
  int tokenStart=prevTokenEnd;
  if (!this.returnDelim) {
    int textLen=this.text.length();
    while (tokenStart < textLen && this.delims.indexOf(this.text.charAt(tokenStart)) >= 0) {
      tokenStart++;
    }
  }
  return tokenStart;
}
