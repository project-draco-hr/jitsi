{
  String name=candidate.getParentComponent().getParentStream().getName();
  if (candidate.getParentComponent().getComponentID() == Component.RTP) {
    name="rtp";
  }
 else {
    name="rtcp";
  }
  return GTalkPacketFactory.createCandidate(candidate,name);
}
