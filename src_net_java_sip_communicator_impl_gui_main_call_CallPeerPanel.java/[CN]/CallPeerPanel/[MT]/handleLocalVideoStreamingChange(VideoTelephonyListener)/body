{
synchronized (videoContainers) {
    if (videoTelephony == null)     return;
    if (videoTelephony.isLocalVideoStreaming(callParticipant.getCall())) {
      try {
        videoTelephony.createLocalVisualComponent(callParticipant,listener);
      }
 catch (      OperationFailedException ex) {
        logger.error("Failed to create local video/visual Component.",ex);
      }
    }
 else     if (localVideo != null) {
      videoTelephony.disposeLocalVisualComponent(callParticipant,localVideo);
      localVideo=null;
    }
  }
}
