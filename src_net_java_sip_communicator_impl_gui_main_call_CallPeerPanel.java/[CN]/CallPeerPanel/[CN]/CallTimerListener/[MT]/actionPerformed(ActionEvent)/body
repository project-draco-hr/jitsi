{
  Date time=GuiUtils.substractDates(new Date(System.currentTimeMillis()),new Date(callParticipant.getCallDurationStartTime()));
  callDuration.setTime(time.getTime());
  timeLabel.setText(GuiUtils.formatTime(time));
}
