{
  byte[] image=new byte[100000];
  String path=Resources.getString(key);
  try {
    Resources.class.getClassLoader().getResourceAsStream(path).read(image);
  }
 catch (  IOException e) {
    log.error("Failed to load image:" + key,e);
  }
  return image;
}
