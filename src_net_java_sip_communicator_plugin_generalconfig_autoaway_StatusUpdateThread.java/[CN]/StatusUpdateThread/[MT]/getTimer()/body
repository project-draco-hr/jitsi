{
  ConfigurationService configService=GeneralConfigPluginActivator.getConfigurationService();
  return configService.getBoolean(Preferences.ENABLE,false) ? configService.getInt(Preferences.TIMER,0) : 0;
}
