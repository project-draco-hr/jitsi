{
  if (inCheckForUpdates == 0)   throw new IllegalStateException("inCheckForUpdates");
 else {
    inCheckForUpdates--;
    if ((checkForUpdatesDialog != null) && (Update.checkForUpdatesDialog == checkForUpdatesDialog))     Update.checkForUpdatesDialog=null;
  }
}
