{
  ContactSipImpl volatileContact=(ContactSipImpl)opSetPersPresence.findContactByID(to);
  if (volatileContact == null)   volatileContact=opSetPersPresence.createVolatileContact(to);
  sendSmsMessage(volatileContact,message);
}
