{
  if (opened)   return;
  long avcodec=FFmpeg.avcodec_find_decoder(FFmpeg.CODEC_ID_H263);
  avcontext=FFmpeg.avcodec_alloc_context();
  FFmpeg.avcodeccontext_set_workaround_bugs(avcontext,FFmpeg.FF_BUG_AUTODETECT);
  if (FFmpeg.avcodec_open(avcontext,avcodec) < 0)   throw new RuntimeException("Could not open codec CODEC_ID_H263");
  avframe=FFmpeg.avcodec_alloc_frame();
  opened=true;
  super.open();
}
