{
  int M=Ld8k.M;
  int i;
  float[] lsp=new float[M];
  for (i=0; i < M; i++)   lsp[i]=lsp_old[i] * 0.5f + lsp_new[i] * 0.5f;
  lsp_az(lsp,az,0);
  lsp_lsf(lsp,lsf_int,M);
  lsp_lsf(lsp_new,lsf_new,M);
}
