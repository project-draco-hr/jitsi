{
  serviceImpl=createServiceImpl();
  if (logger.isInfoEnabled())   logger.info(serviceName + " STARTED");
  bundleContext.registerService(serviceClass.getName(),serviceImpl,null);
  if (logger.isInfoEnabled())   logger.info(serviceName + " REGISTERED");
}
