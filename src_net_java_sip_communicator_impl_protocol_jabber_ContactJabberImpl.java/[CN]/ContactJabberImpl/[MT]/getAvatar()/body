{
  try {
    VCard card=new VCard();
    card.load(ssclCallback.getParentProvider().getConnection(),getAddress());
    return card.getAvatar();
  }
 catch (  Exception e) {
    logger.error("Cannot load image for contact " + this + " : "+ e.getMessage());
  }
  return null;
}
