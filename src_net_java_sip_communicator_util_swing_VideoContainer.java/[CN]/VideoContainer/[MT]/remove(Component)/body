{
  super.remove(removedComp);
  if (((VideoLayout)getLayout()).getComponentConstraints(removedComp).equals(VideoLayout.CENTER_REMOTE) && !removedComp.equals(noVideoComponent)) {
    add(noVideoComponent,VideoLayout.CENTER_REMOTE);
    validate();
  }
}
