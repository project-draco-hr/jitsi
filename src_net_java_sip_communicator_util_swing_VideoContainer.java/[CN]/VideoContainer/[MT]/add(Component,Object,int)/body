{
  if (VideoLayout.CENTER_REMOTE.equals(constraints) && (noVideoComponent != null) && !noVideoComponent.equals(addedComp)) {
    remove(noVideoComponent);
    validate();
  }
  super.add(addedComp,constraints,index);
}
