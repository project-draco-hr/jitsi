{
  if (constraints.equals(VideoLayout.CENTER_REMOTE) && noVideoComponent != null && !addedComp.equals(noVideoComponent)) {
    remove(noVideoComponent);
    revalidate();
  }
  super.add(addedComp,constraints,index);
}
