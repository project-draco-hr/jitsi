{
  super.removeAll();
  if ((canvas != null) && (canvas.getParent() != this))   canvas=null;
  if (noVideoComponent != null) {
    add(noVideoComponent,VideoLayout.CENTER_REMOTE);
    validate();
  }
}
