{
  ServiceReference ref=context.getServiceReference(SystemActivityNotificationsService.class.getName());
  if (ref == null)   return null;
 else   return (SystemActivityNotificationsService)context.getService(ref);
}
