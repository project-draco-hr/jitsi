{
  if (!started)   return;
  PortAudio.Pa_CloseStream(getStream());
synchronized (this) {
    stream=0;
    bufferLeft=null;
    started=false;
    PortAudioManager.getInstance().stoppedOutputPortAudioStream(this);
  }
}
