{
  java.net.URL outputURL=CustomLanguagePackImpl.class.getProtectionDomain().getCodeSource().getLocation();
  String outputString=outputURL.toString();
  String[] parseString;
  parseString=outputString.split("file:");
  String jarFilename=parseString[1];
  return jarFilename;
}
