{
  if (!e.getValueIsAdjusting()) {
    CustomTableModel newModel=(CustomTableModel)languagesTable.get(languageList.getSelectedValue());
    stringsTable.setModel(newModel);
    stringsTable.getColumnModel().getColumn(1).setCellRenderer(new TextAreaCellRenderer());
    stringsTable.repaint();
  }
}
