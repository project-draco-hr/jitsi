{
  super(image.getWidth(null) + rightShift,image.getHeight(null) + rightShift,BufferedImage.TYPE_4BYTE_ABGR);
  this.bgImage=image;
  this.getGraphics().drawImage(this.bgImage,0,rightShift / 2,null);
}
