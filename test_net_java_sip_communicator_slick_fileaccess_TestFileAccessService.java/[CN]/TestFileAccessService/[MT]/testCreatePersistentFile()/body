{
  try {
    File file=this.fileAccessService.getPrivatePersistentFile(fileName);
    if (!file.exists()) {
      assertTrue(file.createNewFile());
    }
  }
 catch (  Exception e) {
    fail(e.getMessage());
  }
}
