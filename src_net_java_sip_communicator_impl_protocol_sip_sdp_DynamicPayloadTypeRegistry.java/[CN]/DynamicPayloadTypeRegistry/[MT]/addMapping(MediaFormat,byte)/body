{
  MediaFormat alreadyMappedFmt=findFormat(payloadType);
  if (alreadyMappedFmt != null) {
    throw new IllegalArgumentException(payloadType + " has already been allocated to " + alreadyMappedFmt);
  }
  if (payloadType < MIN_DYNAMIC_PAYLOAD_TYPE) {
    throw new IllegalArgumentException(payloadType + " is not a valid dynamic payload type number." + " (must be between "+ MIN_DYNAMIC_PAYLOAD_TYPE+ " and "+ MAX_DYNAMIC_PAYLOAD_TYPE);
  }
  payloadMappings.put(format,new Byte(payloadType));
}
