{
  for (  Map.Entry<MediaFormat,Byte> entry : payloadTypeMappings.entrySet()) {
    byte fmtPayloadType=entry.getValue();
    if (fmtPayloadType == payloadType)     return entry.getKey();
  }
  return null;
}
