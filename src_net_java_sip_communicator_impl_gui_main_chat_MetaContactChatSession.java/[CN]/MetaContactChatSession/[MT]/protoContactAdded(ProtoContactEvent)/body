{
  if (evt.getNewParent().equals(metaContact)) {
    MetaContactChatTransport chatTransport=new MetaContactChatTransport(this,evt.getProtoContact());
    sessionRenderer.addChatTransport(chatTransport);
  }
}
