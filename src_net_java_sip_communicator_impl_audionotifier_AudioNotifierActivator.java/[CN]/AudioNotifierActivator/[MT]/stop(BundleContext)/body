{
  try {
    configService.setProperty("net.java.sip.communicator.impl.sound.isSoundEnabled",Boolean.toString(!audioNotifier.isMute()));
  }
 catch (  PropertyVetoException e1) {
    logger.error("The proposed property change " + "represents an unacceptable value");
  }
  logger.info("AudioNotifier Service ...[STOPPED]");
}
