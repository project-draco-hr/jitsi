{
  String[] contactsToInvite=new String[contacts.size()];
  for (int i=0; i < contacts.size(); i++) {
    contactsToInvite[i]=contacts.get(i);
  }
  return createAdHocChatRoom(adHocRoomName,contactsToInvite,reason);
}
