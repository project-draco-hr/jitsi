{
  if (otrEngineService == null) {
    ServiceReference confReference=bundleContext.getServiceReference(OtrEngineService.class.getName());
    otrEngineService=(OtrEngineService)bundleContext.getService(confReference);
  }
  return otrEngineService;
}
