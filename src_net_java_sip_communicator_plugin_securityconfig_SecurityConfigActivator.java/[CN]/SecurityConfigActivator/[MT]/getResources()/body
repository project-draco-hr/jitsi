{
  if (resourceService == null) {
    ServiceReference resReference=bundleContext.getServiceReference(ResourceManagementService.class.getName());
    if (resReference != null)     resourceService=(ResourceManagementService)bundleContext.getService(resReference);
  }
  return resourceService;
}
