{
  if (otrKeyManagerService == null) {
    ServiceReference confReference=bundleContext.getServiceReference(OtrKeyManagerService.class.getName());
    otrKeyManagerService=(OtrKeyManagerService)bundleContext.getService(confReference);
  }
  return otrKeyManagerService;
}
