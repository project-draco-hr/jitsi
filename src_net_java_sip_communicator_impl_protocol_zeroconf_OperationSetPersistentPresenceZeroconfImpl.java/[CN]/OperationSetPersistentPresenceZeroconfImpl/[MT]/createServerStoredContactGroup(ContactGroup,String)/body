{
  ContactGroupZeroconfImpl newGroup=new ContactGroupZeroconfImpl(groupName,parentProvider);
  ((ContactGroupZeroconfImpl)parent).addSubgroup(newGroup);
  this.fireServerStoredGroupEvent(newGroup,ServerStoredGroupEvent.GROUP_CREATED_EVENT);
}
