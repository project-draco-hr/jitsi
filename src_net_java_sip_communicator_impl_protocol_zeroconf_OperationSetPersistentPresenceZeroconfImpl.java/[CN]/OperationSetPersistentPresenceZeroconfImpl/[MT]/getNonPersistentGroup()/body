{
  for (int i=0; i < getServerStoredContactListRoot().countSubgroups(); i++) {
    ContactGroupZeroconfImpl gr=(ContactGroupZeroconfImpl)getServerStoredContactListRoot().getGroup(i);
    if (!gr.isPersistent())     return gr;
  }
  return null;
}
