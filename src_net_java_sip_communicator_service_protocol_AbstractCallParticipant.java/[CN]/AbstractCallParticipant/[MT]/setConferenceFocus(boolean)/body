{
  if (this.conferenceFocus != conferenceFocus) {
    this.conferenceFocus=conferenceFocus;
    fireCallParticipantConferenceEvent(new CallParticipantConferenceEvent(this,CallParticipantConferenceEvent.CONFERENCE_FOCUS_CHANGED));
  }
}
