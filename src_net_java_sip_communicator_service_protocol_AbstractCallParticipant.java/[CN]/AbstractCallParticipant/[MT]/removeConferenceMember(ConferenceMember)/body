{
  if (conferenceMember == null)   throw new NullPointerException("conferenceMember");
synchronized (conferenceMembers) {
    if (!conferenceMembers.remove(conferenceMember))     return;
  }
  fireCallParticipantConferenceEvent(new CallParticipantConferenceEvent(this,CallParticipantConferenceEvent.CONFERENCE_MEMBER_REMOVED,conferenceMember));
}
