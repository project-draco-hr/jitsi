{
  if (serviceRegistration != null) {
    globalShortcutService.stop();
    serviceRegistration.unregister();
    serviceRegistration=null;
    if (logger.isDebugEnabled())     logger.debug("GlobalShortcut Service ... [UNREGISTERED]");
  }
  bundleContext=null;
}
