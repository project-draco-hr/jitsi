{
  for (  ChatRoomProviderWrapper crp : chatRoomProviders) {
    if (crp.getProtocolProvider().getAccountID().getAccountAddress().equals(((JLabel)jcb_chatRoomProviders.getSelectedItem()).getText())) {
      return crp;
    }
  }
  return null;
}
