{
  for (  ChatRoomProviderWrapper crp : chatRoomProviders) {
    if (crp.getProtocolProvider().getAccountID().getAccountAddress().equals((jcb_chatRoomProviders.getSelectedItem()))) {
      return crp;
    }
  }
  return null;
}
