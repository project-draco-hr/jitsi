{
  AccountsTableModel model=(AccountsTableModel)accountsTable.getModel();
  int index=accountsTable.getSelectedRow();
  if (index < 0 || index > model.savedPasswordAccounts.size())   return null;
  return model.savedPasswordAccounts.get(index);
}
