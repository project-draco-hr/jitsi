{
  PasswordsTableModel model=(PasswordsTableModel)accountsTable.getModel();
  int index=accountsTable.getSelectedRow();
  if (index < 0 || index > model.savedPasswords.size())   return null;
  return model.savedPasswords.get(index);
}
