{
  if (keyword != null && keyword.length() != 0) {
    chatMessage.setMessage(processKeyword(chatMessage.getMessage(),chatMessage.getContentType(),keyword));
  }
  return this.processMessage(chatMessage);
}
