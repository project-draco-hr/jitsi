{
  Element root=document.getDefaultRootElement();
  if (!isHistory)   ensureDocumentSize();
  Element elem=document.getElement(root,Attribute.ID,ChatHtmlUtils.MESSAGE_TEXT_ID + lastMessageUID);
  if (GuiActivator.getConfigurationService().getBoolean(ReplacementProperty.REPLACEMENT_ENABLE,true) || GuiActivator.getConfigurationService().getBoolean(ReplacementProperty.getPropertyName("SMILEY"),true)) {
    processReplacement(elem,message,contentType);
  }
}
