{
  PluginComponent c=event.getPluginComponent();
  if (c.getContainer().equals(Container.CONTAINER_MAIN_TABBED_PANE)) {
    this.addTab(c.getName(),(Component)c.getComponent());
    this.repaint();
  }
}
