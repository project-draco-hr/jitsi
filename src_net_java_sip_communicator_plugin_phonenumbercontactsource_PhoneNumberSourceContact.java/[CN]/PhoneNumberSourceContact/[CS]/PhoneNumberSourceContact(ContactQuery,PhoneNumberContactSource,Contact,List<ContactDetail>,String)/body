{
  super(parentQuery,contactSource,contact.getDisplayName(),contactDetails);
  this.contact=contact;
  displayDetails=detailDisplayName;
  setPresenceStatus(contact.getPresenceStatus());
  setImage(contact.getImage());
}
