{
  try {
    stop();
  }
 catch (  IOException ioex) {
    throw new UndeclaredThrowableException(ioex);
  }
  if (connected) {
    outputStream=null;
    connected=false;
    audioMixer.disconnect();
  }
}
