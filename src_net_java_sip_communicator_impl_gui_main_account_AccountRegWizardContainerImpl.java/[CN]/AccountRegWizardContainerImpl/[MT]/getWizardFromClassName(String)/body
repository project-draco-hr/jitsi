{
  Iterator i=this.firstPage.getWizardsList();
  while (i.hasNext()) {
    AccountRegistrationWizard wizard=(AccountRegistrationWizard)i.next();
    String wizardClassName1=wizard.getClass().getName().replace('.','_');
    if (wizardClassName1.equals(wizardClassName)) {
      return wizard;
    }
  }
  return null;
}
