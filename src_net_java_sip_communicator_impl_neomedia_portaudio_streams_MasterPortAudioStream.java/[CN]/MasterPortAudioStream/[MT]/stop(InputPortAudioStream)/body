{
  if (!started)   return;
  slaves.remove(slave);
  if (slaves.isEmpty()) {
    PortAudio.Pa_CloseStream(stream);
    stream=0;
    started=false;
    stopping=false;
    PortAudioManager.getInstance().stoppedInputPortAudioStream(this);
  }
}
