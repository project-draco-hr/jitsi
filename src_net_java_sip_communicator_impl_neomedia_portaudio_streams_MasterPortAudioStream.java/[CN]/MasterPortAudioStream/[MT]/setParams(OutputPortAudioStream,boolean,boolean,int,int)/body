{
  if (out != null)   this.connectedToStreamSync=out.getCloseSyncObject();
  long outStream=(out == null) ? 0 : out.getStream();
  PortAudio.setEchoCancelParams(stream,outStream,deNoiseEnabled,echoCancelEnabled,frameSize,filterLength);
}
