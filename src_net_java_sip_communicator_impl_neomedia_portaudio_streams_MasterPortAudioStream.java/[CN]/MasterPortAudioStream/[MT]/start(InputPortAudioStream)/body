{
  if (slave == null)   throw new NullPointerException("slave");
  if (slaves.isEmpty()) {
    if (stream == 0) {
      initStream();
      if (stream == 0)       return;
    }
    PortAudio.Pa_StartStream(stream);
    started=true;
  }
  slaves.add(slave);
}
