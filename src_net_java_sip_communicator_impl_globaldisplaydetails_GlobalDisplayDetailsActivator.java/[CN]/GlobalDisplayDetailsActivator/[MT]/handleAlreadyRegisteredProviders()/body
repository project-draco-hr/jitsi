{
  for (  ServiceReference protocolProviderRef : ServiceUtils.getServiceReferences(bundleContext,ProtocolProviderService.class)) {
    ProtocolProviderService provider=(ProtocolProviderService)bundleContext.getService(protocolProviderRef);
    this.handleProviderAdded(provider);
  }
}
