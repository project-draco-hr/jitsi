{
  if (getChatTabCount() > 0) {
    int selectedIndex=chatTabbedPane.getSelectedIndex();
    if (selectedIndex != 0)     setCurrentChatTab(selectedIndex - 1);
 else     setCurrentChatTab(chatTabbedPane.getTabCount() - 1);
  }
}
