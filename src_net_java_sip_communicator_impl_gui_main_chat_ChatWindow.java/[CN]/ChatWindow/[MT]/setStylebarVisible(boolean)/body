{
  ChatPanel p=this.getCurrentChatPanel();
  if (p != null)   p.setStylebarVisible(b);
  for (int i=0, imax=this.getChatTabCount(); i < imax; i++) {
    p=(ChatPanel)this.chatTabbedPane.getComponentAt(i);
    p.setStylebarVisible(b);
  }
}
