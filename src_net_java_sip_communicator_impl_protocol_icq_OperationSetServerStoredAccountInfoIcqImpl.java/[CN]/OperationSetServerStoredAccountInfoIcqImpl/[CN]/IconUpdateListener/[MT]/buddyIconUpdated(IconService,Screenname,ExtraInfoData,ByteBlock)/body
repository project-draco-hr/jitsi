{
  if (byteBlock != null) {
    if (screenname.getFormatted().equals(uin)) {
synchronized (this) {
        accountImage=new ImageDetail("Account Image",byteBlock.toByteArray());
        this.notifyAll();
      }
    }
  }
}
