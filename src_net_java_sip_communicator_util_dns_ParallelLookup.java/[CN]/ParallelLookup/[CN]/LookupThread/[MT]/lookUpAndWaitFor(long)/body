{
  this.start();
synchronized (this) {
    if (!done) {
      try {
        wait(patience);
      }
 catch (      InterruptedException iexc) {
      }
    }
    done=true;
  }
  return lookup.getAnswers();
}
