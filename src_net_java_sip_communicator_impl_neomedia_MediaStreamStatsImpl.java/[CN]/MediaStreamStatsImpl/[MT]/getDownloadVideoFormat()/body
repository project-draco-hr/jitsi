{
  Dimension videoSize=null;
  MediaDeviceSession mediaDeviceSession=this.mediaStreamImpl.getDeviceSession();
  if (mediaDeviceSession instanceof VideoMediaDeviceSession) {
    return ((VideoMediaDeviceSession)mediaDeviceSession).getReceivedVideoFormat();
  }
  return null;
}
