{
  MediaDeviceSession mediaDeviceSession=mediaStreamImpl.getDeviceSession();
  return (mediaDeviceSession instanceof VideoMediaDeviceSession) ? ((VideoMediaDeviceSession)mediaDeviceSession).getReceivedVideoFormat() : null;
}
