{
  if (isResolved) {
    ProtocolProviderServiceMsnImpl pps=((ProtocolProviderServiceMsnImpl)getProtocolProvider());
    if (pps.isRegistered()) {
      pps.getMessenger().renameFriend(contact.getEmail(),displayName);
    }
  }
}
