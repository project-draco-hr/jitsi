{
  ServiceReference serviceRef=evt.getServiceReference();
  if (serviceRef.getBundle().getState() != Bundle.STARTING)   return;
  if (bundleContext.getServiceReference(UIService.class.getName()) == serviceRef) {
    this.welcomeWindow.close();
  }
}
