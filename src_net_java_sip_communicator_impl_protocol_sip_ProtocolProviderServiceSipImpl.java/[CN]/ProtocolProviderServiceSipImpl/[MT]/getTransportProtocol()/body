{
  if (this.sipRegistrarConnection == null || this.sipRegistrarConnection instanceof SipRegistrarlessConnection) {
    return TransportProtocolEnum.UNKNOWN;
  }
  return TransportProtocolEnum.parse(sipRegistrarConnection.getTransport());
}
