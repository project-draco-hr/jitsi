{
  if (this.messageFactory == null) {
    MessageFactory wrappedFactory=new MessageFactoryImpl();
    this.messageFactory=new SipMessageFactory(this,wrappedFactory);
  }
  return this.messageFactory;
}
