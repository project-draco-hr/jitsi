{
  for (int i=this.contacts.size() - 1; i >= 0; i--) {
    if (((ContactFacebookImpl)this.contacts.get(i)).getAddress().equals(contact.getAddress())) {
      ((ContactFacebookImpl)this.contacts.get(i)).setParentGroup(null);
      this.contacts.remove(i);
    }
  }
}
