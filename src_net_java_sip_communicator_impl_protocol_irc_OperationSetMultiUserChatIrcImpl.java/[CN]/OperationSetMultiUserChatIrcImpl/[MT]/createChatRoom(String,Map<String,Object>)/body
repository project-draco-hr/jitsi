{
  if (this.chatRoomCache.containsKey(roomName)) {
    throw new OperationFailedException("room already exists",OperationFailedException.IDENTIFICATION_CONFLICT);
  }
  return createLocalChatRoomInstance(roomName);
}
