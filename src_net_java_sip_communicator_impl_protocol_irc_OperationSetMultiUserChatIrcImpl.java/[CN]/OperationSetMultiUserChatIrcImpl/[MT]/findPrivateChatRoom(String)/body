{
synchronized (privateRoomCache) {
    if (privateRoomCache.contains(nickName))     return (ChatRoomIrcImpl)privateRoomCache.get(nickName);
    ChatRoomIrcImpl chatRoom=new ChatRoomIrcImpl(nickName,ircProvider);
    this.chatRoomCache.put(nickName,chatRoom);
    return chatRoom;
  }
}
