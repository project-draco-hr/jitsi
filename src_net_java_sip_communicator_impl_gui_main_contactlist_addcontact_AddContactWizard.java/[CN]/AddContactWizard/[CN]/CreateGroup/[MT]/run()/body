{
  try {
    mcl.createMetaContactGroup(mcl.getRoot(),newContact.getNewGroup());
  }
 catch (  MetaContactListException ex) {
    logger.error(ex);
    JOptionPane.showMessageDialog(mainFrame,Messages.getString("addGroupError",newContact.getNewGroup()),Messages.getString("addGroupErrorTitle"),JOptionPane.WARNING_MESSAGE);
  }
}
