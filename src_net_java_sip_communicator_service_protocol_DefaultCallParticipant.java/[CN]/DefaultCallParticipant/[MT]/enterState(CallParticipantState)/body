{
  CallParticipantState oldState=getState();
  this.callParticipantState=newState;
  this.currentStateStartDate=new Date();
  fireCallParticipantChangeEvent(new CallParticipantChangeEvent(this,CallParticipantChangeEvent.CALL_PARTICIPANT_STATUS_CHANGE,oldState,newState));
}
