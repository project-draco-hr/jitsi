{
  boolean formatIsSupported=false;
  if (format != null)   for (  Format supportedFormat : getSupportedFormats())   if (supportedFormat.matches(format)) {
    formatIsSupported=true;
    break;
  }
  return (formatIsSupported) ? getFormat() : null;
}
