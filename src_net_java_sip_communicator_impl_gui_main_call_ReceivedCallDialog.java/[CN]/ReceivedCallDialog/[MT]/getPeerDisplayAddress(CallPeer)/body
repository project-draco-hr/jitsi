{
  String displayName=peer.getDisplayName();
  String peerAddress=peer.getAddress();
  if (StringUtils.isNullOrEmpty(peerAddress,true))   return null;
  if (peerAddress.equalsIgnoreCase(displayName))   return null;
  return peerAddress;
}
