{
  if (files == null)   return;
  byte[] buf=new byte[1024];
  for (  File f : files) {
    if (filterStartsWith != null && !f.getName().startsWith(JAVA_ERROR_LOG_PREFIX)) {
      continue;
    }
    if (isOurCrashLog(f)) {
      addFileToZip(f,out);
    }
  }
}
