{
  if (chatRoomSession == null)   return false;
  if (chatRoomSession.getState() == ChatSessionState.INROOM || chatRoomSession.getState() == ChatSessionState.CONNECTING || chatRoomSession.getState() == ChatSessionState.INITIALIZING)   return true;
  return false;
}
