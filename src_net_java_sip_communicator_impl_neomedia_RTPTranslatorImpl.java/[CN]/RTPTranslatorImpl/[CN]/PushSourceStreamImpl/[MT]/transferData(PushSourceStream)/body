{
  SourceTransferHandler transferHandler=null;
  for (  PushSourceStreamDesc streamDesc : streams)   if (streamDesc.stream == stream) {
    streamToReadFrom=streamDesc;
    transferHandler=this.transferHandler;
    break;
  }
  if (transferHandler != null)   transferHandler.transferData(this);
}
