{
  ContactGroupDictImpl parentGroup=(ContactGroupDictImpl)((ContactDictImpl)contact).getParentContactGroup();
  parentGroup.removeContact((ContactDictImpl)contact);
  fireSubscriptionEvent(contact,((ContactDictImpl)contact).getParentContactGroup(),SubscriptionEvent.SUBSCRIPTION_REMOVED);
}
