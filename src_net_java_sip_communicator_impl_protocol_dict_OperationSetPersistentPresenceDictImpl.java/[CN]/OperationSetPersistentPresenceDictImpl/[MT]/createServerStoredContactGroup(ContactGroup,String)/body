{
  ContactGroupDictImpl newGroup=new ContactGroupDictImpl(groupName,parentProvider);
  ((ContactGroupDictImpl)parent).addSubgroup(newGroup);
  this.fireServerStoredGroupEvent(newGroup,ServerStoredGroupEvent.GROUP_CREATED_EVENT);
}
