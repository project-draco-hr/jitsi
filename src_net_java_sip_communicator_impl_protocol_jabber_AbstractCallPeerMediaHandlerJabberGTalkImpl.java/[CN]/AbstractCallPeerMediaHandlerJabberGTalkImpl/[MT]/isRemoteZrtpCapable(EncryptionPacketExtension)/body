{
  List<? extends PacketExtension> packetExtensions=encryptionPacketExtension.getChildExtensions();
  for (int i=0; i < packetExtensions.size(); ++i) {
    if (packetExtensions.get(i) instanceof ZrtpHashPacketExtension) {
      return true;
    }
  }
  return false;
}
