{
  files=new File[getLogfileCount()];
  for (int i=0; i < getLogfileCount(); i++) {
    files[i]=PacketLoggingActivator.getFileAccessService().getPrivatePersistentFile("log/sip-communicator" + i + ".pcap");
  }
}
