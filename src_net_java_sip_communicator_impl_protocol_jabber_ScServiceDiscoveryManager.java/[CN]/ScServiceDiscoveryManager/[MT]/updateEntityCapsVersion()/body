{
  if (connection instanceof XMPPConnection) {
    if (capsManager != null) {
      capsManager.calculateEntityCapsVersion(getOwnDiscoverInfo(),ServiceDiscoveryManager.getIdentityType(),ServiceDiscoveryManager.getIdentityName(),discoveryManager.getFeatures(),null);
    }
  }
}
