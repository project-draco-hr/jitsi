{
  if ((connection instanceof XMPPConnection) && (capsManager != null)) {
    capsManager.calculateEntityCapsVersion(getOwnDiscoverInfo(),ServiceDiscoveryManager.getIdentityType(),ServiceDiscoveryManager.getIdentityName(),getFeatures(),null);
  }
}
