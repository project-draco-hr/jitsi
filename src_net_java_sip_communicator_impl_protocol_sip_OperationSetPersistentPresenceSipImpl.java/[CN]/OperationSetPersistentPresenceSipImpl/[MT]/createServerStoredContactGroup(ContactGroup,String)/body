{
  ContactGroupSipImpl newGroup=new ContactGroupSipImpl(groupName,parentProvider);
  ((ContactGroupSipImpl)parent).addSubgroup(newGroup);
  this.fireServerStoredGroupEvent(newGroup,ServerStoredGroupEvent.GROUP_CREATED_EVENT);
}
