{
  Component currentConversation=getCurrentConversation();
  tabbedPane.addTab(name,icon,conversation);
  tabbedPane.getParent().validate();
  if (currentConversation != null && !isSelected)   tabbedPane.setSelectedComponent(currentConversation);
 else   tabbedPane.setSelectedComponent(conversation);
}
