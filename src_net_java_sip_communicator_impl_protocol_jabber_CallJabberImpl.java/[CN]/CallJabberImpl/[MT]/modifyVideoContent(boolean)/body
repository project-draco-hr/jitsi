{
  if (logger.isInfoEnabled())   logger.info(allowed ? "Start local video streaming" : "Stop local video streaming");
  for (  CallPeerJabberImpl peer : getCallPeersVector())   peer.sendModifyVideoContent(allowed);
}
