{
  if (logger.isInfoEnabled()) {
    logger.info((allowed ? "Start" : "Stop") + " local video streaming");
  }
  for (  CallPeerJabberImpl peer : getCallPeerList())   peer.sendModifyVideoContent(allowed);
}
