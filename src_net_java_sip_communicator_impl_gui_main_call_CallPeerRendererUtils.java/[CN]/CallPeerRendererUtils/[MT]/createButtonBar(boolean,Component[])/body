{
  JComponent buttonBar=fullScreen ? new CallToolBarPanel(true,false) : new CallToolBarPanel(false,false);
  if (buttons != null) {
    for (    Component button : buttons) {
      if (button != null)       ((Container)buttonBar).add(button);
    }
  }
  return buttonBar;
}
