{
  JComponent buttonBar=new CallToolBarPanel(fullScreen,false);
  if (buttons != null) {
    for (    Component button : buttons) {
      if (button != null)       buttonBar.add(button);
    }
  }
  return buttonBar;
}
