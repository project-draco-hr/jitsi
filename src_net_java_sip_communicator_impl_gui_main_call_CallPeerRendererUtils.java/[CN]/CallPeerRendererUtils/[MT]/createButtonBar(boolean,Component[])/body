{
  JComponent buttonBar=fullScreen ? new CallToolBarPanel(true) : new CallToolBarPanel(false);
  if (buttons != null) {
    for (    Component button : buttons) {
      if (button != null)       ((Container)buttonBar).add(button);
    }
  }
  return buttonBar;
}
