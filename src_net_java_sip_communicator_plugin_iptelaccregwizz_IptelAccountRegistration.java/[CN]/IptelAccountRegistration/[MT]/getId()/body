{
  String serverAddress=getServerAddress();
  return ((id != null) && (id.indexOf('@') < 0) && ((serverAddress == null) || serverAddress.equals(IPTEL_CONNECT_SRV))) ? (id + '@' + IPTEL_CONNECT_SRV) : id;
}
