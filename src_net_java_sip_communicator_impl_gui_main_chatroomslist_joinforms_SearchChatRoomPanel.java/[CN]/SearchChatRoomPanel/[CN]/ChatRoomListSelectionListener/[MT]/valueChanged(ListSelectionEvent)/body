{
  if (e.getValueIsAdjusting() || chatRoomsList.getSelectedIndex() == -1)   return;
  namePanel.removeChatRoomNameListener(SearchChatRoomPanel.this);
  namePanel.setChatRoomName(chatRoomsList.getSelectedValue().toString());
  namePanel.addChatRoomNameListener(SearchChatRoomPanel.this);
}
