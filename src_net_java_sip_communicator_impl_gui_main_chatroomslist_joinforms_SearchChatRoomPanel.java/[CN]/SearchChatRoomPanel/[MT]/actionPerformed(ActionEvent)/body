{
  new Thread(){
    public void run(){
      SwingUtilities.invokeLater(new Runnable(){
        public void run(){
          joinChatRoomDialog.loadChatRoomsList();
        }
      }
);
    }
  }
.start();
  joinChatRoomDialog.setCursor(Cursor.getPredefinedCursor(Cursor.WAIT_CURSOR));
}
