{
  new Thread(){
    public void run(){
      SwingUtilities.invokeLater(new Runnable(){
        public void run(){
          loadChatRoomsList();
        }
      }
);
    }
  }
.start();
  if (parentWindow != null)   parentWindow.setCursor(Cursor.getPredefinedCursor(Cursor.WAIT_CURSOR));
}
