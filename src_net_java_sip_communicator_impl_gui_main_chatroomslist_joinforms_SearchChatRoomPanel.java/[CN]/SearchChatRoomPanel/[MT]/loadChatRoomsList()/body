{
  List list=GuiActivator.getUIService().getConferenceChatManager().getExistingChatRooms(chatRoomProvider);
  if (list != null) {
    if (list.size() == 0)     list.add(Messages.getI18NString("noAvailableRooms").getText());
    chatRoomsList.setListData(new Vector(list));
    chatRoomsScrollPane.setPreferredSize(new Dimension(500,120));
    chatRoomsScrollPane.getViewport().add(chatRoomsList);
    this.mainPanel.add(chatRoomsScrollPane);
    if (wizardContainer != null)     wizardContainer.refresh();
    this.setCursor(Cursor.getDefaultCursor());
  }
}
