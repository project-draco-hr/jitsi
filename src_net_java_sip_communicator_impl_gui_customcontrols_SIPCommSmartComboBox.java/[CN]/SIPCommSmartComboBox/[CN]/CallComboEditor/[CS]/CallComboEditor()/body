{
  text=new JTextField(15);
  text.getDocument().addDocumentListener(this);
  if (text.getUI() instanceof SIPCommTextFieldUI) {
    ((SIPCommTextFieldUI)text.getUI()).setDeleteButtonEnabled(true);
  }
}
