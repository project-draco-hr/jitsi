{
  if (searchSourceType == DEFAULT_SOURCE) {
    MetaContactQuery defaultQuery=mclSource.queryMetaContactSource(filterPattern);
    defaultQuery.addContactQueryListener(GuiActivator.getContactList());
    filterQuery.addContactQuery(defaultQuery);
  }
  if (filterQuery.isCanceled())   return;
  Iterator<ExternalContactSource> filterSources=getContactSources().iterator();
  while (filterSources.hasNext()) {
    final ExternalContactSource filterSource=filterSources.next();
    if (filterQuery.isCanceled())     return;
    filterQuery.addContactQuery(applyFilter(filterSource));
  }
}
