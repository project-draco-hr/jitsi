{
  Iterator<String> searchStrings=uiContact.getSearchStrings();
  while (searchStrings != null && searchStrings.hasNext()) {
    if (isMatching(searchStrings.next()))     return true;
  }
  return false;
}
