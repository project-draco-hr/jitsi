{
  if (filterPattern != null)   return filterPattern.matcher(text).find();
  if (isSearchingPhoneNumber && this.filterString != null)   return GuiActivator.getPhoneNumberI18nService().phoneNumbersMatch(this.filterString,text);
  return true;
}
