{
  super.connect(captureDevice);
  if (!OSUtils.IS_LINUX) {
    Control bufferControl=(Control)captureDevice.getControl(BufferControl.class.getName());
    if (bufferControl != null)     ((BufferControl)bufferControl).setBufferLength(60);
  }
}
