{
  this.context=ctx;
  ctx.addServiceListener(this);
  ServiceReference[] refs=ServiceUtils.getServiceReferences(ctx,className);
  for (  ServiceReference ref : refs)   services.add((T)ctx.getService(ref));
}
