{
  try {
    if (streams == null)     streams=new DSAudioStream[]{new DSAudioStream(getLocator())};
  }
 catch (  Exception e) {
    e.printStackTrace();
    streams=new DSAudioStream[0];
  }
  return streams;
}
