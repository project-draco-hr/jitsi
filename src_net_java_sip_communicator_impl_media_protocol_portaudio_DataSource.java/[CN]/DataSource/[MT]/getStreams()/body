{
  try {
    if (streams == null)     streams=new PortAudioStream[]{new PortAudioStream(getLocator())};
  }
 catch (  Exception e) {
    e.printStackTrace();
    streams=new PortAudioStream[0];
  }
  return streams;
}
