{
  if (connected)   return;
  try {
    PortAudio.initialize();
  }
 catch (  PortAudioException paex) {
    IOException ioex=new IOException();
    ioex.initCause(paex);
    throw ioex;
  }
  connected=true;
}
