{
  if (connected)   return;
  if (!portAudioIsInitialized)   try {
    PortAudio.Pa_Initialize();
    portAudioIsInitialized=true;
  }
 catch (  PortAudioException paex) {
    IOException ioex=new IOException();
    ioex.initCause(paex);
    throw ioex;
  }
  connected=true;
}
