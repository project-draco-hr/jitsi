{
  if (!started)   return;
  try {
    streams[0].stop();
  }
 catch (  PortAudioException paex) {
    IOException ioex=new IOException();
    ioex.initCause(paex);
    throw ioex;
  }
  started=false;
}
