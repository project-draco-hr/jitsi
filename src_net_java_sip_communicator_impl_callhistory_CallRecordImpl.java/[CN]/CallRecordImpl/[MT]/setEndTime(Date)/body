{
  this.endTime=endTime;
  for (  CallParticipantRecord item : participantRecords) {
    CallParticipantRecordImpl itemImpl=(CallParticipantRecordImpl)item;
    if (item.getEndTime() == null)     itemImpl.setEndTime(endTime);
  }
}
