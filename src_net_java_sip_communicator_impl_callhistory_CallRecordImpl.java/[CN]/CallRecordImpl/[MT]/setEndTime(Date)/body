{
  this.endTime=endTime;
  for (  CallPeerRecord item : participantRecords) {
    CallPeerRecordImpl itemImpl=(CallPeerRecordImpl)item;
    if (item.getEndTime() == null)     itemImpl.setEndTime(endTime);
  }
}
