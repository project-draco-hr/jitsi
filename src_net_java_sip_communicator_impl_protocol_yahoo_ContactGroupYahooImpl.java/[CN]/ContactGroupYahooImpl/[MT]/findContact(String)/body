{
  Iterator contacts=contacts();
  while (contacts.hasNext()) {
    ContactYahooImpl item=(ContactYahooImpl)contacts.next();
    if (YahooSession.getYahooUserID(item.getAddress()).equals(YahooSession.getYahooUserID(id)))     return item;
  }
  return null;
}
