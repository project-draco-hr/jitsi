{
  this.yahooGroup=yahooGroup;
  this.isResolved=isResolved;
  this.ssclCallback=ssclCallback;
  Iterator iter=groupMembers.iterator();
  while (iter.hasNext()) {
    addContact(new ContactYahooImpl((YahooUser)iter.next(),ssclCallback,true,true));
  }
}
