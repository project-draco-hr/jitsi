{
  int gridColCount=(int)Math.ceil(Math.sqrt(itemsCount));
  int gridRowCount=itemsCount / gridColCount;
  if (itemsCount > gridRowCount * gridColCount)   gridColCount++;
  return new Dimension(gridColCount,gridRowCount);
}
