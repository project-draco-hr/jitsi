{
  this.setIcon(new ImageIcon(ImageLoader.getImage(ImageLoader.SMILIES_ICON)));
  if (popupMenu != null) {
    popupMenu=this.getPopupMenu();
    if (smiliesService != null)     smiliesService.reloadSmiliesPack();
    popupMenu.removeAll();
  }
}
