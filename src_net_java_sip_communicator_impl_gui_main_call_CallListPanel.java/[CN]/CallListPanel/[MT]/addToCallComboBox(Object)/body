{
  FilterableComboBoxModel callComboModel=(FilterableComboBoxModel)mainFrame.getCallManager().getCallComboBox().getModel();
  if (!callComboModel.contains(obj) && callComboModel.getSize() <= CallComboBox.MAX_COMBO_SIZE)   callComboModel.addElement(obj);
}
