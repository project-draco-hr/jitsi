{
  FilterableComboBoxModel comboModel=(FilterableComboBoxModel)searchComboBox.getModel();
  String allItem=Messages.getString("all");
  if (!comboModel.contains(allItem)) {
    searchComboBox.addItem(allItem);
  }
  if (!comboModel.contains(obj))   comboModel.addElement(obj);
}
