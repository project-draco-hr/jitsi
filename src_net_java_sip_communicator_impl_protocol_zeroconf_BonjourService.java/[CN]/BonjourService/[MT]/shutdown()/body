{
  if (logger.isDebugEnabled())   logger.debug("ZEROCONF: Shutdown!");
  dead=true;
  try {
    sock.close();
  }
 catch (  Exception ex) {
    logger.error(ex);
  }
  changeStatus(ZeroconfStatusEnum.OFFLINE);
  if (jmdns != null)   jmdns.close();
}
