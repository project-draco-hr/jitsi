{
  if (contactName != null) {
    Contact contact=getContactFromMetaContact(contactName);
    if (contact != null) {
      CallManager.call(contact,isVideo,isDesktopSharing,shareRegion);
      return;
    }
 else {
      CallManager.call(contactName,isVideo,isDesktopSharing,shareRegion);
      return;
    }
  }
  CallManager.call(metaContact,isVideo,isDesktopSharing,shareRegion);
}
