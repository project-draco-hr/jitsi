{
  super(displayName);
  this.contact=contact;
  this.contactResource=contactResource;
  setIcon(icon);
  setName(menuName);
  if (isBold)   setFont(getFont().deriveFont(Font.BOLD));
  addActionListener(MetaContactRightButtonMenu.this);
}
