{
  String result=null;
  this.out.println(query);
  result=in.readLine();
  if (result == null) {
    this.connected=false;
    this.connect();
    this.out.println(query);
    result=in.readLine();
    if (result == null) {
      throw new DictException(420);
    }
  }
  return result;
}
