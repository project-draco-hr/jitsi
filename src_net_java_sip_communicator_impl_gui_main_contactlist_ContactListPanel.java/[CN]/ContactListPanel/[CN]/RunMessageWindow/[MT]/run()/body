{
  ChatPanel chatPanel;
  if (protocolContact != null)   chatPanel=chatWindowManager.getContactChat(metaContact,protocolContact);
 else   chatPanel=chatWindowManager.getContactChat(metaContact);
  chatPanel.setSmsSelected(isSmsSelected);
  chatWindowManager.openChat(chatPanel,true);
}
