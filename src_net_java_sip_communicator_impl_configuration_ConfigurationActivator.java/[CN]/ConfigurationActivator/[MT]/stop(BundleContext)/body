{
  try {
    logger.logEntry();
    impl.stop();
    if (logger.isInfoEnabled())     logger.info("ConfigurationService#stop() has been called.");
  }
  finally {
    if (ConfigurationActivator.bundleContext == bundleContext)     ConfigurationActivator.bundleContext=null;
  }
}
