{
  selectAccountPanel=new SelectAccountPanel(newContact,protocolProvidersList);
  selectAccountPanel.addCheckBoxCellListener(this);
  setPanelDescriptorIdentifier(IDENTIFIER);
  setPanelComponent(selectAccountPanel);
}
