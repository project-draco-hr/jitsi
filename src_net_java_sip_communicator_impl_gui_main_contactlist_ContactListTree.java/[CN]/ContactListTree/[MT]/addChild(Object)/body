{
  ContactNode parentNode=null;
  TreePath parentPath=this.getSelectionPath();
  if (parentPath == null) {
    parentNode=this.rootNode;
  }
 else {
    parentNode=(ContactNode)(parentPath.getLastPathComponent());
  }
  return addChild(parentNode,child,true);
}
