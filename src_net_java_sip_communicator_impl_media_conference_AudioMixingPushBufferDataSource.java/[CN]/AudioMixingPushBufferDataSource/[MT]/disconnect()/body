{
  try {
    stop();
  }
 catch (  IOException ex) {
    throw new UndeclaredThrowableException(ex);
  }
  if (connected) {
    outputStream=null;
    connected=false;
    audioMixer.disconnect();
  }
}
