{
  Enumeration e=contactsTable.keys();
  while (e.hasMoreElements()) {
    Contact comboContact=(Contact)e.nextElement();
    if (!comboContact.equals(contact) && comboContact.getPresenceStatus().isOnline())     return true;
  }
  return false;
}
