{
  final String value=state.getServerOptions().getKey(ISupport.TOPICLEN.name());
  if (value == null) {
    return null;
  }
  if (LOGGER.isDebugEnabled()) {
    LOGGER.debug("Setting ISUPPORT parameter " + ISupport.TOPICLEN.name() + " to "+ value);
  }
  return new Integer(value);
}
