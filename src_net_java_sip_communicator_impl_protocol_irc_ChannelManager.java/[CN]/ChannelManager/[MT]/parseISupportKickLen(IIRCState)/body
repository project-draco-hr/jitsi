{
  final String value=state.getServerOptions().getKey(ISupport.KICKLEN.name());
  if (value == null) {
    return null;
  }
  if (LOGGER.isDebugEnabled()) {
    LOGGER.debug("Setting ISUPPORT parameter " + ISupport.KICKLEN.name() + " to "+ value);
  }
  return new Integer(value);
}
