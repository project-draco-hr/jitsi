{
  waitUntilStarted();
  assertStarted();
  CallSessionImpl callSession=new CallSessionImpl(call,this,(URL)callDataSinkMappings.get(call));
  activeCallSessions.put(call,callSession);
  return callSession;
}
