{
  if (logger.isTraceEnabled())   logger.trace("Own status changed to " + messenger.getOwner().getStatus());
  PresenceStatus oldStatus=currentStatus;
  currentStatus=msnStatusToPresenceStatus(messenger.getOwner().getStatus());
  fireProviderStatusChangeEvent(oldStatus,currentStatus);
}
