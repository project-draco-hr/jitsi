{
  if (!(parentGroup instanceof ContactGroupMsnImpl || parentGroup instanceof RootContactGroupMsnImpl))   throw new IllegalArgumentException("Argument is not an msn contact group (group=" + parentGroup + ")");
  ContactMsnImpl contact=ssContactList.createUnresolvedContact(parentGroup,address);
  contact.setPersistentData(persistentData);
  return contact;
}
