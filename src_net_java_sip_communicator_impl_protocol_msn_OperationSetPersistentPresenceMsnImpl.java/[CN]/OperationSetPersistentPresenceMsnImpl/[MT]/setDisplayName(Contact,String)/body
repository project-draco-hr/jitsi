{
  assertConnected();
  if (!(contact instanceof ContactMsnImpl))   throw new IllegalArgumentException("The specified contact is not an msn contact." + contact);
  ssContactList.setDisplayName((ContactMsnImpl)contact,newName);
}
