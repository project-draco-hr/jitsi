{
  assertConnected();
  if (!(group instanceof ContactGroupMsnImpl))   throw new IllegalArgumentException("The specified group is not an msn contact group: " + group);
  ssContactList.renameGroup((ContactGroupMsnImpl)group,newName);
}
