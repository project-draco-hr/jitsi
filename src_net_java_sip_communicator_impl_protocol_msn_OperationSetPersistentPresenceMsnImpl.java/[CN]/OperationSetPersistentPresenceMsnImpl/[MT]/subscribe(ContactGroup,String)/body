{
  assertConnected();
  if (!(parent instanceof ContactGroupMsnImpl))   throw new IllegalArgumentException("Argument is not an msn contact group (group=" + parent + ")");
  ssContactList.addContact((ContactGroupMsnImpl)parent,contactIdentifier);
}
