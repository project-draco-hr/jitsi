{
  int delta=5;
  if (!isOneActionButtonEnabled())   delta+=5;
 else {
    if (isCloseButtonEnabled)     delta+=BUTTONSIZE + WIDTHDELTA;
    if (isMaxButtonEnabled)     delta+=BUTTONSIZE + WIDTHDELTA;
  }
  return super.calculateTabWidth(tabPlacement,tabIndex,metrics) + delta;
}
