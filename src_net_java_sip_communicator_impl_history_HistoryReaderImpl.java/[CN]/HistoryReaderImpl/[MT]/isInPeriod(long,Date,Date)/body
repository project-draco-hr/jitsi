{
  if (startDate == null) {
    if (endDate == null)     return true;
 else     return timestamp < endDate.getTime();
  }
 else {
    if (endDate == null)     return timestamp > startDate.getTime();
 else     return timestamp > startDate.getTime() && timestamp < endDate.getTime();
  }
}
