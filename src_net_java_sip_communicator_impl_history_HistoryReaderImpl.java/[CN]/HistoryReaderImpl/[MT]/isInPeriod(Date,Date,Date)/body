{
  if (startDate == null) {
    if (endDate == null)     return true;
 else     return timestamp.before(endDate);
  }
 else {
    if (endDate == null)     return timestamp.after(startDate);
 else     return timestamp.after(startDate) && timestamp.before(endDate);
  }
}
