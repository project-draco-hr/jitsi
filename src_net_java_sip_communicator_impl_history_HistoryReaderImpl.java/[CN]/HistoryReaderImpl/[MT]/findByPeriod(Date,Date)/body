{
  String expr="/history/record[@timestamp>" + startDate.getTime() + "]"+ "[@timestamp<"+ endDate.getTime()+ "]";
  return this.findByXpath(expr,startDate,endDate,new ProgressEvent(this,startDate,endDate,null,null));
}
