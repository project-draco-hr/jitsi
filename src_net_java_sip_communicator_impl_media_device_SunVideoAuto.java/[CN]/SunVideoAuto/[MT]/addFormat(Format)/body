{
  Enumeration enumeration=formats.elements();
  while (enumeration.hasMoreElements()) {
    Format format=(Format)enumeration.nextElement();
    if (format.equals(fin))     return;
  }
  formats.addElement(fin);
}
