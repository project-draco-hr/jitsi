{
  if (contactListService == null) {
    ServiceReference serviceReference=bundleContext.getServiceReference(MetaContactListService.class.getName());
    contactListService=(MetaContactListService)bundleContext.getService(serviceReference);
  }
  return contactListService;
}
