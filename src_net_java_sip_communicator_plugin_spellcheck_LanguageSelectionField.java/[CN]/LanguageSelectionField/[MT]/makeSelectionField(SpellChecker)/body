{
  if (CLASS_INSTANCES.containsKey(checker))   return CLASS_INSTANCES.get(checker);
 else {
    LanguageSelectionField instance=new LanguageSelectionField(checker);
    CLASS_INSTANCES.put(checker,instance);
    return instance;
  }
}
