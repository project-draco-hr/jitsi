{
  firstWizardPage.commitPage();
  if (registration.getId() != null)   return signin(registration.getId(),registration.getPassword());
 else   throw new OperationFailedException("User name null.",OperationFailedException.ILLEGAL_ARGUMENT);
}
