{
  if (super.isMute() == mute)   return;
  super.setMute(mute);
  if (mute) {
    audioMixerMediaDeviceSession.removeLocalUserAudioLevelListener(this.localUserAudioLevelListener);
  }
 else {
    audioMixerMediaDeviceSession.addLocalUserAudioLevelListener(this.localUserAudioLevelListener);
  }
}
