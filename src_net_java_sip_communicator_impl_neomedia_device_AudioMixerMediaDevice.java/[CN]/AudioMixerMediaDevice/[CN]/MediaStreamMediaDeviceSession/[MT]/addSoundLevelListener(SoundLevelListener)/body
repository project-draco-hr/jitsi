{
synchronized (stSLListeners) {
    if (!stSLListeners.contains(listener))     stSLListeners.add(listener);
    if (receiveStream != null)     audioMixerMediaDeviceSession.addSoundLevelListener(receiveStream,listener);
  }
}
