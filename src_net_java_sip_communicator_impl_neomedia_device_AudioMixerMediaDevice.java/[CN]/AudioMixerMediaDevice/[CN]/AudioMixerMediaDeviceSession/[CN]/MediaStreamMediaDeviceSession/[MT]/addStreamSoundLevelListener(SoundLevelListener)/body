{
synchronized (streamSndLevelListeners) {
    if (!streamSndLevelListeners.contains(listener))     streamSndLevelListeners.add(listener);
    if (receiveStream != null)     audioMixerMediaDeviceSession.addStreamSoundLevelListener(receiveStream,listener);
  }
}
