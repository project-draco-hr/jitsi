{
synchronized (streamSndLevelListeners) {
    streamSndLevelListeners.remove(listener);
    if (receiveStream != null)     audioMixerMediaDeviceSession.removeSoundLevelListener(receiveStream,listener);
  }
}
