{
synchronized (streamAudioLevelListeners) {
    AudioLevelEventDispatcher dispatcher=new AudioLevelEventDispatcher();
    dispatcher.addAudioLevelListener(l);
    streamAudioLevelListeners.put(stream,dispatcher);
  }
}
