{
  if (!MediaType.AUDIO.equals(device.getMediaType()))   throw new IllegalArgumentException("device");
  if (!device.getDirection().allowsSending())   throw new IllegalArgumentException("device");
  this.device=device;
}
