{
  List<MediaDeviceSession> sessions=new ArrayList<MediaDeviceSession>();
  for (  MediaStreamMediaDeviceSession s : deviceSession.mediaStreamMediaDeviceSessions) {
    sessions.add(s);
  }
  for (  MediaDeviceSession s : sessions)   s.close();
}
