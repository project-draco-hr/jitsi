{
  if (audioMixer == null)   audioMixer=new AudioMixer(device.getCaptureDevice()){
    @Override protected void readCaptureDeviceStream(    PushBufferStream stream,    Buffer buffer) throws IOException {
      super.readCaptureDeviceStream(stream,buffer);
    }
  }
;
  return audioMixer;
}
