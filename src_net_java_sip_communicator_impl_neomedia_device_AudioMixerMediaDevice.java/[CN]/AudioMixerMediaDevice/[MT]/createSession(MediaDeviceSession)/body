{
  MediaStreamMediaDeviceSession session=(MediaStreamMediaDeviceSession)createSession();
  MediaStreamMediaDeviceSession old=(MediaStreamMediaDeviceSession)oldSession;
  session.setStreamAudioLevelListener(old.streamAudioLevelListener);
  session.setLocalUserAudioLevelListener(old.localUserAudioLevelListener);
  return session;
}
