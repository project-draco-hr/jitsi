{
  if (isSimpleTheme)   return createSimpleIncomingMessageTag(messageID,contactName,contactDisplayName,avatarPath,date,message,contentType);
 else   return createAdvancedIncomingMessageTag(messageID,contactName,contactDisplayName,avatarPath,date,message,contentType);
}
