{
  JPanel mainPanel=new TransparentPanel(new BorderLayout(5,5));
  enable=new SIPCommCheckBox(GeneralConfigPluginActivator.getResources().getI18NString("plugin.autoaway.ENABLE_CHANGE_STATUS"));
  mainPanel.add(enable,BorderLayout.NORTH);
  enable.addActionListener(new ActionListener(){
    public void actionPerformed(    ActionEvent e){
      timer.setEnabled(enable.isSelected());
      saveData();
    }
  }
);
  JPanel timerPanel=new TransparentPanel(new FlowLayout(FlowLayout.LEFT));
  timerPanel.add(new JLabel(GeneralConfigPluginActivator.getResources().getI18NString("plugin.autoaway.AWAY_MINUTES")));
  timer=new JSpinner(new SpinnerNumberModel(DEFAULT_TIMER,1,180,1));
  timerPanel.add(timer);
  timer.addChangeListener(new ChangeListener(){
    public void stateChanged(    ChangeEvent e){
      saveData();
    }
  }
);
  mainPanel.add(timerPanel,BorderLayout.WEST);
  return mainPanel;
}
