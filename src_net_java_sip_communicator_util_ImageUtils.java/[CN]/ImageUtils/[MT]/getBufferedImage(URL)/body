{
  BufferedImage image=null;
  if (imagePath == null) {
    return null;
  }
  try {
    image=ImageIO.read(imagePath);
  }
 catch (  Exception exc) {
    logger.debug("Failed to load image:" + imagePath,exc);
  }
  return image;
}
