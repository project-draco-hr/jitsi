{
  Container parent=getParent();
  if (parent != null)   for (  Component component : parent.getComponents())   if (component instanceof SwingVideoComponentCanvas)   return (SwingVideoComponentCanvas)component;
  return null;
}
