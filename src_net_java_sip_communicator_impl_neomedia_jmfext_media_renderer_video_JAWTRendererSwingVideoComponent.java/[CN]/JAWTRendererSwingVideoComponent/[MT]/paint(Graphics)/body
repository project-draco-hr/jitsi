{
synchronized (renderer.getHandleLock()) {
    long handle=renderer.getHandle();
    if (wantsPaint && (handle != 0)) {
      wantsPaint=JAWTRenderer.paintLightweightComponent(handle,this,g);
    }
  }
}
