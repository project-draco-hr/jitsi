{
synchronized (resultTreeModel) {
    ExternalContactSource sourceUI=TreeContactList.getContactSource(event.getQuerySource().getContactSource());
    addHistoryContact(event.getContact(),sourceUI);
  }
}
