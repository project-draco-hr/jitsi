{
  ProtocolProviderFactory factory=FacebookAccRegWizzActivator.getFacebookProtocolProviderFactory();
  Iterable<AccountID> registeredAccounts=factory.getRegisteredAccounts();
  for (  AccountID accountID : registeredAccounts)   if (email.equalsIgnoreCase(accountID.getUserID()))   return true;
  return false;
}
