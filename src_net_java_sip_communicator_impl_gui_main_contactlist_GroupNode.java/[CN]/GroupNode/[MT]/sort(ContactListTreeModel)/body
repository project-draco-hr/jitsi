{
  if (children != null) {
    SwingUtilities.invokeLater(new Runnable(){
      public void run(){
        Collections.sort(children,nodeComparator);
        fireNodesChanged();
      }
    }
);
  }
}
