{
  GroupNode groupNode=new GroupNode(treeModel,metaGroup);
  this.add(groupNode);
  Collections.sort(children,nodeComparator);
  if (isRefreshView)   this.fireNodeInserted(treeModel,getIndex(groupNode));
  return groupNode;
}
