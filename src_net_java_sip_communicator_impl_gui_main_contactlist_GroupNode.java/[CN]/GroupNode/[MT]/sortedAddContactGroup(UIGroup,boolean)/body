{
  GroupNode groupNode=new GroupNode(treeModel,uiGroup);
  uiGroup.setGroupNode(groupNode);
  this.add(groupNode);
  Collections.sort(children,nodeComparator);
  if (isRefreshView)   this.fireNodeInserted(getIndex(groupNode));
  return groupNode;
}
