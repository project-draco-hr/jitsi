{
  Enumeration<TreeNode> children=children();
  while (children.hasMoreElements()) {
    TreeNode treeNode=children.nextElement();
    if (treeNode instanceof ContactNode && ((ContactNode)treeNode).getMetaContact().equals(metaContact))     return (ContactNode)treeNode;
  }
  return null;
}
