{
  ContactNode contactNode=new ContactNode(metaContact);
  this.add(contactNode);
  Collections.sort(children,nodeComparator);
  if (isRefreshView)   this.fireNodeInserted(treeModel,getIndex(contactNode));
  return contactNode;
}
