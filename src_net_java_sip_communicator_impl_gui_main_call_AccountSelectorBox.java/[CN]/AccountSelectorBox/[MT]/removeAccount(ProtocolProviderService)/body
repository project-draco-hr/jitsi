{
  JMenuItem accountItem=this.accountsTable.get(pps);
  this.menu.remove(accountItem);
  this.accountsTable.remove(pps);
  if (selectedProvider == pps && accountsTable.size() > 0)   setSelected(accountsTable.keySet().iterator().next());
}
