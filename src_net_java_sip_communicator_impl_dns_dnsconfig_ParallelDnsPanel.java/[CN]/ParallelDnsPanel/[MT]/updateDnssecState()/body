{
  boolean isDnssec=configService.getBoolean(ParallelResolver.PNAME_DNSSEC_RESOLVER_ENABLED,ParallelResolver.PDEFAULT_DNSSEC_RESOLVER_ENABLED);
  if (isDnssec)   chkBackupDnsEnabled.setSelected(false);
  chkBackupDnsEnabled.setEnabled(!isDnssec);
  updateButtonsState();
}
