{
  if (getToAddress().equals(toAddress)) {
    String thisEventId=getEventId();
    if (((thisEventId == null) && (eventId == null)) || ((thisEventId != null) && thisEventId.equals(eventId)))     return true;
  }
  return false;
}
