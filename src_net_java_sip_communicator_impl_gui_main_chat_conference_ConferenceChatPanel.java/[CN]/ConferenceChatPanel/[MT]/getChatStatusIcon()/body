{
  String status=Constants.OFFLINE_STATUS;
  if (chatRoomWrapper.getChatRoom() != null && chatRoomWrapper.getChatRoom().isJoined())   status=Constants.ONLINE_STATUS;
  return new ImageIcon(Constants.getStatusIcon(status));
}
