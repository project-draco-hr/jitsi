{
switch (mode) {
case 'O':
    return new Mode(add,mode,this.params[this.index++]);
case 'o':
  return new Mode(add,mode,this.params[this.index++]);
case 'v':
return new Mode(add,mode,this.params[this.index++]);
default :
throw new IllegalArgumentException("" + mode);
}
}
