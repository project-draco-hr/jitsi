{
  ColibriConferenceIQ.Channel channel=null;
  if (getPeer().isJitsiVideobridge() && (transportManager instanceof RawUdpTransportManager)) {
    channel=((RawUdpTransportManager)transportManager).getColibriChannel(mediaType,false);
  }
  return channel;
}
