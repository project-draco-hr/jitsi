{
  if (MediaType.AUDIO.equals(mediaType))   this.audioSenders=senders;
 else   if (MediaType.VIDEO.equals(mediaType))   this.videoSenders=senders;
 else   throw new IllegalArgumentException("mediaType");
}
