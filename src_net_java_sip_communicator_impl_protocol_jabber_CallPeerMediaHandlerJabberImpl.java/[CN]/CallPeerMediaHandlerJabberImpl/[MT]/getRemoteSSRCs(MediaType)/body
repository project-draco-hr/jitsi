{
  TransportManagerJabberImpl transportManager=this.transportManager;
  if (transportManager instanceof RawUdpTransportManager) {
    RawUdpTransportManager rawUdpTransportManager=(RawUdpTransportManager)transportManager;
    CobriConferenceIQ.Channel channel=rawUdpTransportManager.getCobriChannel(mediaType,false);
    if (channel != null)     return channel.getSSRCs();
  }
  long ssrc=super.getRemoteSSRC(mediaType);
  return (ssrc == -1) ? CobriConferenceIQ.NO_SSRCS : new long[]{ssrc};
}
