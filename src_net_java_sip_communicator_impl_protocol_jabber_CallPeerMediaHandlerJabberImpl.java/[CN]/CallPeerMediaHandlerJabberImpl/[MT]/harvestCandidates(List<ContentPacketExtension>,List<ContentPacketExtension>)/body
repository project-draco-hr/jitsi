{
  TransportManagerJabberImpl transportManager=getTransportManager();
  if (remote == null)   transportManager.startCandidateHarvest(local);
 else   transportManager.startCandidateHarvest(remote,local);
  return transportManager.wrapupCandidateHarvest();
}
