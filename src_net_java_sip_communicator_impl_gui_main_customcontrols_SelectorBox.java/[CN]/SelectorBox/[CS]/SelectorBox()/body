{
  super(ImageLoader.getImage(ImageLoader.STATUS_SELECTOR_BOX),ImageLoader.getImage(ImageLoader.STATUS_SELECTOR_BOX),null);
  this.popup=new AntialiasedPopupMenu();
  this.popup.setInvoker(this);
  this.addActionListener(this);
}
