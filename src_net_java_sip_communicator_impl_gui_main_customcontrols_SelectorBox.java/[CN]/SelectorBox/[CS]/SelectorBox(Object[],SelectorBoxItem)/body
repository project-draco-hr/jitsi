{
  super(ImageLoader.getImage(ImageLoader.STATUS_SELECTOR_BOX),ImageLoader.getImage(ImageLoader.STATUS_SELECTOR_BOX),selectedItem.getIcon());
  this.items=items;
  this.popup=new AntialiasedPopupMenu();
  this.popup.setInvoker(this);
  this.addActionListener(this);
  this.init();
}
