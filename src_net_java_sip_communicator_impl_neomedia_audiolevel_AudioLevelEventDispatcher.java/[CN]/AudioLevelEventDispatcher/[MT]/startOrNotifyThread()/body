{
  if ((this.listener == null) && ((cache == null) || (ssrc == -1))) {
    thread=null;
    notify();
  }
 else   if ((data != null) && (dataLength > 0)) {
    if (thread == null)     startThread();
 else     notify();
  }
}
