{
  Iterator<?> ii=subCode.iterator();
  Object msgCode=ii.next();
  if (logger.isDebugEnabled())   logger.debug(sessionTypeToString(sessionType) + ": ZRTP key negotiation failed, sub code: " + msgCode);
}
