{
  if (escape) {
    if (isFullScreen()) {
      setFullScreen(false);
      return;
    }
  }
 else {
    callPanel.disposeCallInfoFrame();
    setVisible(false);
  }
  callPanel.actionPerformedOnHangupButton(escape);
}
