{
  Call call=callPanel.getCall();
  Iterator<CallPeer> callParticipants=(call == null) ? new Vector<CallPeer>().iterator() : callPanel.getCall().getCallPeers();
  return new DialpadDialog(callParticipants);
}
