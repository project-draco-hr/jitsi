{
  Call call=callPanel.getCall();
  Iterator<CallPeer> callParticipants=(call == null) ? new Vector<CallPeer>().iterator() : callPanel.getCall().getCallParticipants();
  return new DialpadDialog(callParticipants);
}
