{
  if (evt.getSourceCall() == call) {
    Timer timer=new Timer(5000,new RemovePeerPanelListener(evt.getSourceCallPeer()));
    timer.setRepeats(false);
    timer.start();
    if (call.getCallPeerCount() == 0) {
      this.stopCallTimer();
    }
  }
}
