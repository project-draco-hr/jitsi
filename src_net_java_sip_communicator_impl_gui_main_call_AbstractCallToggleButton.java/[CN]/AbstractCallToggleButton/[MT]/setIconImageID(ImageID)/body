{
  this.iconImageID=iconImageID;
  Image iconImage=ImageLoader.getImage(iconImageID);
  if (!isFullScreen() && !settingsPanel)   iconImage=ImageUtils.scaleImageWithinBounds(iconImage,18,18);
  setIconImage(iconImage);
}
