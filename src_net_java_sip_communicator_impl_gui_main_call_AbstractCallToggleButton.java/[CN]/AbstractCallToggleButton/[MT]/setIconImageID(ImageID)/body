{
  this.iconImageID=iconImageID;
  if (!fullScreen && !settingsPanel)   setIconImage(ImageUtils.scaleImageWithinBounds(ImageLoader.getImage(iconImageID),12,12));
 else   setIconImage(ImageLoader.getImage(iconImageID));
}
