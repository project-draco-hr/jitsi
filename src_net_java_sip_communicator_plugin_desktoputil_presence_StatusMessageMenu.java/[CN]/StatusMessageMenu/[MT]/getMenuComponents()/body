{
  if (menu instanceof JMenu)   return ((JMenu)menu).getMenuComponents();
 else {
    int c=((Menu)menu).getItemCount();
    Object[] res=new Object[c];
    for (int i=0; i < c; i++) {
      res[i]=((Menu)menu).getItem(i);
    }
    return res;
  }
}
