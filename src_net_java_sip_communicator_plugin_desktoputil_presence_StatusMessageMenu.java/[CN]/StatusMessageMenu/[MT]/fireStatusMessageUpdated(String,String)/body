{
  PropertyChangeEvent evt=new PropertyChangeEvent(protocolProvider,STATUS_MESSAGE_UPDATED_PROP,oldMessage,newMessage);
  if (propertyChangeListeners != null) {
    for (    PropertyChangeListener target : propertyChangeListeners)     target.propertyChange(evt);
  }
}
