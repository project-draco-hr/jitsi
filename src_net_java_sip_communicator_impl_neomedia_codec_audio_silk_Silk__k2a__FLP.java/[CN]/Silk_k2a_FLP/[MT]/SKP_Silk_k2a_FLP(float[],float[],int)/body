{
  int k, n;
  float[] Atmp=new float[Silk_SigProc_FIX.SKP_Silk_MAX_ORDER_LPC];
  for (k=0; k < order; k++) {
    for (n=0; n < k; n++) {
      Atmp[n]=A[n];
    }
    for (n=0; n < k; n++) {
      A[n]+=Atmp[k - n - 1] * rc[k];
    }
    A[k]=-rc[k];
  }
}
