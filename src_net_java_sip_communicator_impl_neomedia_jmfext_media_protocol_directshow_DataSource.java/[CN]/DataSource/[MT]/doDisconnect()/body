{
  if (logger.isInfoEnabled())   logger.info("doDisconnect");
  super.doDisconnect();
  if (manager != null) {
    device.setDelegate(null);
    device=null;
    DSManager.dispose();
    manager=null;
  }
}
