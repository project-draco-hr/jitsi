{
  try {
    notifyGrowlOf("Message Received",sipIconPath,popupMessage.getMessageTitle(),popupMessage.getMessage());
  }
 catch (  Exception ex) {
    logger.error("Could not notify the received message to Growl",ex);
  }
}
