{
  PopupMessage m=(PopupMessage)shownPopups.get(context);
  if (m != null) {
synchronized (shownPopups) {
      shownPopups.remove(context);
    }
    logger.trace("Growl notification timed-out :" + m.getMessageTitle() + ": "+ m.getMessage());
  }
}
