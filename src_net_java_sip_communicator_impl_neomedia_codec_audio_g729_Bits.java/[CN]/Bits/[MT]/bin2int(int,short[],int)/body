{
  short BIT_1=Ld8k.BIT_1;
  int value, i;
  int bit;
  value=0;
  for (i=0; i < no_of_bits; i++) {
    value<<=1;
    bit=bitstream[bitstream_offset++];
    if (bit == BIT_1)     value+=1;
  }
  return (value);
}
