{
  int i=this.list.getSelectedIndex();
  if (e.getKeyCode() == KeyEvent.VK_UP && i > 0) {
    this.list.setSelectedIndex(i - 1);
  }
 else   if (e.getKeyCode() == KeyEvent.VK_DOWN && i < this.list.getModel().getSize() - 1) {
    this.list.setSelectedIndex(i + 1);
  }
}
