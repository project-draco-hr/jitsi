{
  if (family.endsWith(".plain"))   family=family.replace(".plain","");
  for (  String f : fontFamilies) {
    String oldF=f;
    if (family.equals(f) || family.equals(f.replaceAll(" ","")))     fontFamilyPanel.setSelected(oldF);
  }
  updatePreview();
}
