{
  if (family.endsWith(".plain"))   family=family.replace(".plain","");
  for (  String f : this.fontFamilies)   if (family.equals(f))   this.fontFamilyPanel.setSelected(family);
  this.updatePreview();
}
