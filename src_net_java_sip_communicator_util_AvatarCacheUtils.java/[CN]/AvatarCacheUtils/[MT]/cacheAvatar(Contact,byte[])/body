{
  String avatarDirPath=AVATAR_DIR + File.separator + escapeSpecialCharacters(protoContact.getProtocolProvider().getAccountID().getAccountUniqueID());
  String avatarFileName=escapeSpecialCharacters(protoContact.getAddress());
  cacheAvatar(avatarDirPath,avatarFileName,avatarBytes);
}
