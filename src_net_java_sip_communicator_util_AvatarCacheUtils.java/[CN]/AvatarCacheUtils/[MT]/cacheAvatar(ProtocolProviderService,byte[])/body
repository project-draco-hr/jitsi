{
  String accountUID=protocolProvider.getAccountID().getAccountUniqueID();
  String avatarDirPath=AVATAR_DIR + File.separator + escapeSpecialCharacters(accountUID);
  String avatarFileName=escapeSpecialCharacters(accountUID);
  cacheAvatar(avatarDirPath,avatarFileName,avatarBytes);
}
