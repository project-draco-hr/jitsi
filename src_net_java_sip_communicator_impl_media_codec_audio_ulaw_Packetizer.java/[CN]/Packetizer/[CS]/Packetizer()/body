{
  super();
  packetSize=240;
  setPacketSize(packetSize);
  PlugInManager.removePlugIn("com.sun.media.codec.audio.ulaw.Packetizer",PlugInManager.CODEC);
  try {
    PlugInManager.commit();
  }
 catch (  IOException e) {
  }
  PLUGIN_NAME="ULaw Packetizer";
}
