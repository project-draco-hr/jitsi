{
  if (certificateService == null) {
    ServiceReference certVerifyReference=bundleContext.getServiceReference(CertificateVerificationService.class.getName());
    if (certVerifyReference != null)     certificateService=(CertificateVerificationService)bundleContext.getService(certVerifyReference);
  }
  return certificateService;
}
