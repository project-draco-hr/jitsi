{
  int hashCode=0;
  InetSocketAddress controlAddress=getControlAddress();
  if (controlAddress != null)   hashCode|=controlAddress.hashCode();
  InetSocketAddress dataAddress=getDataAddress();
  if (dataAddress != null)   hashCode|=dataAddress.hashCode();
  return hashCode;
}
