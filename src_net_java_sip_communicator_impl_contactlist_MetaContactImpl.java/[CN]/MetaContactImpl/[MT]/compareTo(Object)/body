{
  MetaContactImpl target=(MetaContactImpl)o;
  return (totalStatus - target.totalStatus) * 1000000 + getDisplayName().compareTo(target.getDisplayName()) * 100000 + getMetaUID().compareTo(target.getMetaUID());
}
