{
  MetaContactImpl target=(MetaContactImpl)o;
  return ((PresenceStatus.MAX_STATUS_VALUE - totalStatus) - (PresenceStatus.MAX_STATUS_VALUE - target.totalStatus)) * 1000000 + getDisplayName().compareToIgnoreCase(target.getDisplayName()) * 100000 + getMetaUID().compareTo(target.getMetaUID());
}
