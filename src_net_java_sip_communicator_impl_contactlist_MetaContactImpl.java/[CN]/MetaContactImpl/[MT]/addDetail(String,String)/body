{
  List<String> values=details.get(name);
  if (values == null)   values=new ArrayList<String>();
  values.add(value);
  details.put(name,values);
  mclServiceImpl.fireMetaContactEvent(new MetaContactModifiedEvent(this,name,null,value));
}
