{
  for (  Contact contact : protoContacts) {
    if (contact.getProtocolProvider() == ownerProvider && (contact.getAddress().equals(contactAddress) || contact.equals(contactAddress)))     return contact;
  }
  return null;
}
