{
  ArrayList values=(ArrayList)details.get(name);
  if (values == null)   return;
  values.remove(value);
  mclServiceImpl.fireMetaContactEvent(new MetaContactModifiedEvent(this,name,value,null));
}
