{
  LinkedList<Contact> opSetContacts=new LinkedList<Contact>();
  for (  Contact contact : protoContacts) {
    if (contact.getProtocolProvider().getOperationSet(opSetClass) != null)     opSetContacts.add(contact);
  }
  return opSetContacts;
}
