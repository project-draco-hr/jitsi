{
  for (int i=0; i < container.getComponentCount(); i++) {
    Component c=container.getComponent(i);
    if (c instanceof Skinnable)     ((Skinnable)c).loadSkin();
    if (c instanceof java.awt.Container)     reloadComponents((java.awt.Container)c);
  }
}
