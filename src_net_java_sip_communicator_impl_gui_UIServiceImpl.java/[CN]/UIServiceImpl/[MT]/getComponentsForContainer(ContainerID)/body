{
  Vector plugins=(Vector)this.registeredPlugins.get(containerID);
  if (plugins != null)   return plugins.iterator();
 else   throw new IllegalArgumentException("The container that you specified is not " + "supported by this UIService implementation.");
}
