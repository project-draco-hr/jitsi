{
  ChatWindowManager chatWindowManager=getChatWindowManager();
  ChatPanel chatPanel=chatWindowManager.getMultiChat(chatRoom,false);
  if (chatPanel != null)   chatWindowManager.closeChat(chatPanel);
}
