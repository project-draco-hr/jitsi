{
  try {
    if (this.rssFeed == null)     this.opSet.refreshRssFeed();
 else     this.opSet.refreshRssFeed(this.rssFeed,this.newContact,this.aloneUpdate);
  }
 catch (  Exception exc) {
    exc.printStackTrace();
  }
}
