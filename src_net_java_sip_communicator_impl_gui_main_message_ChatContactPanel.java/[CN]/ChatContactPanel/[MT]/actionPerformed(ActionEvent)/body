{
  JButton button=(JButton)e.getSource();
  MainFrame mainFrame=chatPanel.getChatWindow().getMainFrame();
  if (button.getName().equals("call")) {
  }
 else   if (button.getName().equals("info")) {
    if (contactItem != null) {
      Contact protocolContact=chatPanel.getProtocolContact();
      ProtocolProviderService pps=protocolContact.getProtocolProvider();
      OperationSetWebContactInfo wContactInfo=mainFrame.getWebContactInfo(pps);
      if (wContactInfo != null) {
        GuiActivator.getBrowserLauncher().openURL(wContactInfo.getWebContactInfo(protocolContact).toString());
      }
    }
  }
}
