{
  updateContactInfo();
  if (contactMetaInfo != null && contactMetaInfo.status != null && !contactMetaInfo.status.trim().equals(""))   return contactMetaInfo.status + "[" + contactMetaInfo.statusTimeRel+ "]";
 else   return null;
}
