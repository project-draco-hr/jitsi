{
  if (obj == null || !(obj instanceof ContactFacebookImpl))   return false;
  ContactFacebookImpl facebookContact=(ContactFacebookImpl)obj;
  return this.getAddress().equals(facebookContact.getAddress());
}
