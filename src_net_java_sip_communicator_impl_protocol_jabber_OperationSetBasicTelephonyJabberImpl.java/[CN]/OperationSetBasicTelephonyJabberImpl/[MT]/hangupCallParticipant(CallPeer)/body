{
  CallPeerJabberImpl callParticipant=(CallPeerJabberImpl)participant;
  try {
    callParticipant.getJingleSession().terminate();
  }
 catch (  XMPPException ex) {
    ex.printStackTrace();
  }
 finally {
    callParticipant.setState(CallPeerState.DISCONNECTED);
  }
}
