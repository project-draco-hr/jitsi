{
  Presence p=jabberProvider.getConnection().getRoster().getPresence(contactIdentifier);
  if (p != null)   return jabberStatusToPresenceStatus(p.getMode());
 else   return JabberStatusEnum.OFFLINE;
}
