{
  Presence presence=jabberProvider.getConnection().getRoster().getPresence(contactIdentifier);
  if (presence != null)   return jabberStatusToPresenceStatus(presence,jabberProvider);
 else   return jabberProvider.getJabberStatusEnum().getStatus(JabberStatusEnum.OFFLINE);
}
