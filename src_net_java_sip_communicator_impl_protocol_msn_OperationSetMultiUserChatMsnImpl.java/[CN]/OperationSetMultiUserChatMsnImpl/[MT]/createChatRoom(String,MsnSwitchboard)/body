{
synchronized (chatRoomCache) {
    ChatRoomMsnImpl chatRoom=new ChatRoomMsnImpl(chatRoomName,msnProvider,switchboard);
    this.chatRoomCache.put(chatRoom.getName(),chatRoom);
    return chatRoom;
  }
}
