{
  boolean formatIsSupported=false;
  if (format != null)   for (  Format supportedFormat : formatControl.getSupportedFormats())   if (supportedFormat.matches(format)) {
    formatIsSupported=true;
    break;
  }
  return formatIsSupported ? formatControl.getFormat() : null;
}
