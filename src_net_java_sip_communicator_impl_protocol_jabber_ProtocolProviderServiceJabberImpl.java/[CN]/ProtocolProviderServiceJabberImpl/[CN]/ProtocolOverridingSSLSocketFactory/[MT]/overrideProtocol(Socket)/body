{
  if (socket instanceof SSLSocket) {
    if (enabledProtocols != null && enabledProtocols.length > 0) {
      for (      String s : ((SSLSocket)socket).getEnabledProtocols())       System.err.println("ooo " + s);
    }
  }
  return socket;
}
