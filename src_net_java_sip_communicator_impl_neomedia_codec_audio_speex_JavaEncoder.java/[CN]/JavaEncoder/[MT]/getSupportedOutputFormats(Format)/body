{
  if (in == null)   return DEFAULT_OUTPUT_FORMATS;
  if (!(in instanceof AudioFormat) || (null == AbstractCodecExt.matches(in,inputFormats)))   return new Format[0];
  return getMatchingOutputFormats(in);
}
