{
  fixture.mockP1ServiceRegistration=MetaContactListServiceLick.registerMockProviderService(fixture.mockP1);
  fixture.mockP2ServiceRegistration=MetaContactListServiceLick.registerMockProviderService(fixture.mockP2);
  fixture.metaP1Grp1=fixture.metaClService.findMetaContactGroupByContactGroup(fixture.mockP1Grp1);
  assertNotNull("The MCL impl ignored a newly added proto provider.",fixture.metaP1Grp1);
  MclSlickFixture.assertGroupEquals(fixture.mockP1Grp1,fixture.metaP1Grp1,false);
  fixture.metaP2Grp1=fixture.metaClService.findMetaContactGroupByContactGroup(fixture.mockP2Grp1);
  assertNotNull("The MCL impl ignored a newly added proto provider.",fixture.metaP2Grp1);
  MclSlickFixture.assertGroupEquals(fixture.mockP2Grp1,fixture.metaP2Grp1,false);
}
