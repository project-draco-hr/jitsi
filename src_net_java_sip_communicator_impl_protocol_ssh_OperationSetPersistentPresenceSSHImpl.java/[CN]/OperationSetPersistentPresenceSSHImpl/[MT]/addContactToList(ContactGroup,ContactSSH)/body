{
  ((ContactGroupSSHImpl)parent).addContact(sshContact);
  fireSubscriptionEvent(sshContact,parent,SubscriptionEvent.SUBSCRIPTION_CREATED);
  fireContactPresenceStatusChangeEvent(sshContact,parent,SSHStatusEnum.NOT_AVAILABLE);
  sshContact.startTimerTask();
}
