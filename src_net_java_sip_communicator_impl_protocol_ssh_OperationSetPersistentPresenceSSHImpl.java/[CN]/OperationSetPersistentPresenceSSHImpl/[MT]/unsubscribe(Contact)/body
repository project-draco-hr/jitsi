{
  ContactGroupSSHImpl parentGroup=(ContactGroupSSHImpl)((ContactSSHImpl)contact).getParentContactGroup();
  parentGroup.removeContact((ContactSSHImpl)contact);
  fireSubscriptionEvent((ContactSSHImpl)contact,((ContactSSHImpl)contact).getParentContactGroup(),SubscriptionEvent.SUBSCRIPTION_REMOVED);
}
