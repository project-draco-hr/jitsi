{
  SourcePacketExtension copy=AbstractPacketExtension.clone(this);
  for (  ParameterPacketExtension ppe : getParameters()) {
    ParameterPacketExtension ppeCopy=new ParameterPacketExtension(ppe.getName(),ppe.getValue());
    copy.addParameter(ppeCopy);
  }
  return copy;
}
