{
  super.setTabLayoutPolicy(JTabbedPane.SCROLL_TAB_LAYOUT);
  if (enhancedUI)   paneUI=new CloseTabPaneEnhancedUI();
 else   paneUI=new CloseTabPaneUI();
  super.setUI(paneUI);
  this.setMaxIcon(false);
}
