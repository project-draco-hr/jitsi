{
synchronized (messageWaitingNotificationListeners) {
    List<?> l=this.messageWaitingNotificationListeners.get(type);
    if (l != null)     this.messageWaitingNotificationListeners.remove(listener);
  }
}
