{
  if (direction.allowsSending() && (processor.getState() != Processor.Started)) {
    processor.start();
    if (logger.isTraceEnabled())     logger.trace("Started Processor with hashCode " + processor.hashCode());
  }
}
