{
  if (direction == null)   throw new NullPointerException("direction");
  this.direction=this.direction.or(direction);
  if (this.direction.allowsSending()) {
    Processor processor=getProcessor();
    if (processor != null)     startProcessorInAccordWithDirection(processor);
  }
}
