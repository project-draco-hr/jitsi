{
  if (direction == null)   throw new NullPointerException("direction");
  if (MediaDirection.SENDRECV.equals(direction) || MediaDirection.SENDONLY.equals(direction)) {
    Processor processor=getProcessor();
    if ((processor != null) && (processor.getState() != Processor.Started))     processor.start();
  }
}
