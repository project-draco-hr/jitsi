{
  if (this.mute != mute) {
    this.mute=mute;
    DataSource captureDevice=this.captureDevice;
    if (captureDevice instanceof MutePushBufferDataSource)     ((MutePushBufferDataSource)captureDevice).setMute(this.mute);
  }
}
