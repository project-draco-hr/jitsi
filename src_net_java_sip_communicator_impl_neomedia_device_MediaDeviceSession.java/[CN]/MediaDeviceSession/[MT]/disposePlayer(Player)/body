{
synchronized (players) {
    Iterator<Map.Entry<DataSource,Player>> playerIter=players.entrySet().iterator();
    while (playerIter.hasNext())     if (playerIter.next().getValue().equals(player)) {
      playerIter.remove();
      break;
    }
    player.stop();
    player.deallocate();
    player.close();
  }
}
