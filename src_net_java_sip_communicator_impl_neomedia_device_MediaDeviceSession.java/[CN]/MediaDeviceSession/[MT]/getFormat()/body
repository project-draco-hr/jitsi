{
  if (logger.isDebugEnabled() && (processor != null)) {
    Format processorFormat=getProcessorFormat();
    Format format=(this.format == null) ? null : this.format.getFormat();
    boolean processorFormatEqualsFormat=(processorFormat == null) ? (format == null) : processorFormat.equals(format);
    if (!processorFormatEqualsFormat)     logger.debug("processorFormat != format");
  }
  return format;
}
