{
  Format jmfFormat=(processor == null) ? this.format : getProcessorFormat();
  if (jmfFormat != null) {
    MediaFormat format=MediaFormatImpl.createInstance(jmfFormat);
    return format;
  }
  return null;
}
