{
  if (event instanceof RealizeCompleteEvent) {
    Player player=(Player)event.getSourceController();
    if (player != null) {
      if (logger.isTraceEnabled())       logger.trace("Realized Player with hashCode " + player.hashCode());
      player.start();
      realizeComplete(player);
    }
  }
}
