{
  DataSource captureDevice=this.captureDevice;
  if (captureDevice == null)   return mute;
  if (captureDevice instanceof MutePushBufferDataSource)   return ((MutePushBufferDataSource)captureDevice).isMute();
  return false;
}
