{
  for (  ChatRoom joinedChatRoom : multiUserChat.getCurrentlyJoinedChatRooms()) {
    joinedChatRoom.leave();
  }
  final IrcConnection connection=this.ircStack.getConnection();
  if (connection != null) {
    this.ircStack.disconnect();
  }
}
