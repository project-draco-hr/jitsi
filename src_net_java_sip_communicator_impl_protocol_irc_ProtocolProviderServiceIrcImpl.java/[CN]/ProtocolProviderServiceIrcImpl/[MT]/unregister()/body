{
  Iterator joinedChatRooms=multiUserChat.getCurrentlyJoinedChatRooms().iterator();
  while (joinedChatRooms.hasNext()) {
    ChatRoom joinedChatRoom=(ChatRoom)joinedChatRooms.next();
    joinedChatRoom.leave();
  }
  if (ircStack.isConnected())   ircStack.disconnect();
}
