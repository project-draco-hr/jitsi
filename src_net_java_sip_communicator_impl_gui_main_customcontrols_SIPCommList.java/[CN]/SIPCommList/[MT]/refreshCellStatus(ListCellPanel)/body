{
  for (int i=0; i < this.listPanel.getComponentCount(); i++) {
    ListCellPanel cpanel=(ListCellPanel)this.listPanel.getComponent(i);
    if (!cpanel.equals(cellSelected)) {
      cpanel.setSelected(false);
    }
  }
}
