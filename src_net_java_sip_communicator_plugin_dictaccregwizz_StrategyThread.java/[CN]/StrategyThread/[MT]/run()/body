{
  while (true) {
    if (this.needProcess()) {
      this.setRunning(true);
      this.process();
      this.processDone();
      this.setRunning(false);
    }
    try {
      this.sleep(500);
    }
 catch (    InterruptedException ie) {
      logger.info("DICT THREAD : " + ie);
    }
  }
}
