{
  ExportedWindow loginWindow=SystrayActivator.getUIService().getAuthenticationWindow(protocolProvider,realm,userCredentials,isUserNameEditable);
  loginWindow.setVisible(true);
  return userCredentials;
}
