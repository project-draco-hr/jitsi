{
  if (content == null)   return null;
  RtpDescriptionPacketExtension desc=getRtpDescription(content);
  if (desc != null) {
    String mediaTypeStr=desc.getMedia();
    if (mediaTypeStr != null)     return MediaType.parseString(mediaTypeStr);
  }
  return null;
}
