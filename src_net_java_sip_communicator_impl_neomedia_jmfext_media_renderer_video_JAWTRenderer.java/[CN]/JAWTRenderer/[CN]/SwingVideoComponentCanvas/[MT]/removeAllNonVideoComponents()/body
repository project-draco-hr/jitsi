{
synchronized (nonVideoComponents) {
    Iterator<NonVideoComponent> nonVideoComponentIter=nonVideoComponents.iterator();
    while (nonVideoComponentIter.hasNext()) {
      NonVideoComponent nonVideoComponent=nonVideoComponentIter.next();
      nonVideoComponentIter.remove();
      nonVideoComponent.dispose();
    }
  }
}
