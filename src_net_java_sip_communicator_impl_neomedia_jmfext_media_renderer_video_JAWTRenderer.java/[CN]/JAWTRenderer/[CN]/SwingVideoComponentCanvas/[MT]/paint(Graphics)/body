{
  try {
synchronized (nonVideoComponents) {
      for (      NonVideoComponent nonVideoComponent : nonVideoComponents)       nonVideoComponent.paint();
    }
  }
  finally {
    super.paint(g);
  }
}
