{
synchronized (getHandleLock()) {
synchronized (nonVideoComponents) {
      for (      NonVideoComponent nonVideoComponent : nonVideoComponents) {
        if (nonVideoComponent.component.equals(c))         return;
      }
      nonVideoComponents.add(new NonVideoComponent(c,getHandle()));
    }
  }
}
