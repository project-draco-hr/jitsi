{
  if (component == null) {
    component=new Canvas(){
      @Override public void paint(      Graphics g){
synchronized (JAWTRenderer.this) {
          if (handle != 0)           JAWTRenderer.this.paint(handle,this,g);
        }
      }
    }
;
  }
  return component;
}
