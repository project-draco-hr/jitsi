{
  if (stream == null) {
    try {
      stream=PortAudioManager.getInstance().getOutputStream(deviceIndex,inputFormat.getSampleRate(),inputFormat.getChannels());
    }
 catch (    PortAudioException paex) {
      throw new ResourceUnavailableException(paex.getMessage());
    }
  }
}
