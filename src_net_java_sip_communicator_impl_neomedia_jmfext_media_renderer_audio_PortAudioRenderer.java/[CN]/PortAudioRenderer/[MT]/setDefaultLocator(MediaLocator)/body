{
  if (PortAudioRenderer.defaultLocator == null) {
    if (defaultLocator == null)     return;
  }
 else   if (PortAudioRenderer.defaultLocator.equals(defaultLocator))   return;
  PortAudioRenderer.defaultLocator=defaultLocator;
}
