{
  try {
    stream.write((byte[])buffer.getData(),buffer.getOffset(),buffer.getLength());
  }
 catch (  PortAudioException paex) {
    logger.error("Error writing to device",paex);
  }
  return BUFFER_PROCESSED_OK;
}
