{
  if (logger.isDebugEnabled())   logger.debug("mDNS provisioning discovery Service [STARTED]");
  bundleContext.registerService(ProvisioningDiscoveryService.class.getName(),provisioningService,null);
  if (logger.isDebugEnabled())   logger.debug("mDNS provisioning discovery Service [REGISTERED]");
}
