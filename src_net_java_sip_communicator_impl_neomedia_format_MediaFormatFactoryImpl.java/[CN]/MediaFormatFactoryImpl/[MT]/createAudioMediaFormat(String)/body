{
  for (  MediaFormat format : MediaUtils.encodingToMediaFormats(encoding))   if (format instanceof AudioMediaFormat)   return (AudioMediaFormat)format;
  return new AudioMediaFormatImpl(encodingToJmfEncoding(encoding));
}
