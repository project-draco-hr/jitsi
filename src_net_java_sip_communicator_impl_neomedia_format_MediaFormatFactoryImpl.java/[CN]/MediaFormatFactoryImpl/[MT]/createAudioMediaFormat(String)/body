{
  for (  MediaFormat format : MediaUtils.getMediaFormats(encoding))   if (format instanceof AudioMediaFormat)   return (AudioMediaFormat)format;
  return new AudioMediaFormatImpl(encodingToJmfEncoding(encoding));
}
