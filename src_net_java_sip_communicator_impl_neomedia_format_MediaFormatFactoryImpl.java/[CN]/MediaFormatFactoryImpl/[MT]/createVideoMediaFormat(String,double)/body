{
  for (  MediaFormat format : MediaUtils.encodingToMediaFormats(encoding))   if ((format instanceof VideoMediaFormat) && (format.getClockRate() == clockRate))   return (VideoMediaFormat)format;
  return new VideoMediaFormatImpl(encodingToJmfEncoding(encoding),clockRate);
}
