{
  if (encoding != null) {
    int encodingLength=encoding.length();
    if ((encodingLength < 4) || !"/rtp".equalsIgnoreCase(encoding.substring(encodingLength - 4)))     encoding+="/rtp";
  }
  return encoding;
}
