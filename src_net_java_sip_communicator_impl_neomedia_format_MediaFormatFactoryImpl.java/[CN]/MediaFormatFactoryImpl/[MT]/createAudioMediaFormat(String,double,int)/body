{
  for (  MediaFormat format : getSupportedMediaFormats(encoding,clockRate))   if (format instanceof AudioMediaFormat) {
    AudioMediaFormat audioFormat=(AudioMediaFormat)format;
    if (audioFormat.getChannels() == channels)     return audioFormat;
  }
  return null;
}
