{
  for (  MediaFormat format : MediaUtils.getMediaFormats(encoding))   if ((format instanceof AudioMediaFormat) && (format.getClockRate() == clockRate) && MediaFormatImpl.formatParametersAreEqual(format.getFormatParameters(),formatParams))   return (AudioMediaFormat)format;
  return new AudioMediaFormatImpl(encodingToJmfEncoding(encoding),clockRate,formatParams);
}
