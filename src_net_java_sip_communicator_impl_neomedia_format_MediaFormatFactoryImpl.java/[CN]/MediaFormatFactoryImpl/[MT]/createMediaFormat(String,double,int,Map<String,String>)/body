{
  for (  MediaFormat format : getSupportedMediaFormats(encoding,clockRate)) {
    if (AbstractMediaStream.matches(format,format.getMediaType(),format.getEncoding(),format.getClockRate(),channels,fmtps))     return format;
  }
  return null;
}
