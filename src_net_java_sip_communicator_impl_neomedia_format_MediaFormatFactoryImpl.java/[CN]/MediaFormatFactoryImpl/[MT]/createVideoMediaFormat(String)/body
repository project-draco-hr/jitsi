{
  for (  MediaFormat format : MediaUtils.getMediaFormats(encoding))   if (format instanceof VideoMediaFormat)   return (VideoMediaFormat)format;
  return new VideoMediaFormatImpl(encodingToJmfEncoding(encoding));
}
