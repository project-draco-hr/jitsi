{
  for (  MediaFormat format : MediaUtils.encodingToMediaFormats(encoding))   if (format instanceof VideoMediaFormat)   return (VideoMediaFormat)format;
  return new VideoMediaFormatImpl(encodingToJmfEncoding(encoding));
}
