{
  for (  MediaFormat format : MediaUtils.getMediaFormats(encoding))   if ((format instanceof AudioMediaFormat) && (format.getClockRate() == clockRate))   return (AudioMediaFormat)format;
  return new AudioMediaFormatImpl(encodingToJmfEncoding(encoding),clockRate);
}
