{
  audioFormat=new AudioFormat(AudioFormat.LINEAR,8000.0,16,1,AudioFormat.LITTLE_ENDIAN,AudioFormat.SIGNED,16,Format.NOT_SPECIFIED,Format.byteArray);
  maxDataLength=160;
  jni_alsa_init();
  thread=new Thread(this);
}
