{
  QualityControls qControls=getMediaQualityControls();
  if (qControls == null)   return remoteSendMaxPreset;
  QualityPresets qp=qControls.getRemoteSendMaxPreset();
  if (qp != null && maxFrameRate > 0)   qp=new QualityPresets(qp.getResolution(),(int)maxFrameRate);
  return qp;
}
