{
  if (qualityControls != null)   return qualityControls;
  MediaStream stream=peer.getMediaHandler().getStream(MediaType.VIDEO);
  if (stream != null && stream instanceof VideoMediaStream)   qualityControls=((VideoMediaStream)stream).getQualityControls();
  return qualityControls;
}
