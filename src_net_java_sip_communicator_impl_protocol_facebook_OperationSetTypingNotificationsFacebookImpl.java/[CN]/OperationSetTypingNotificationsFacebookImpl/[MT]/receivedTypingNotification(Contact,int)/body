{
  if (!(fromContact instanceof ContactFacebookImpl))   throw new IllegalArgumentException("The specified contact is not a Facebook contact." + fromContact);
  fireTypingNotificationsEvent(fromContact,typingState);
}
