{
  CallParticipantSipImpl callParticipant=activeCallsRepository.findCallParticipant(dialogTerminatedEvent.getDialog());
  if (callParticipant == null) {
    return;
  }
  callParticipant.setState(CallParticipantState.DISCONNECTED);
}
