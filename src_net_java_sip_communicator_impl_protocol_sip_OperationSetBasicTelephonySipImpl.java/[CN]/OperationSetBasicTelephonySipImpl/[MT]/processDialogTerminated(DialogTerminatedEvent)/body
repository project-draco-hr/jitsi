{
  CallPeerSipImpl callParticipant=activeCallsRepository.findCallParticipant(dialogTerminatedEvent.getDialog());
  if (callParticipant == null) {
    return false;
  }
  callParticipant.setState(CallPeerState.DISCONNECTED);
  return true;
}
