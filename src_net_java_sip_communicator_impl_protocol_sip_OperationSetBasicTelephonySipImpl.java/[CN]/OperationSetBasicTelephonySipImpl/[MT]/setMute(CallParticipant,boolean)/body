{
  CallParticipantSipImpl sipParticipant=(CallParticipantSipImpl)participant;
  ((CallSipImpl)sipParticipant.getCall()).getMediaCallSession().setMute(mute);
  if (mute)   sipParticipant.setState(CallParticipantState.MUTED);
 else   sipParticipant.setState(CallParticipantState.CONNECTED);
}
