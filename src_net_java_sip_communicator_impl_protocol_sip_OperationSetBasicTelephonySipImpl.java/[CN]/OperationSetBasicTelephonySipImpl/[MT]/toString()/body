{
  String className=getClass().getName();
  try {
    className=className.substring(className.lastIndexOf('.') + 1);
  }
 catch (  Exception ex) {
  }
  return className + "-[dn=" + protocolProvider.getOurDisplayName()+ " addr="+ protocolProvider.getOurSipAddress()+ "]";
}
