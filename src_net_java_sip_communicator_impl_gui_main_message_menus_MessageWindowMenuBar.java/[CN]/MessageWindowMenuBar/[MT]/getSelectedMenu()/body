{
  int menuCount=this.getMenuCount();
  for (int i=0; i < menuCount; i++) {
    SIPCommMenu menu=(SIPCommMenu)this.getMenu(i);
    if (menu.isSelected()) {
      return menu;
    }
  }
  return null;
}
