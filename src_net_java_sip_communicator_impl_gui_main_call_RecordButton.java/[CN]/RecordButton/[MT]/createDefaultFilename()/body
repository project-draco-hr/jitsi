{
  String callsDir=configurationService.getString(Recorder.SAVED_CALLS_PATH);
  if (callsDir == null) {
    try {
      callsDir=GuiActivator.getFileAccessService().getDefaultDownloadDirectory().getAbsolutePath();
    }
 catch (    IOException ioex) {
    }
  }
  String ext=configurationService.getString(Recorder.CALL_FORMAT);
  if (ext == null)   ext=SoundFileUtils.mp2;
  return ((callsDir == null) ? "" : (callsDir + File.separator)) + generateCallFilename(ext);
}
