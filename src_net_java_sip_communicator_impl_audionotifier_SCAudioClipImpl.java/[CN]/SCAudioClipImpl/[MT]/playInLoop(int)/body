{
  this.loopInterval=interval;
  if (interval == 0)   audioClip.loop();
 else {
    audioClip.play();
    playAudioTimer.setDelay(interval);
    playAudioTimer.setRepeats(true);
    playAudioTimer.start();
  }
  this.isLooping=true;
}
