{
  Iterator settingKeys=GuiCustomizationActivator.getResources().getCurrentSettings();
  while (settingKeys.hasNext()) {
    String key=(String)settingKeys.next();
    String value=GuiCustomizationActivator.getResources().getSettingsString(key);
    settingsTableModel.addRow(new Object[]{key,value});
  }
}
