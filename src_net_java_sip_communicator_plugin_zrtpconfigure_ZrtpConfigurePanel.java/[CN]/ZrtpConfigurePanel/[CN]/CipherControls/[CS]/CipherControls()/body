{
  String id=ZrtpConfigureUtils.getPropertyID(ZrtpConstants.SupportedSymCiphers.AES1);
  String savedConf=ZrtpConfigureActivator.configService.getString(id);
  if (savedConf == null)   savedConf="";
  dataModel=new ZrtpConfigureTableModel<ZrtpConstants.SupportedSymCiphers>(ZrtpConstants.SupportedSymCiphers.AES1,active,inActive,savedConf);
  createControls(this,dataModel,ZrtpConfigureActivator.resourceService.getI18NString("impl.media.security.zrtp.SYM_CIPHERS"));
}
