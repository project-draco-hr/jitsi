{
  SystemActivityNotifications.stop();
  if (OSUtils.IS_LINUX) {
    NetworkManagerListenerImpl.getInstance().stop();
  }
  eventDispatcher.stop();
  running=false;
synchronized (this) {
    this.notifyAll();
  }
}
