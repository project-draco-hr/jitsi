{
  if (evt.getEventID() == SystemActivityEvent.EVENT_NETWORK_CHANGE && OSUtils.IS_WINDOWS) {
    eventDispatcher.fireSystemActivityEvent(evt,500);
  }
 else   eventDispatcher.fireSystemActivityEvent(evt);
}
