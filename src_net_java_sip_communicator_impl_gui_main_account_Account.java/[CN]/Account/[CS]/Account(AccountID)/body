{
  this.accountID=accountID;
  this.name=accountID.getDisplayName();
  this.icon=ImageLoader.getImageForPath(accountID.getAccountPropertyString(ProtocolProviderFactory.ACCOUNT_ICON_PATH));
  this.isEnabled=accountID.isEnabled();
}
