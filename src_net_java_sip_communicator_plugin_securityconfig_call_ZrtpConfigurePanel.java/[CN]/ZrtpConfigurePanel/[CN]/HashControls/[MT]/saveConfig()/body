{
  String value=getPropertyValue(ZrtpConstants.SupportedHashes.S256);
  String id=getPropertyID(ZrtpConstants.SupportedHashes.S256);
  SecurityConfigActivator.getConfigurationService().setProperty(id,value);
}
