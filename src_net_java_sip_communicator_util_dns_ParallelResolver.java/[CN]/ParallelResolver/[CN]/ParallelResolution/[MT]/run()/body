{
  Message localResponse=null;
  try {
    localResponse=defaultResolver.send(query);
  }
 catch (  Throwable exc) {
    this.exception=exc;
  }
synchronized (this) {
    if (done)     return;
    response=localResponse;
    done=true;
    notify();
  }
}
