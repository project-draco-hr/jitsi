{
  final MessageHistoryService msgHistory=GuiActivator.getMsgHistoryService();
  if (msgHistory == null)   return;
  loadHistoryPeriod();
  Collection historyList=msgHistory.findLast(metaContact,ConfigurationManager.getChatHistorySize());
  processHistory(historyList,escapedMessageID);
}
