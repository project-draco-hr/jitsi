{
  String displayName=metaContact.getDisplayName();
  if (displayName != null && displayName.length() > 0)   return metaContact.getDisplayName();
  return Messages.getI18NString("unknown").getText();
}
