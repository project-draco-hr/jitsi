{
  int startOffset=this.offset + offset;
  if (offset < 0 || length <= 0 || startOffset + length > this.buffer.length) {
    return null;
  }
  byte[] region=new byte[length];
  System.arraycopy(this.buffer,startOffset,region,0,length);
  return region;
}
