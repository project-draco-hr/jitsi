{
  Iterator<PropertyChangeListener> iter=propertyChangeListeners.iterator();
  while (iter.hasNext()) {
    PropertyChangeListener pl=(PropertyChangeListener)iter.next();
    pl.propertyChange(new PropertyChangeEvent(this,property,oldValue,newValue));
  }
}
