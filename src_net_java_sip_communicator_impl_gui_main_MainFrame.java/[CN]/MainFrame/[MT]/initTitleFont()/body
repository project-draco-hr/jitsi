{
  JComponent layeredPane=this.getLayeredPane();
  String fontName=GuiActivator.getResources().getSettingsString("fontName");
  String titleFontSize=GuiActivator.getResources().getSettingsString("titleFontSize");
  Font font=new Font(fontName,Font.BOLD,new Integer(titleFontSize).intValue());
  for (int i=0; i < layeredPane.getComponentCount(); i++) {
    layeredPane.getComponent(i).setFont(font);
  }
}
