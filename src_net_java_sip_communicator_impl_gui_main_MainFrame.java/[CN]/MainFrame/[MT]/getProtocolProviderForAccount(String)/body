{
  for (int i=0; i < protocolProviders.size(); i++) {
    ProtocolProviderService pps=(ProtocolProviderService)protocolProviders.get(i);
    if (pps.getAccountID().getAccountUserID().equals(accountName)) {
      return pps;
    }
  }
  return null;
}
