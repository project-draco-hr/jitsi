{
  Contact sourceContact=evt.getSourceContact();
  MetaContact metaContact=contactList.findMetaContactByContact(sourceContact);
  if (metaContact != null && ((evt.getOldStatus() != evt.getNewStatus()) || sourceContact.getStatusMessage() != null)) {
    contactListPanel.getContactList().refreshContact(metaContact);
  }
}
