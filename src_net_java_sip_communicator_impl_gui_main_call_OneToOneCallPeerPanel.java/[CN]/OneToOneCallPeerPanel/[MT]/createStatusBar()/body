{
  callStatusLabel.setForeground(Color.WHITE);
  callStatusLabel.setText(callPeer.getState().getStateString());
  PeerStatusPanel statusPanel=new PeerStatusPanel(new FlowLayout(FlowLayout.CENTER,10,0));
  TransparentPanel statusIconsPanel=new TransparentPanel(new FlowLayout(FlowLayout.CENTER,5,0));
  statusIconsPanel.add(securityStatusLabel);
  statusIconsPanel.add(holdStatusLabel);
  statusIconsPanel.add(muteStatusLabel);
  statusIconsPanel.add(callStatusLabel);
  statusPanel.add(statusIconsPanel);
  Component[] buttons=new Component[]{CallPeerRendererUtils.createTransferCallButton(callPeer),CallPeerRendererUtils.createEnterFullScreenButton(this)};
  Component buttonBar=CallPeerRendererUtils.createButtonBar(false,buttons);
  statusPanel.add(buttonBar);
  return statusPanel;
}
