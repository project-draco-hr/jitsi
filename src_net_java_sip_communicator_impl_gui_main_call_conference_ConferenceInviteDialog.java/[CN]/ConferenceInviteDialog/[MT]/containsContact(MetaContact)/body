{
  if (call == null)   return false;
  Iterator<? extends CallPeer> callPeers=call.getCallPeers();
  while (callPeers.hasNext()) {
    CallPeer callPeer=callPeers.next();
    if (metaContact.containsContact(callPeer.getContact()))     return true;
  }
  return false;
}
