{
  bundleContext=bc;
  resPackImpl=new ResourceManagementServiceImpl();
  bundleContext.registerService(ResourceManagementService.class.getName(),resPackImpl,null);
  if (logger.isInfoEnabled())   logger.info("Resource manager ... [REGISTERED]");
}
