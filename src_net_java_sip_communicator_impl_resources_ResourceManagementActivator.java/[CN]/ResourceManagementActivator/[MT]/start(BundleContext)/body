{
  bundleContext=bc;
  ResourceManagementServiceImpl resPackImpl=new ResourceManagementServiceImpl();
  bundleContext.registerService(ResourceManagementService.class.getName(),resPackImpl,null);
  logger.info("Default resources ... [REGISTERED]");
}
