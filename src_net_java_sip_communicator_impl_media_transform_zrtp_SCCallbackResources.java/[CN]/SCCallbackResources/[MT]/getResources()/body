{
  if (resourcesService == null) {
    ServiceReference serviceReference=MediaActivator.getBundleContext().getServiceReference(ResourceManagementService.class.getName());
    if (serviceReference == null)     return null;
    resourcesService=(ResourceManagementService)MediaActivator.getBundleContext().getService(serviceReference);
  }
  return resourcesService;
}
