{
  if (format instanceof AudioFormat)   return new AudioMediaFormatImpl((AudioFormat)format);
  if (format instanceof VideoFormat)   return new VideoMediaFormatImpl((VideoFormat)format);
  return null;
}
