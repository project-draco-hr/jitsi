{
  int status=(presenceStatus == null) ? Integer.MIN_VALUE : presenceStatus.getStatus();
  if (this.presenceStatus != status) {
    int oldValue=this.presenceStatus;
    this.presenceStatus=status;
    Messenger.onContactStatusChange(signinName,presenceStatusToMISTATUS(oldValue));
  }
}
