{
  super("Facebook error " + code);
  this.code=code;
  if (code == FacebookErrorException.kError_Async_NotLoggedIn || code == FacebookErrorException.kError_Async_LoginChanged) {
    session.logout();
  }
}
