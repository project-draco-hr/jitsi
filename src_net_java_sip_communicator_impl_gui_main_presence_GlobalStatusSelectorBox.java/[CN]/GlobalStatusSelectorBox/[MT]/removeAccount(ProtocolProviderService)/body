{
  StatusEntry menu=getStatusEntry(protocolProvider);
  if (menu != null) {
    menu.dispose();
    remove(menu.getEntryComponent());
  }
  if (!isPresenceOpSetForProvidersAvailable())   removeAvailableStatuses();
}
