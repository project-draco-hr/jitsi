{
  Enumeration statusMenus=accountMenus.elements();
  while (statusMenus.hasMoreElements()) {
    StatusSelectorMenu statusSelectorMenu=(StatusSelectorMenu)statusMenus.nextElement();
    if (statusSelectorMenu.isSelected()) {
      return true;
    }
  }
  return false;
}
