{
  ChatRoomWrapper room=rooms.get(rowIndex);
switch (columnIndex) {
case 0:
    return room;
case 1:
  return room.getParentProvider().getProtocolProvider();
case 2:
return room.isAutojoin();
default :
return room;
}
}
