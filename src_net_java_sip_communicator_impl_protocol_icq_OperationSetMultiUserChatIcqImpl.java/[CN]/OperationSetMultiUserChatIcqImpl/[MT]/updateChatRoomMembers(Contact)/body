{
  Enumeration<ChatRoomIcqImpl> chatRooms=chatRoomCache.elements();
  while (chatRooms.hasMoreElements()) {
    ChatRoomIcqImpl chatRoom=chatRooms.nextElement();
    ChatRoomMemberIcqImpl member=chatRoom.findMemberForNickName(contact.getAddress());
    if (member != null) {
      member.setContact(contact);
      if (contact != null)       member.setAvatar(contact.getImage());
    }
  }
}
