{
  for (  ChatRoom chatRoom : chatRoomCache.values()) {
    ChatRoomMemberIcqImpl member=((ChatRoomIcqImpl)chatRoom).findMemberForNickName(contact.getAddress());
    if (member != null) {
      member.setContact(contact);
      member.setAvatar(contact.getImage());
    }
  }
}
