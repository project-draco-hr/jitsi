{
synchronized (chatRoomCache) {
    ChatRoom newChatRoom=new ChatRoomIcqImpl(chatInvitation,icqProvider);
    chatRoomCache.put(chatInvitation.getRoomName(),newChatRoom);
    return newChatRoom;
  }
}
