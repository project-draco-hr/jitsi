{
  MediaService mediaService=SipActivator.getMediaService();
  MediaStream stream=mediaService.createMediaStream(connector,device);
  stream.setFormat(format);
  stream.setTarget(target);
  if (stream instanceof AudioMediaStream)   this.audioStream=(AudioMediaStream)stream;
 else   this.videoStream=(VideoMediaStream)stream;
  stream.start();
  return stream;
}
