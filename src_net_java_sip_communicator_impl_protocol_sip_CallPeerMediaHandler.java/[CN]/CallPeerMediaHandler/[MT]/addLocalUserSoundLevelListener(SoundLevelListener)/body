{
synchronized (localSoundLevelListeners) {
    if (!localSoundLevelListeners.contains(l))     localSoundLevelListeners.add(l);
  }
  if (audioStream != null) {
    audioStream.addLocalUserSoundLevelListener(l);
  }
}
