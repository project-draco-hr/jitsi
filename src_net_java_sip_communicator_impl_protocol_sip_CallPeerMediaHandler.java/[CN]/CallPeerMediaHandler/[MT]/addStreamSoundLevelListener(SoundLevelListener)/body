{
synchronized (streamSoundLevelListeners) {
    if (!streamSoundLevelListeners.contains(listener))     streamSoundLevelListeners.add(listener);
  }
  if (audioStream != null) {
    audioStream.addStreamAudioLevelListener(listener);
  }
}
