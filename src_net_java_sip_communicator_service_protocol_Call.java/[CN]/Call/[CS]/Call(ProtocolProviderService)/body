{
  this.callID=String.valueOf(System.currentTimeMillis()) + String.valueOf(super.hashCode());
  this.protocolProvider=sourceProvider;
  AccountID accountID=sourceProvider.getAccountID();
  String defaultEncryptionObj=(String)accountID.getAccountProperties().get(ProtocolProviderFactory.DEFAULT_ENCRYPTION);
  defaultEncryption=false;
  if (defaultEncryptionObj != null) {
    defaultEncryption=Boolean.valueOf(defaultEncryptionObj).booleanValue();
  }
}
